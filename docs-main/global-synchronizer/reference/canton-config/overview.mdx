---
title: "Canton Configuration Reference"
description: "Every configuration key a Canton node accepts, generated from the Canton sources."
---

{/* GENERATED_FROM source="digital-asset/canton" ref="a865b90824a94d307d329eea3d9d5fb4fd53dfb0" version="3.7.0-SNAPSHOT" path="docs-open/target/config-reference.json" */}

Every configuration key Canton 3.7.0-SNAPSHOT accepts: 2452 keys, 1430 (58%) with a description. Generated from the Canton sources; do not edit by hand.

<CardGroup cols={2}>
  <Card title="Participant node" href="/global-synchronizer/reference/canton-config/participant-node">
    Ledger API, admin API, storage, crypto and parameters of a participant. 791 keys under `canton.participants.<participant>`.
  </Card>
  <Card title="Sequencer node" href="/global-synchronizer/reference/canton-config/sequencer-node">
    Sequencer implementation, public and admin APIs, storage and ordering. 965 keys under `canton.sequencers.<sequencer>`.
  </Card>
  <Card title="Mediator node" href="/global-synchronizer/reference/canton-config/mediator-node">
    Admin API, storage, crypto and parameters of a mediator. 412 keys under `canton.mediators.<mediator>`.
  </Card>
  <Card title="Remote participant" href="/global-synchronizer/reference/canton-config/remote-participant">
    How the console reaches a participant running elsewhere. 42 keys under `canton.remote-participants.<remote-participant>`.
  </Card>
  <Card title="Remote sequencer" href="/global-synchronizer/reference/canton-config/remote-sequencer">
    How the console reaches a sequencer running elsewhere. 53 keys under `canton.remote-sequencers.<remote-sequencer>`.
  </Card>
  <Card title="Remote mediator" href="/global-synchronizer/reference/canton-config/remote-mediator">
    How the console reaches a mediator running elsewhere. 21 keys under `canton.remote-mediators.<remote-mediator>`.
  </Card>
  <Card title="Monitoring" href="/global-synchronizer/reference/canton-config/monitoring">
    Logging, metrics, tracing and health checks. 94 keys under `canton.monitoring`.
  </Card>
  <Card title="Global parameters" href="/global-synchronizer/reference/canton-config/global-parameters">
    Process-wide settings: clock, timeouts, startup behaviour. 71 keys under `canton.parameters`.
  </Card>
  <Card title="Features" href="/global-synchronizer/reference/canton-config/features">
    Feature flags for preview, testing and repair commands. 3 keys under `canton.features`.
  </Card>
  <Card title="Configuration types" href="/global-synchronizer/reference/canton-config/types">
    Every section that takes a `type`, collected from all pages, with the keys each type accepts and an example of each.
  </Card>
</CardGroup>

## How to read these pages

Canton is configured with [HOCON](https://github.com/lightbend/config/blob/master/HOCON.md). Scala field names appear as lowercase-with-dashes, so `maxInboundMessageSize` is written `max-inbound-message-size`. **The key set is closed**: an unrecognised key fails startup rather than being ignored, so a typo is caught immediately.

Paths are rooted at `canton`. Segments in angle brackets are names you choose:

- `<additional-header>` — `user-chosen name of each entry under canton.monitoring.tracing.tracer.exporter.additional-headers`
- `<annotation>` — `user-chosen name of each entry under canton.participants.<participant>.alpha-dynamic.users[].annotations`
- `<connection>` — `user-chosen name of each entry under canton.participants.<participant>.alpha-dynamic.connections[].connections`
- `<custom-tag>` — `user-chosen name of each entry under canton.participants.<participant>.crypto.kms.custom-tags`
- `<extension>` — `user-chosen name of each entry under canton.participants.<participant>.parameters.engine.extensions`
- `<key>` — `user-chosen name of each entry under canton.participants.<participant>.admin-api.limits.active`
- `<mediator>` — `user-chosen name of each entry under canton.mediators`
- `<participant>` — `user-chosen name of each entry under canton.participants`
- `<remote-mediator>` — `user-chosen name of each entry under canton.remote-mediators`
- `<remote-participant>` — `user-chosen name of each entry under canton.remote-participants`
- `<remote-sequencer>` — `user-chosen name of each entry under canton.remote-sequencers`
- `<request-header>` — `user-chosen name of each entry under canton.participants.<participant>.alpha-dynamic.dars[].request-headers`
- `<sequencer>` — `user-chosen name of each entry under canton.sequencers`
- `<skew>` — `user-chosen name of each entry under canton.parameters.clock.skews`

A `[]` suffix marks the fields of a list element. Keys marked **alpha** or **beta** are not covered by compatibility guarantees.

**Minimum Required Fields** is the smallest configuration that starts: keys Canton's startup validation demands (a node's ports), and keys with no default inside sections that are not constructed by default, with when each applies: always, unless you disable the service, or once you have chosen a particular `type`. A key marked **required** elsewhere on the page is required only if you write the section that holds it. **All options** covers every key, one section per heading, where the heading is the section's absolute path. Each is shown two ways behind tabs: **HOCON** as you would write it from the root, every key with its default, and **Table**, the same keys relative to the heading with their descriptions. Where a section takes a `type`, the HOCON tab enumerates one complete block per type after the section block, and the table groups that type's keys under a `type = …` header; in the table a **bold** row names a section and the keys indented beneath it live inside it. In the Required skeleton a key needed only under a particular `type` is a comment carrying that condition.

Some sections take a `type` that decides which other keys they accept. **Configuration types** at the end of each page documents every such type the page uses, one block and one table per type, and the [types page](/global-synchronizer/reference/canton-config/types) collects them all.

In the Default column:

- a value is what the key holds when you do not set it;
- **required** means startup fails unless you set it;
- _unset_ means the feature is off, or the surrounding section is absent, until you set it;
- † marks a value that was not written in the source but was seen in a loaded configuration. It is an example from the environment the defaults were captured in, not a guaranteed default;
- ‡ marks a default written as an expression in the source that has not been evaluated; the expression is shown so you can find it;
- ◊ marks a field that declares one default but was seen holding another in a running configuration, because the section that builds it passes an explicit value. Both are shown.

## Provenance

| | |
|---|---|
| Canton version | `3.7.0-SNAPSHOT` |
| Commit | `a865b90824a94d307d329eea3d9d5fb4fd53dfb0` |
| Artifact schema | `2.2.0` |
| Load-time defaults from | `default-3.5.17.conf` |

## Renamed and moved keys

Canton still reads the old path and logs a notice. Paths are relative to the config class that moved them.

| Old path | Reads as | Since | Declared by |
|---|---|---|---|
| `kms.session-signing-keys` | `session-signing-keys` | 3.5.0 | `CryptoConfig` |
| `keep-alive-client` | `channel.keep-alive-client` | 3.5.0 | `FullClientConfig` |
| `handshake-retry` | `handshake.retry` | 3.5.1 | `LsuConfig` |
| `features.profileDir` | `parameters.engine` | 3.5.0 | `ParticipantNodeConfig` |
| `features.snapshotDir` | `parameters.engine` | 3.5.0 | `ParticipantNodeConfig` |
| `http-ledger-api.server` | `http-ledger-api` | 3.4.0 | `ParticipantNodeConfig` |
| `traffic-enforcement` | `traffic-accounting` | 3.6.0 | `ParticipantNodeConfig` |
| `alpha-multi-synchronizer-support` | `enable-all-ledger-api-reassignments` | 3.5.4 | `ParticipantNodeParameterConfig` |
| `automatically-perform-logical-synchronizer-upgrade` | `automatically-perform-lsu` | 3.5.0 | `ParticipantNodeParameterConfig` |
| `automatically-perform-logical-synchronizer-upgrade` | `lsu.automatically-perform-lsu` | 3.6.0 | `ParticipantNodeParameterConfig` |
| `automatically-perform-lsu` | `lsu.automatically-perform-lsu` | 3.6.0 | `ParticipantNodeParameterConfig` |
| `unsafe-online-party-replication` | `alpha-online-party-replication-support` | 3.5.0 | `ParticipantNodeParameterConfig` |
| `keep-alive-client` | `channel.keep-alive-client` | 3.5.0 | `SequencerApiClientConfig` |
| `unsafe-enable-online-party-replication` | `unsafe-sequencer-channel-support` | 3.5.0 | `SequencerNodeParameterConfig` |
