```scala
// Bootstrap the synchronizer with the sequencer and mediator
synchronizer.setup.bootstrap(
  synchronizerName = "my-private-sync",
  sequencers = Seq(sequencer),
  mediators = Seq(mediator)
)
```
