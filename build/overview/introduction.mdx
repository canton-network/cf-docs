<a id="build-overview-introduction"></a>

# Introduction

<a id="build-overview-what-is-canton-network-application"></a>

## What is a Canton Network application

Canton Network applications are applications that use the Canton ledger to
operate business processes spanning multiple organizations or business entities.
The benefit of using the Canton ledger is that the state of these applications
is automatically synchronized across all participating organizations while
preserving privacy on a need-to-know basis.

Canton Network applications often build on the functionality of other
applications by composing with them. This composition can happen on-ledger using
composed Daml transactions as well as off-ledger by building on APIs exposed by
other Canton Network applications.

An example for on-ledger composition are trading applications that compose with
tokenization or asset registry applications and settle their trades on-ledger
using a Daml transaction to implement truly atomic DvP settlement. An example
for off-ledger composition is a block explorer for the Global Synchronizer built
on top of the Global Synchronizer Scan HTTP APIs.

Note that Canton Network applications do not require developing custom Daml code
by default. They can use the Daml code of other applications to read or update
the on-ledger state or even just build on the off-ledger APIs of other
applications. For example a block explorer does not require custom Daml code,
but it might rely on the Daml code that implements the tokenomics of the Global
Synchronizer to parse Canton Coin transactions and present them in their UI.

<a id="build-overview-how-to-build-canton-network-application"></a>

## How to build a Canton Network application

Use the documentation on this site to guide you through the following steps:

1. Get familiar with the technology:
   : 1. Read the [Key concepts of Canton Network applications](key_concepts.mdx#build-overview-key-concepts) to learn the key concepts
        that the rest of the documentation builds on.
     2. Follow the [Get started with Canton and the JSON Ledger API](../tutorials/json-api/canton_and_the_json_ledger_api.mdx#tutorial-canton-and-the-json-ledger-api) tutorial to
        learn about how to access the Canton ledger on a validator node.
     3. Follow the [Get started with smart contract development](../tutorials/smart-contracts/intro.mdx#smart-contract-development) tutorial if you
        want to learn about how to read and write Daml code.
     4. Browse the table of contents to the left to get an intuition for what
        documentation is there.
     5. Review the [SDK components](sdk_components.mdx#build-overview-sdk-components) to learn about
        what tools are available for you to build your application.

{/* todo: https://github.com/DACH-NY/docs-website/issues/425 - add this back as the second point in the list above once the quickstart docs are available
#. Follow the :brokenref:`“Get started with Canton Network App Dev Quickstart” <build_tutorial_cn_quickstart_get_started>`` to learn
   about how to run a full-fledged demo application against LocalNet or
   DevNet. */}
1. Scope your application and decide on its architecture:
   : 1. Use the  [Best practices for Canton Network application development](../sdlc-howtos/sdlc-best-practices.mdx#build-sdlc-howto-cn-app-dev-best-practices) to
        learn about special considerations for the software development lifecycle
        of Canton Network applications.
     2. Use the how-to guides in [System design](../sdlc-howtos/system-design/index.mdx#build-sdlc-howtos-system-design) to decide
        on common architectural questions.
2. Develop and deploy your application:
   : 1. Use the [Development how-tos](../sdlc-howtos/index.mdx#build-sdlc-howtos) for guidance on common tasks encountered
        during the software development lifecycle.
     2. Use the [Component how-tos](../component-howtos/index.mdx#build-component-howtos) for guidance on how to use the SDK
        components.

{/* todo: https://github.com/DACH-NY/docs-website/issues/425 - add this back once QS is available
#. Consider using the :brokenref:`Canton Network Application Development Quickstart <build_sdlc_howto_use_the_cn_quickstart>` to
   start with a fully working blueprint for wiring up the SDK components to
   build a Canton Network application with its own frontend, backend, and
   custom Daml code. */}
