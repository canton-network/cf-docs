---
title: "Remote participant"
description: "How the console reaches a participant running elsewhere."
---

{/* GENERATED_FROM source="digital-asset/canton" ref="9543a9170cbdaea12c1909dd1d96e8975b60bbdb" version="3.7.0-SNAPSHOT" path="docs-open/target/config-reference.json" */}

Configuration under `canton.remote-participants.<remote-participant>` for Canton 3.7.0-SNAPSHOT: 42 keys. Part of the [Canton configuration reference](/global-synchronizer/reference/canton-config/overview).

<Accordion title="How to read this page">

Canton is configured with [HOCON](https://github.com/lightbend/config/blob/master/HOCON.md). Scala field names appear as lowercase-with-dashes, so `maxInboundMessageSize` is written `max-inbound-message-size`. **The key set is closed**: an unrecognised key fails startup rather than being ignored, so a typo is caught immediately.

Paths are rooted at `canton`. Segments in angle brackets are names you choose:

- `<remote-participant>` — `user-chosen name of each entry under canton.remote-participants`

A `[]` suffix marks the fields of a list element. Keys marked **alpha** or **beta** are not covered by compatibility guarantees.

**Required** lists only what you must set, and when: always, when you configure a particular section, or when you have chosen a particular `type`. **All options** covers every key. Each part is labelled with its **Path**, the absolute location in the configuration, and shown two ways behind tabs: **HOCON** as you would write it from the root, every key with its default, and **Table**, the same keys relative to that path with their descriptions. A key that only applies under a particular `type` appears in the HOCON as a comment carrying that condition; in the table a **bold** row names a section and the keys indented beneath it live inside it.

Some sections take a `type` that decides which other keys they accept. Those are documented once on the [types page](/global-synchronizer/reference/canton-config/types) and linked from wherever they occur.

In the Default column:

- a value is what the key holds when you do not set it;
- **required** means startup fails unless you set it;
- _unset_ means the feature is off, or the surrounding section is absent, until you set it;
- † marks a value assigned while the configuration loads (a port, for example). The number shown is an example from the environment the defaults were captured in, not a guaranteed default;
- ‡ marks a default written as an expression in the source that has not been evaluated; the expression is shown so you can find it;
- ◊ marks a field that declares one default but was seen holding another in a running configuration, because the section that builds it passes an explicit value. Both are shown.

</Accordion>

## Required

**Path** `canton.remote-participants.<remote-participant>`

<Tabs>
<Tab title="HOCON">

```hocon
canton.remote-participants.<remote-participant> {
  admin-api {
    channel {
      max-inbound-message-size = 0   # required
      trace-context-propagation = enabled|disabled   # required
    }
    port = 0   # required
    tls {
      client-cert {
        cert-chain-file {
          # pem-bytes = "..."   # required when type = pem-string
          # pem-file = "/path/to/file"   # required when type = pem-file
          type = pem-file|pem-string   # required
        }
        private-key-file = "..."   # required
      }
      trust-collection-file {
        # pem-bytes = "..."   # required when type = pem-string
        # pem-file = "/path/to/file"   # required when type = pem-file
        type = pem-file|pem-string   # required
      }
    }
  }
  http-health {
    port = 0   # required
  }
  ledger-api {
    channel {
      max-inbound-message-size = 0   # required
      trace-context-propagation = enabled|disabled   # required
    }
    port = 0   # required
    tls {
      client-cert {
        cert-chain-file {
          # pem-bytes = "..."   # required when type = pem-string
          # pem-file = "/path/to/file"   # required when type = pem-file
          type = pem-file|pem-string   # required
        }
        private-key-file = "..."   # required
      }
      trust-collection-file {
        # pem-bytes = "..."   # required when type = pem-string
        # pem-file = "/path/to/file"   # required when type = pem-file
        type = pem-file|pem-string   # required
      }
    }
  }
  ledger-json-api {
    port = 0   # required
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required when | Description |
|---|---|---|---|
| `admin-api.channel.max-inbound-message-size` | int (NonNegativeInt) | `admin-api.channel` is configured |  |
| `admin-api.channel.trace-context-propagation` | one of `enabled`, `disabled` | `admin-api.channel` is configured |  |
| `admin-api.port` | int (Port) | `admin-api` is configured |  |
| `admin-api.tls.client-cert.cert-chain-file.pem-bytes` | string | `admin-api.tls.client-cert.cert-chain-file` is configured and `admin-api.tls.client-cert.cert-chain-file.type` is `pem-string` |  |
| `admin-api.tls.client-cert.cert-chain-file.pem-file` | path (ExistingFile) | `admin-api.tls.client-cert.cert-chain-file` is configured and `admin-api.tls.client-cert.cert-chain-file.type` is `pem-file` |  |
| `admin-api.tls.client-cert.cert-chain-file.type` | one of `pem-file`, `pem-string` | `admin-api.tls.client-cert.cert-chain-file` is configured | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). |
| `admin-api.tls.client-cert.private-key-file` | string (PemFile) | `admin-api.tls.client-cert` is configured |  |
| `admin-api.tls.trust-collection-file.pem-bytes` | string | `admin-api.tls.trust-collection-file` is configured and `admin-api.tls.trust-collection-file.type` is `pem-string` |  |
| `admin-api.tls.trust-collection-file.pem-file` | path (ExistingFile) | `admin-api.tls.trust-collection-file` is configured and `admin-api.tls.trust-collection-file.type` is `pem-file` |  |
| `admin-api.tls.trust-collection-file.type` | one of `pem-file`, `pem-string` | `admin-api.tls.trust-collection-file` is configured | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). |
| `http-health.port` | int (Port) | `http-health` is configured |  |
| `ledger-api.channel.max-inbound-message-size` | int (NonNegativeInt) | `ledger-api.channel` is configured |  |
| `ledger-api.channel.trace-context-propagation` | one of `enabled`, `disabled` | `ledger-api.channel` is configured |  |
| `ledger-api.port` | int (Port) | `ledger-api` is configured |  |
| `ledger-api.tls.client-cert.cert-chain-file.pem-bytes` | string | `ledger-api.tls.client-cert.cert-chain-file` is configured and `ledger-api.tls.client-cert.cert-chain-file.type` is `pem-string` |  |
| `ledger-api.tls.client-cert.cert-chain-file.pem-file` | path (ExistingFile) | `ledger-api.tls.client-cert.cert-chain-file` is configured and `ledger-api.tls.client-cert.cert-chain-file.type` is `pem-file` |  |
| `ledger-api.tls.client-cert.cert-chain-file.type` | one of `pem-file`, `pem-string` | `ledger-api.tls.client-cert.cert-chain-file` is configured | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). |
| `ledger-api.tls.client-cert.private-key-file` | string (PemFile) | `ledger-api.tls.client-cert` is configured |  |
| `ledger-api.tls.trust-collection-file.pem-bytes` | string | `ledger-api.tls.trust-collection-file` is configured and `ledger-api.tls.trust-collection-file.type` is `pem-string` |  |
| `ledger-api.tls.trust-collection-file.pem-file` | path (ExistingFile) | `ledger-api.tls.trust-collection-file` is configured and `ledger-api.tls.trust-collection-file.type` is `pem-file` |  |
| `ledger-api.tls.trust-collection-file.type` | one of `pem-file`, `pem-string` | `ledger-api.tls.trust-collection-file` is configured | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). |
| `ledger-json-api.port` | int (Port) | `ledger-json-api` is configured |  |


</Tab>
</Tabs>

## All options

### Top-level keys

**Path** `canton.remote-participants.<remote-participant>`

Takes a `type`: [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring) at `admin-api.tls.trust-collection-file`.

<Tabs>
<Tab title="HOCON">

```hocon
canton.remote-participants.<remote-participant> {
  # token = <string>
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `token` | string | _unset_ | optional bearer token to use on the ledger-api if jwt authorization is enabled |

</Tab>
</Tabs>

### `admin-api`

**Path** `canton.remote-participants.<remote-participant>.admin-api`

Takes a `type`: [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring) at `tls.trust-collection-file`.

<Tabs>
<Tab title="HOCON">

```hocon
canton.remote-participants.<remote-participant>.admin-api {
  address = "127.0.0.1"
  channel {
    # flow-control-window = <int>
    # initial-flow-control-window = <int>
    keep-alive-client {
      idle-timeout = "30 minutes"
      keep-alive-without-calls = false
      time = "40 seconds"
      timeout = "15 seconds"
    }
    max-inbound-message-size = 0   # required
    trace-context-propagation = enabled|disabled   # required
  }
  port = 0   # required
  tls {
    client-cert {
      cert-chain-file {
        # pem-bytes = "..."   # required when type = pem-string
        # pem-file = "/path/to/file"   # required when type = pem-file
        type = pem-file|pem-string   # required
      }
      private-key-file = "..."   # required
    }
    enabled = true
    trust-collection-file {
      # pem-bytes = "..."   # required when type = pem-string
      # pem-file = "/path/to/file"   # required when type = pem-file
      type = pem-file|pem-string   # required
    }
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `address` | string | `127.0.0.1` |  |
| `port` | int (Port) | **required** |  |
| **channel** |  |  |  |
|     `flow-control-window` | int (PositiveInt) | _unset_ | Switches to manual gRPC flow control and sets its window; if `None`, then it is not configured and the implementation default is used. At most one of `flowControlWindow` and `initialFlowControlWindow` can be set. |
|     `initial-flow-control-window` | int (PositiveInt) | _unset_ | Switches to automatic gRPC flow control and sets its initial window; if `None`, then it is not configured and the implementation default is used. At most one of `flowControlWindow` and `initialFlowControlWindow` can be set. |
|     `max-inbound-message-size` | int (NonNegativeInt) | **required** |  |
|     `trace-context-propagation` | one of `enabled`, `disabled` | **required** |  |
|     **keep-alive-client** |  |  |  |
|         `idle-timeout` | duration (NonNegativeFiniteDuration) | `30 minutes` | If there are no RPCs during this duration, the channel will close all connections and switch to Idle state (default is 30 min) |
|         `keep-alive-without-calls` | boolean | `false` | Enables sending of keep alive even when there are no RPCs on the channel (default to false). This increases network resource consumption for idle connections and requires the server to enable permitKeepAliveWithoutCalls. Prefer using idleTimeout. |
|         `time` | duration (NonNegativeFiniteDuration) | `40 seconds` | Sets the time without read activity before sending a keepalive ping. Do not set to small numbers (default is 40s) |
|         `timeout` | duration (NonNegativeFiniteDuration) | `15 seconds` | Sets the time waiting for read activity after sending a keepalive ping (default is 15s). The default timeout was previously 20s and was lowered to 15s, because besides configuring the gRPC KeepAliveManager, it also sets up the socket's TCP_USER_TIMEOUT (see html). 15s gives a larger margin to detect a faulty connection earlier and retry a submission on another sequencer via amplification, thereby avoiding a request failure. |
| **tls** |  |  |  |
|     `enabled` | boolean | `true` | allows enabling TLS without `trustCollectionFile` or `clientCert` |
|     **client-cert** |  |  |  |
|         `private-key-file` | string (PemFile) | **required** |  |
|         **cert-chain-file** |  |  |  |
|             `type` | one of `pem-file`, `pem-string` | **required** | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
|         **cert-chain-file** `type = pem-file` |  |  |  |
|             `pem-file` | path (ExistingFile) | **required** |  |
|         **cert-chain-file** `type = pem-string` |  |  |  |
|             `pem-bytes` | string | **required** |  |
|     **trust-collection-file** |  |  |  |
|         `type` | one of `pem-file`, `pem-string` | **required** | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
|     **trust-collection-file** `type = pem-file` |  |  |  |
|         `pem-file` | path (ExistingFile) | **required** |  |
|     **trust-collection-file** `type = pem-string` |  |  |  |
|         `pem-bytes` | string | **required** |  |

</Tab>
</Tabs>

### `http-health`

**Path** `canton.remote-participants.<remote-participant>.http-health`

<Tabs>
<Tab title="HOCON">

```hocon
canton.remote-participants.<remote-participant>.http-health {
  address = "0.0.0.0"
  port = 0   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `address` | string | `0.0.0.0` |  |
| `port` | int (Port) | **required** |  |

</Tab>
</Tabs>

### `ledger-api`

**Path** `canton.remote-participants.<remote-participant>.ledger-api`

Takes a `type`: [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring) at `tls.trust-collection-file`.

<Tabs>
<Tab title="HOCON">

```hocon
canton.remote-participants.<remote-participant>.ledger-api {
  address = "127.0.0.1"
  channel {
    # flow-control-window = <int>
    # initial-flow-control-window = <int>
    keep-alive-client {
      idle-timeout = "30 minutes"
      keep-alive-without-calls = false
      time = "40 seconds"
      timeout = "15 seconds"
    }
    max-inbound-message-size = 0   # required
    trace-context-propagation = enabled|disabled   # required
  }
  port = 0   # required
  tls {
    client-cert {
      cert-chain-file {
        # pem-bytes = "..."   # required when type = pem-string
        # pem-file = "/path/to/file"   # required when type = pem-file
        type = pem-file|pem-string   # required
      }
      private-key-file = "..."   # required
    }
    enabled = true
    trust-collection-file {
      # pem-bytes = "..."   # required when type = pem-string
      # pem-file = "/path/to/file"   # required when type = pem-file
      type = pem-file|pem-string   # required
    }
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `address` | string | `127.0.0.1` |  |
| `port` | int (Port) | **required** |  |
| **channel** |  |  |  |
|     `flow-control-window` | int (PositiveInt) | _unset_ | Switches to manual gRPC flow control and sets its window; if `None`, then it is not configured and the implementation default is used. At most one of `flowControlWindow` and `initialFlowControlWindow` can be set. |
|     `initial-flow-control-window` | int (PositiveInt) | _unset_ | Switches to automatic gRPC flow control and sets its initial window; if `None`, then it is not configured and the implementation default is used. At most one of `flowControlWindow` and `initialFlowControlWindow` can be set. |
|     `max-inbound-message-size` | int (NonNegativeInt) | **required** |  |
|     `trace-context-propagation` | one of `enabled`, `disabled` | **required** |  |
|     **keep-alive-client** |  |  |  |
|         `idle-timeout` | duration (NonNegativeFiniteDuration) | `30 minutes` | If there are no RPCs during this duration, the channel will close all connections and switch to Idle state (default is 30 min) |
|         `keep-alive-without-calls` | boolean | `false` | Enables sending of keep alive even when there are no RPCs on the channel (default to false). This increases network resource consumption for idle connections and requires the server to enable permitKeepAliveWithoutCalls. Prefer using idleTimeout. |
|         `time` | duration (NonNegativeFiniteDuration) | `40 seconds` | Sets the time without read activity before sending a keepalive ping. Do not set to small numbers (default is 40s) |
|         `timeout` | duration (NonNegativeFiniteDuration) | `15 seconds` | Sets the time waiting for read activity after sending a keepalive ping (default is 15s). The default timeout was previously 20s and was lowered to 15s, because besides configuring the gRPC KeepAliveManager, it also sets up the socket's TCP_USER_TIMEOUT (see html). 15s gives a larger margin to detect a faulty connection earlier and retry a submission on another sequencer via amplification, thereby avoiding a request failure. |
| **tls** |  |  |  |
|     `enabled` | boolean | `true` | allows enabling TLS without `trustCollectionFile` or `clientCert` |
|     **client-cert** |  |  |  |
|         `private-key-file` | string (PemFile) | **required** |  |
|         **cert-chain-file** |  |  |  |
|             `type` | one of `pem-file`, `pem-string` | **required** | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
|         **cert-chain-file** `type = pem-file` |  |  |  |
|             `pem-file` | path (ExistingFile) | **required** |  |
|         **cert-chain-file** `type = pem-string` |  |  |  |
|             `pem-bytes` | string | **required** |  |
|     **trust-collection-file** |  |  |  |
|         `type` | one of `pem-file`, `pem-string` | **required** | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
|     **trust-collection-file** `type = pem-file` |  |  |  |
|         `pem-file` | path (ExistingFile) | **required** |  |
|     **trust-collection-file** `type = pem-string` |  |  |  |
|         `pem-bytes` | string | **required** |  |

</Tab>
</Tabs>

### `ledger-json-api`

**Path** `canton.remote-participants.<remote-participant>.ledger-json-api`

<Tabs>
<Tab title="HOCON">

```hocon
canton.remote-participants.<remote-participant>.ledger-json-api {
  address = "127.0.0.1"
  # path-prefix = <string>
  port = 0   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `address` | string | `127.0.0.1` |  |
| `path-prefix` | string | _unset_ |  |
| `port` | int (Port) | **required** |  |

</Tab>
</Tabs>
