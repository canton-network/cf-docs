```yaml
# Example values excerpt (validator)
migration:
  id: <MIGRATION_ID>
sponsor:
  svUrl: "<SPONSOR_SV_URL>"
onboarding:
  secretFrom:
    secretName: splice-app-validator-onboarding-validator
    key: secret
participant:
  storage:
    type: postgres
    config:
      url: "jdbc:postgresql://<DB_HOST>:5432/participant"
      user: "canton"
      password:
        secretName: postgres-secrets
        key: postgresPassword
```
