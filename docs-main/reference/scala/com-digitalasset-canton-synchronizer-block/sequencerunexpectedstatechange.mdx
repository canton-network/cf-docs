---
title: "SequencerUnexpectedStateChange"
description: "Thrown if the ephemeral state does not match what is expected in the persisted store. This is not expected to be able to occur, but if it does likely means that the ephemeral state is inconsistent with the persisted state. The sequencer should be restarted and logs verified to ensure that the persisted state is correct."
---

## SequencerUnexpectedStateChange - stable

Upstream docs: [Open](<https://docs.digitalasset.com/operate/3.4/scaladoc/com/digitalasset/canton/synchronizer/block/SequencerUnexpectedStateChange.html>)

**Signature**

```text
class SequencerUnexpectedStateChange extends RuntimeException
```

**Members**

No members found for this object in the configured artifacts.
