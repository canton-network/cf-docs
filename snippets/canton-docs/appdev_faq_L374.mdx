```scala
// Check party authorization
   participant.parties.list()
   
   // Check package vetting
   participant.packages.list()
   
   // Check ACS for contract existence
   participant.ledger_api.state.acs.of_party(aliceParty)
```
