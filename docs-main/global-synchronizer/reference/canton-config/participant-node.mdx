---
title: "Participant node"
description: "Ledger API, admin API, storage, crypto and parameters of a participant."
---

{/* GENERATED_FROM source="digital-asset/canton" ref="9543a9170cbdaea12c1909dd1d96e8975b60bbdb" version="3.7.0-SNAPSHOT" path="docs-open/target/config-reference.json" */}

Configuration under `canton.participants.<participant>` for Canton 3.7.0-SNAPSHOT: 791 keys. Part of the [Canton configuration reference](/global-synchronizer/reference/canton-config/overview).

<Accordion title="How to read this page">

Canton is configured with [HOCON](https://github.com/lightbend/config/blob/master/HOCON.md). Scala field names appear as lowercase-with-dashes, so `maxInboundMessageSize` is written `max-inbound-message-size`. **The key set is closed**: an unrecognised key fails startup rather than being ignored, so a typo is caught immediately.

Paths are rooted at `canton`. Segments in angle brackets are names you choose:

- `<annotation>` — `user-chosen name of each entry under canton.participants.<participant>.alpha-dynamic.users[].annotations`
- `<connection>` — `user-chosen name of each entry under canton.participants.<participant>.alpha-dynamic.connections[].connections`
- `<custom-tag>` — `user-chosen name of each entry under canton.participants.<participant>.crypto.kms.custom-tags`
- `<extension>` — `user-chosen name of each entry under canton.participants.<participant>.parameters.engine.extensions`
- `<key>` — `user-chosen name of each entry under canton.participants.<participant>.admin-api.limits.active`
- `<participant>` — `user-chosen name of each entry under canton.participants`
- `<request-header>` — `user-chosen name of each entry under canton.participants.<participant>.alpha-dynamic.dars[].request-headers`

A `[]` suffix marks the fields of a list element. Keys marked **alpha** or **beta** are not covered by compatibility guarantees.

**Required** lists only what you must set, and when: always, when you configure a particular section, or when you have chosen a particular `type`. **All options** covers every key, one section per heading, where the heading is the section's absolute path. Each is shown two ways behind tabs: **HOCON** as you would write it from the root, every key with its default, and **Table**, the same keys relative to the heading with their descriptions. Where a section takes a `type`, the HOCON tab enumerates one complete block per type after the section block, and the table groups that type's keys under a `type = …` header; in the table a **bold** row names a section and the keys indented beneath it live inside it. In the Required skeleton a key needed only under a particular `type` is a comment carrying that condition.

Some sections take a `type` that decides which other keys they accept. Those are documented once on the [types page](/global-synchronizer/reference/canton-config/types) and linked from wherever they occur.

In the Default column:

- a value is what the key holds when you do not set it;
- **required** means startup fails unless you set it;
- _unset_ means the feature is off, or the surrounding section is absent, until you set it;
- † marks a value assigned while the configuration loads (a port, for example). The number shown is an example from the environment the defaults were captured in, not a guaranteed default;
- ‡ marks a default written as an expression in the source that has not been evaluated; the expression is shown so you can find it;
- ◊ marks a field that declares one default but was seen holding another in a running configuration, because the section that builds it passes an explicit value. Both are shown.

</Accordion>

## Required

<Tabs>
<Tab title="HOCON">

```hocon
canton.participants.<participant> {
  admin-api {
    auth-services = [
      {
        # certificate = "..."   # required when type = jwt-es-256-crt|jwt-es-512-crt|jwt-rs-256-crt
        # secret = "..."   # required when type = unsafe-jwt-hmac-256
        type = wildcard|unsafe-jwt-hmac-256|jwt-rs-256-crt|jwt-es-256-crt|jwt-es-512-crt|jwt-jwks|party-jwt   # required
        # url = "..."   # required when type = jwt-jwks
        users = [
          {
            # allowed-services = [ ]   # required when admin-api.auth-services[].type = jwt-es-256-crt|jwt-es-512-crt|jwt-jwks|jwt-rs-256-crt|unsafe-jwt-hmac-256
            # user-id = "..."   # required when admin-api.auth-services[].type = jwt-es-256-crt|jwt-es-512-crt|jwt-jwks|jwt-rs-256-crt|unsafe-jwt-hmac-256
          }
        ]
      }
    ]
    tls {
      cert-chain-file {
        # pem-bytes = "..."   # required when type = pem-string
        # pem-file = "/path/to/file"   # required when type = pem-file
        type = pem-file|pem-string   # required
      }
      client-auth {
        admin-client {
          cert-chain-file {
            # pem-bytes = "..."   # required when type = pem-string and admin-api.tls.client-auth.type = require
            # pem-file = "/path/to/file"   # required when type = pem-file and admin-api.tls.client-auth.type = require
            # type = pem-file|pem-string   # required when admin-api.tls.client-auth.type = require
          }
          # private-key-file = "..."   # required when admin-api.tls.client-auth.type = require
        }
        type = require|optional|none   # required
      }
      private-key-file = "..."   # required
      trust-collection-file {
        # pem-bytes = "..."   # required when type = pem-string
        # pem-file = "/path/to/file"   # required when type = pem-file
        type = pem-file|pem-string   # required
      }
    }
  }
  alpha-dynamic {
    connections = [
      {
        connections {
          <connection> {
            endpoints = [
              {
                host = "..."   # required
                port = 0   # required
              }
            ]
          }
        }
        synchronizer-alias = "..."   # required
      }
    ]
    dars = [
      {
        location = "..."   # required
      }
    ]
    idps = [
      {
        identity-provider-id = "..."   # required
        issuer = "..."   # required
        jwks-url = "..."   # required
      }
    ]
    parties = [
      {
        party = "..."   # required
      }
    ]
    users = [
      {
        user = "..."   # required
      }
    ]
  }
  crypto {
    kms {
      # config = { }   # required when type = driver
      # key-ring-id = "..."   # required when type = gcp
      # location-id = "..."   # required when type = gcp
      # name = "..."   # required when type = driver
      # project-id = "..."   # required when type = gcp
      # region = "..."   # required when type = aws
      retries {
        create-key-check {
          initial-delay = "30 seconds"   # required
          max-delay = "30 seconds"   # required
          max-retries = 0   # required
        }
        failures {
          initial-delay = "30 seconds"   # required
          max-delay = "30 seconds"   # required
          max-retries = 0   # required
        }
      }
      type = driver|aws|gcp   # required
    }
    private-key-store {
      encryption {
        type = kms   # required
      }
    }
    session-signing-keys {
      enabled = false   # required
    }
  }
  init {
    identity {
      identifier {
        # name = "..."   # required when type = explicit and init.identity.type = auto
        # type = random|explicit|config   # required when init.identity.type = auto
      }
      type = external|auto|manual   # required
    }
  }
  ledger-api {
    auth-services = [
      {
        # certificate = "..."   # required when type = jwt-es-256-crt|jwt-es-512-crt|jwt-rs-256-crt
        # secret = "..."   # required when type = unsafe-jwt-hmac-256
        type = wildcard|unsafe-jwt-hmac-256|jwt-rs-256-crt|jwt-es-256-crt|jwt-es-512-crt|jwt-jwks|party-jwt   # required
        # url = "..."   # required when type = jwt-jwks
        users = [
          {
            # allowed-services = [ ]   # required when ledger-api.auth-services[].type = jwt-es-256-crt|jwt-es-512-crt|jwt-jwks|jwt-rs-256-crt|unsafe-jwt-hmac-256
            # user-id = "..."   # required when ledger-api.auth-services[].type = jwt-es-256-crt|jwt-es-512-crt|jwt-jwks|jwt-rs-256-crt|unsafe-jwt-hmac-256
          }
        ]
      }
    ]
    postgres-data-source {
      synchronous-commit {
        type = on|off|remote-write|remote-apply|local   # required
      }
    }
    tls {
      cert-chain-file {
        # pem-bytes = "..."   # required when type = pem-string
        # pem-file = "/path/to/file"   # required when type = pem-file
        type = pem-file|pem-string   # required
      }
      client-auth {
        admin-client {
          cert-chain-file {
            # pem-bytes = "..."   # required when type = pem-string and ledger-api.tls.client-auth.type = require
            # pem-file = "/path/to/file"   # required when type = pem-file and ledger-api.tls.client-auth.type = require
            # type = pem-file|pem-string   # required when ledger-api.tls.client-auth.type = require
          }
          # private-key-file = "..."   # required when ledger-api.tls.client-auth.type = require
        }
        type = require|optional|none   # required
      }
      private-key-file = "..."   # required
      trust-collection-file {
        # pem-bytes = "..."   # required when type = pem-string
        # pem-file = "/path/to/file"   # required when type = pem-file
        type = pem-file|pem-string   # required
      }
    }
  }
  monitoring {
    http-health-server {
      port = 0   # required
    }
  }
  parameters {
    acs-commitments {
      load-batching {
        type = batching|no-batching   # required
      }
    }
    batching {
      aggregator {
        type = batching|no-batching   # required
      }
      contract-store-aggregator {
        type = batching|no-batching   # required
      }
      mediator-fetch-finalized-responses-aggregator {
        type = batching|no-batching   # required
      }
      mediator-store-finalized-responses-aggregator {
        type = batching|no-batching   # required
      }
      topology-cache-aggregator {
        type = batching|no-batching   # required
      }
    }
    caching {
      bft-ordering-batch-cache {
        maximum-memory = "..."   # required
      }
      contract-store {
        maximum-size = 0   # required
      }
      finalized-mediator-confirmation-requests {
        maximum-size = 0   # required
      }
      indexed-strings {
        maximum-size = 0   # required
      }
      key-cache {
        maximum-size = 0   # required
      }
      kms-metadata-cache {
        maximum-size = 0   # required
      }
      member-cache {
        maximum-size = 0   # required
      }
      package-upgrade-cache {
        maximum-size = 0   # required
      }
      package-vetting-cache {
        maximum-size = 0   # required
      }
      participant-cache {
        maximum-size = 0   # required
      }
      party-cache {
        maximum-size = 0   # required
      }
      public-key-conversion-cache {
        maximum-size = 0   # required
      }
      sequencer-catchup-payload-cache {
        maximum-memory = "..."   # required
      }
      sequencer-payload-cache {
        maximum-memory = "..."   # required
      }
      session-encryption-key-cache {
        receiver-cache {
          maximum-size = 0   # required
        }
        sender-cache {
          maximum-size = 0   # required
        }
      }
      synchronizer-client-max-timestamp {
        maximum-size = 0   # required
      }
      topology-snapshot {
        maximum-size = 0   # required
      }
    }
    engine {
      extensions {
        <extension> {
          address = "..."   # required
          auth {
            # token-file = "/path/to/file"   # required when type = bearer-token-file
            type = none|bearer-token-file   # required
          }
          port = 0   # required
          tls {
            trust-collection-file {
              # pem-bytes = "..."   # required when type = pem-string
              # pem-file = "/path/to/file"   # required when type = pem-file
              type = pem-file|pem-string   # required
            }
          }
        }
      }
      transaction-limits {
        acting-parties = 0   # required
        choice-authorizers = 0   # required
        choice-observers = 0   # required
        contract-signatories = 0   # required
        contract-stakeholders = 0   # required
        external-call-result-size = 0   # required
        external-call-results = 0   # required
        key-maintainers = 0   # required
        node-children = 0   # required
        query-result = 0   # required
        total-informees = 0   # required
        transaction-input-contracts = 0   # required
        transaction-nodes = 0   # required
        transaction-roots = 0   # required
        value-size = 0   # required
      }
    }
    ledger-api-server {
      contract-id-seeding {
        type = strong|weak|static   # required
      }
      indexer {
        achs-config {
          last-populated-distance-target = 0   # required
          valid-at-distance-target = 0   # required
        }
        postgres-data-source {
          synchronous-commit {
            type = on|off|remote-write|remote-apply|local   # required
          }
        }
      }
    }
    lsu {
      handshake {
        retry {
          initial-delay = "30 seconds"   # required
          max-delay = "30 seconds"   # required
          max-retries = 0   # required
        }
      }
      lsu-retry {
        initial-delay = "30 seconds"   # required
        max-delay = "30 seconds"   # required
        max-retries = 0   # required
      }
      sequencer-ids-retrieval-retry {
        initial-delay = "30 seconds"   # required
        max-delay = "30 seconds"   # required
        max-retries = 0   # required
      }
    }
    stores {
      safe-to-prune-commitment-state {
        type = match|match-or-mismatch|all   # required
      }
    }
    watchdog {
      enabled = false   # required
    }
  }
  sequencer-client {
    auth-token {
      backoff {
        jitter {
          type = equal|full   # required
        }
      }
    }
  }
  storage {
    type = memory|h2|postgres   # required
  }
  testing-time {
    type = monotonic-time   # required
  }
  traffic-accounting {
    traffic-enforcement-server {
      type = internal   # required
    }
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required when | Description |
|---|---|---|---|
| `admin-api.auth-services[].certificate` | string | `admin-api.auth-services[]` is configured and `admin-api.auth-services[].type` is one of `jwt-es-256-crt`, `jwt-es-512-crt`, `jwt-rs-256-crt` |  |
| `admin-api.auth-services[].secret` | string (NonEmptyString) | `admin-api.auth-services[]` is configured and `admin-api.auth-services[].type` is `unsafe-jwt-hmac-256` |  |
| `admin-api.auth-services[].type` | one of `wildcard`, `unsafe-jwt-hmac-256`, `jwt-rs-256-crt`, `jwt-es-256-crt`, `jwt-es-512-crt`, `jwt-jwks`, `party-jwt` | `admin-api.auth-services[]` is configured | Choose one type — see [AuthServiceConfig](/global-synchronizer/reference/canton-config/types#authserviceconfig). |
| `admin-api.auth-services[].url` | string (NonEmptyString) | `admin-api.auth-services[]` is configured and `admin-api.auth-services[].type` is `jwt-jwks` |  |
| `admin-api.auth-services[].users[].allowed-services` | array of string | `admin-api.auth-services[].users[]` is configured and `admin-api.auth-services[].type` is one of `jwt-es-256-crt`, `jwt-es-512-crt`, `jwt-jwks`, `jwt-rs-256-crt`, `unsafe-jwt-hmac-256` |  |
| `admin-api.auth-services[].users[].user-id` | string | `admin-api.auth-services[].users[]` is configured and `admin-api.auth-services[].type` is one of `jwt-es-256-crt`, `jwt-es-512-crt`, `jwt-jwks`, `jwt-rs-256-crt`, `unsafe-jwt-hmac-256` |  |
| `admin-api.tls.cert-chain-file.pem-bytes` | string | `admin-api.tls.cert-chain-file` is configured and `admin-api.tls.cert-chain-file.type` is `pem-string` |  |
| `admin-api.tls.cert-chain-file.pem-file` | path (ExistingFile) | `admin-api.tls.cert-chain-file` is configured and `admin-api.tls.cert-chain-file.type` is `pem-file` |  |
| `admin-api.tls.cert-chain-file.type` | one of `pem-file`, `pem-string` | `admin-api.tls.cert-chain-file` is configured | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). |
| `admin-api.tls.client-auth.admin-client.cert-chain-file.pem-bytes` | string | `admin-api.tls.client-auth.admin-client.cert-chain-file` is configured and `admin-api.tls.client-auth.admin-client.cert-chain-file.type` is `pem-string` and `admin-api.tls.client-auth.type` is `require` |  |
| `admin-api.tls.client-auth.admin-client.cert-chain-file.pem-file` | path (ExistingFile) | `admin-api.tls.client-auth.admin-client.cert-chain-file` is configured and `admin-api.tls.client-auth.admin-client.cert-chain-file.type` is `pem-file` and `admin-api.tls.client-auth.type` is `require` |  |
| `admin-api.tls.client-auth.admin-client.cert-chain-file.type` | one of `pem-file`, `pem-string` | `admin-api.tls.client-auth.admin-client.cert-chain-file` is configured and `admin-api.tls.client-auth.type` is `require` | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). |
| `admin-api.tls.client-auth.admin-client.private-key-file` | string (PemFile) | `admin-api.tls.client-auth.admin-client` is configured and `admin-api.tls.client-auth.type` is `require` |  |
| `admin-api.tls.client-auth.type` | one of `require`, `optional`, `none` | `admin-api.tls.client-auth` is configured | Choose one type — see [ServerAuthRequirementConfig](/global-synchronizer/reference/canton-config/types#serverauthrequirementconfig). |
| `admin-api.tls.private-key-file` | string (PemFile) | `admin-api.tls` is configured | a file containing the server's private key. The key must not use a password. |
| `admin-api.tls.trust-collection-file.pem-bytes` | string | `admin-api.tls.trust-collection-file` is configured and `admin-api.tls.trust-collection-file.type` is `pem-string` |  |
| `admin-api.tls.trust-collection-file.pem-file` | path (ExistingFile) | `admin-api.tls.trust-collection-file` is configured and `admin-api.tls.trust-collection-file.type` is `pem-file` |  |
| `admin-api.tls.trust-collection-file.type` | one of `pem-file`, `pem-string` | `admin-api.tls.trust-collection-file` is configured | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). |
| `alpha-dynamic.connections[].connections.<connection>.endpoints` | array of Endpoint | `alpha-dynamic.connections[].connections.<connection>` is configured | the list of endpoints for the given sequencer. all endpoints must be of the same sequencer (same-id) |
| `alpha-dynamic.connections[].connections.<connection>.endpoints[].host` | string | `alpha-dynamic.connections[].connections.<connection>.endpoints[]` is configured |  |
| `alpha-dynamic.connections[].connections.<connection>.endpoints[].port` | int (Port) | `alpha-dynamic.connections[].connections.<connection>.endpoints[]` is configured |  |
| `alpha-dynamic.connections[].synchronizer-alias` | string | `alpha-dynamic.connections[]` is configured | the alias to refer to this connection |
| `alpha-dynamic.dars[].location` | string | `alpha-dynamic.dars[]` is configured | the path (or URL) to the dar or dar directory |
| `alpha-dynamic.idps[].identity-provider-id` | string | `alpha-dynamic.idps[]` is configured |  |
| `alpha-dynamic.idps[].issuer` | string | `alpha-dynamic.idps[]` is configured |  |
| `alpha-dynamic.idps[].jwks-url` | string | `alpha-dynamic.idps[]` is configured |  |
| `alpha-dynamic.parties[].party` | string | `alpha-dynamic.parties[]` is configured |  |
| `alpha-dynamic.users[].user` | string | `alpha-dynamic.users[]` is configured | the user id |
| `crypto.kms.config` | config-value | `crypto.kms` is configured and `crypto.kms.type` is `driver` | The driver specific raw config section |
| `crypto.kms.key-ring-id` | string | `crypto.kms` is configured and `crypto.kms.type` is `gcp` | defines a key-ring to where keys will be added. This can be created as a multi-region key-ring, which enables multi-region keys |
| `crypto.kms.location-id` | string | `crypto.kms` is configured and `crypto.kms.type` is `gcp` | defines the GCP region to use (e.g. us-east1) |
| `crypto.kms.name` | string | `crypto.kms` is configured and `crypto.kms.type` is `driver` | The name of the external KMS driver |
| `crypto.kms.project-id` | string | `crypto.kms` is configured and `crypto.kms.type` is `gcp` | defines a GCP project to use (e.g. gcp-kms-testing) |
| `crypto.kms.region` | string | `crypto.kms` is configured and `crypto.kms.type` is `aws` | defines the AWS region to be use (e.g. us-east-1) |
| `crypto.kms.retries.create-key-check.initial-delay` | duration (NonNegativeFiniteDuration) | `crypto.kms.retries.create-key-check` is configured | initial delay before the first retry |
| `crypto.kms.retries.create-key-check.max-delay` | duration (NonNegativeDuration) | `crypto.kms.retries.create-key-check` is configured | max delay between retries |
| `crypto.kms.retries.create-key-check.max-retries` | int | `crypto.kms.retries.create-key-check` is configured | max number of retries |
| `crypto.kms.retries.failures.initial-delay` | duration (NonNegativeFiniteDuration) | `crypto.kms.retries.failures` is configured | initial delay before the first retry |
| `crypto.kms.retries.failures.max-delay` | duration (NonNegativeDuration) | `crypto.kms.retries.failures` is configured | max delay between retries |
| `crypto.kms.retries.failures.max-retries` | int | `crypto.kms.retries.failures` is configured | max number of retries |
| `crypto.kms.type` | one of `driver`, `aws`, `gcp` | `crypto.kms` is configured | Choose one type — see [KmsConfig](/global-synchronizer/reference/canton-config/types#kmsconfig). |
| `crypto.private-key-store.encryption.type` | one of `kms` | `crypto.private-key-store.encryption` is configured | Choose one type — see [EncryptedPrivateStoreConfig](/global-synchronizer/reference/canton-config/types#encryptedprivatestoreconfig). |
| `crypto.session-signing-keys.enabled` | boolean | `crypto.session-signing-keys` is configured | Enables the usage of session signing keys in the protocol. |
| `init.identity.identifier` | string | `init.identity` is configured and `init.identity.type` is `external` | the string to use as identifier of the node |
| `init.identity.identifier.name` | string | `init.identity.identifier` is configured and `init.identity.identifier.type` is `explicit` and `init.identity.type` is `auto` | name to use as identifier |
| `init.identity.identifier.type` | one of `random`, `explicit`, `config` | `init.identity.identifier` is configured and `init.identity.type` is `auto` | Choose one type — see [NodeIdentifierConfig](/global-synchronizer/reference/canton-config/types#nodeidentifierconfig). |
| `init.identity.type` | one of `external`, `auto`, `manual` | `init.identity` is configured | Choose one type — see [IdentityConfig](/global-synchronizer/reference/canton-config/types#identityconfig). |
| `ledger-api.auth-services[].certificate` | string | `ledger-api.auth-services[]` is configured and `ledger-api.auth-services[].type` is one of `jwt-es-256-crt`, `jwt-es-512-crt`, `jwt-rs-256-crt` |  |
| `ledger-api.auth-services[].secret` | string (NonEmptyString) | `ledger-api.auth-services[]` is configured and `ledger-api.auth-services[].type` is `unsafe-jwt-hmac-256` |  |
| `ledger-api.auth-services[].type` | one of `wildcard`, `unsafe-jwt-hmac-256`, `jwt-rs-256-crt`, `jwt-es-256-crt`, `jwt-es-512-crt`, `jwt-jwks`, `party-jwt` | `ledger-api.auth-services[]` is configured | Choose one type — see [AuthServiceConfig](/global-synchronizer/reference/canton-config/types#authserviceconfig). |
| `ledger-api.auth-services[].url` | string (NonEmptyString) | `ledger-api.auth-services[]` is configured and `ledger-api.auth-services[].type` is `jwt-jwks` |  |
| `ledger-api.auth-services[].users[].allowed-services` | array of string | `ledger-api.auth-services[].users[]` is configured and `ledger-api.auth-services[].type` is one of `jwt-es-256-crt`, `jwt-es-512-crt`, `jwt-jwks`, `jwt-rs-256-crt`, `unsafe-jwt-hmac-256` |  |
| `ledger-api.auth-services[].users[].user-id` | string | `ledger-api.auth-services[].users[]` is configured and `ledger-api.auth-services[].type` is one of `jwt-es-256-crt`, `jwt-es-512-crt`, `jwt-jwks`, `jwt-rs-256-crt`, `unsafe-jwt-hmac-256` |  |
| `ledger-api.postgres-data-source.synchronous-commit.type` | one of `on`, `off`, `remote-write`, `remote-apply`, `local` | `ledger-api.postgres-data-source.synchronous-commit` is configured |  |
| `ledger-api.tls.cert-chain-file.pem-bytes` | string | `ledger-api.tls.cert-chain-file` is configured and `ledger-api.tls.cert-chain-file.type` is `pem-string` |  |
| `ledger-api.tls.cert-chain-file.pem-file` | path (ExistingFile) | `ledger-api.tls.cert-chain-file` is configured and `ledger-api.tls.cert-chain-file.type` is `pem-file` |  |
| `ledger-api.tls.cert-chain-file.type` | one of `pem-file`, `pem-string` | `ledger-api.tls.cert-chain-file` is configured | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). |
| `ledger-api.tls.client-auth.admin-client.cert-chain-file.pem-bytes` | string | `ledger-api.tls.client-auth.admin-client.cert-chain-file` is configured and `ledger-api.tls.client-auth.admin-client.cert-chain-file.type` is `pem-string` and `ledger-api.tls.client-auth.type` is `require` |  |
| `ledger-api.tls.client-auth.admin-client.cert-chain-file.pem-file` | path (ExistingFile) | `ledger-api.tls.client-auth.admin-client.cert-chain-file` is configured and `ledger-api.tls.client-auth.admin-client.cert-chain-file.type` is `pem-file` and `ledger-api.tls.client-auth.type` is `require` |  |
| `ledger-api.tls.client-auth.admin-client.cert-chain-file.type` | one of `pem-file`, `pem-string` | `ledger-api.tls.client-auth.admin-client.cert-chain-file` is configured and `ledger-api.tls.client-auth.type` is `require` | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). |
| `ledger-api.tls.client-auth.admin-client.private-key-file` | string (PemFile) | `ledger-api.tls.client-auth.admin-client` is configured and `ledger-api.tls.client-auth.type` is `require` |  |
| `ledger-api.tls.client-auth.type` | one of `require`, `optional`, `none` | `ledger-api.tls.client-auth` is configured | Choose one type — see [ServerAuthRequirementConfig](/global-synchronizer/reference/canton-config/types#serverauthrequirementconfig). |
| `ledger-api.tls.private-key-file` | string (PemFile) | `ledger-api.tls` is configured | a file containing the server's private key. The key must not use a password. |
| `ledger-api.tls.trust-collection-file.pem-bytes` | string | `ledger-api.tls.trust-collection-file` is configured and `ledger-api.tls.trust-collection-file.type` is `pem-string` |  |
| `ledger-api.tls.trust-collection-file.pem-file` | path (ExistingFile) | `ledger-api.tls.trust-collection-file` is configured and `ledger-api.tls.trust-collection-file.type` is `pem-file` |  |
| `ledger-api.tls.trust-collection-file.type` | one of `pem-file`, `pem-string` | `ledger-api.tls.trust-collection-file` is configured | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). |
| `monitoring.http-health-server.port` | int (Port) | `monitoring.http-health-server` is configured |  |
| `parameters.acs-commitments.load-batching.type` | one of `batching`, `no-batching` | `parameters.acs-commitments.load-batching` is configured | Choose one type — see [BatchAggregatorConfig](/global-synchronizer/reference/canton-config/types#batchaggregatorconfig). |
| `parameters.batching.aggregator.type` | one of `batching`, `no-batching` | `parameters.batching.aggregator` is configured | Choose one type — see [BatchAggregatorConfig](/global-synchronizer/reference/canton-config/types#batchaggregatorconfig). |
| `parameters.batching.contract-store-aggregator.type` | one of `batching`, `no-batching` | `parameters.batching.contract-store-aggregator` is configured | Choose one type — see [BatchAggregatorConfig](/global-synchronizer/reference/canton-config/types#batchaggregatorconfig). |
| `parameters.batching.mediator-fetch-finalized-responses-aggregator.type` | one of `batching`, `no-batching` | `parameters.batching.mediator-fetch-finalized-responses-aggregator` is configured | Choose one type — see [BatchAggregatorConfig](/global-synchronizer/reference/canton-config/types#batchaggregatorconfig). |
| `parameters.batching.mediator-store-finalized-responses-aggregator.type` | one of `batching`, `no-batching` | `parameters.batching.mediator-store-finalized-responses-aggregator` is configured | Choose one type — see [BatchAggregatorConfig](/global-synchronizer/reference/canton-config/types#batchaggregatorconfig). |
| `parameters.batching.topology-cache-aggregator.type` | one of `batching`, `no-batching` | `parameters.batching.topology-cache-aggregator` is configured | Choose one type — see [BatchAggregatorConfig](/global-synchronizer/reference/canton-config/types#batchaggregatorconfig). |
| `parameters.caching.bft-ordering-batch-cache.maximum-memory` | string (PositiveNumeric) | `parameters.caching.bft-ordering-batch-cache` is configured |  |
| `parameters.caching.contract-store.maximum-size` | int (PositiveNumeric) | `parameters.caching.contract-store` is configured | the maximum size of the cache |
| `parameters.caching.finalized-mediator-confirmation-requests.maximum-size` | int (PositiveNumeric) | `parameters.caching.finalized-mediator-confirmation-requests` is configured | the maximum size of the cache |
| `parameters.caching.indexed-strings.maximum-size` | int (PositiveNumeric) | `parameters.caching.indexed-strings` is configured | the maximum size of the cache |
| `parameters.caching.key-cache.maximum-size` | int (PositiveNumeric) | `parameters.caching.key-cache` is configured | the maximum size of the cache |
| `parameters.caching.kms-metadata-cache.maximum-size` | int (PositiveNumeric) | `parameters.caching.kms-metadata-cache` is configured | the maximum size of the cache |
| `parameters.caching.member-cache.maximum-size` | int (PositiveNumeric) | `parameters.caching.member-cache` is configured | the maximum size of the cache |
| `parameters.caching.package-upgrade-cache.maximum-size` | int (PositiveNumeric) | `parameters.caching.package-upgrade-cache` is configured | the maximum size of the cache |
| `parameters.caching.package-vetting-cache.maximum-size` | int (PositiveNumeric) | `parameters.caching.package-vetting-cache` is configured | the maximum size of the cache |
| `parameters.caching.participant-cache.maximum-size` | int (PositiveNumeric) | `parameters.caching.participant-cache` is configured | the maximum size of the cache |
| `parameters.caching.party-cache.maximum-size` | int (PositiveNumeric) | `parameters.caching.party-cache` is configured | the maximum size of the cache |
| `parameters.caching.public-key-conversion-cache.maximum-size` | int (PositiveNumeric) | `parameters.caching.public-key-conversion-cache` is configured | the maximum size of the cache |
| `parameters.caching.sequencer-catchup-payload-cache.maximum-memory` | string (PositiveNumeric) | `parameters.caching.sequencer-catchup-payload-cache` is configured |  |
| `parameters.caching.sequencer-payload-cache.maximum-memory` | string (PositiveNumeric) | `parameters.caching.sequencer-payload-cache` is configured |  |
| `parameters.caching.session-encryption-key-cache.receiver-cache.maximum-size` | int (PositiveNumeric) | `parameters.caching.session-encryption-key-cache.receiver-cache` is configured | the maximum size of the cache |
| `parameters.caching.session-encryption-key-cache.sender-cache.maximum-size` | int (PositiveNumeric) | `parameters.caching.session-encryption-key-cache.sender-cache` is configured | the maximum size of the cache |
| `parameters.caching.synchronizer-client-max-timestamp.maximum-size` | int (PositiveNumeric) | `parameters.caching.synchronizer-client-max-timestamp` is configured | the maximum size of the cache |
| `parameters.caching.topology-snapshot.maximum-size` | int (PositiveNumeric) | `parameters.caching.topology-snapshot` is configured | the maximum size of the cache |
| `parameters.engine.extensions.<extension>.address` | string | `parameters.engine.extensions.<extension>` is configured | Hostname of the extension service |
| `parameters.engine.extensions.<extension>.auth.token-file` | path (ExistingFile) | `parameters.engine.extensions.<extension>.auth` is configured and `parameters.engine.extensions.<extension>.auth.type` is `bearer-token-file` |  |
| `parameters.engine.extensions.<extension>.auth.type` | one of `none`, `bearer-token-file` | `parameters.engine.extensions.<extension>.auth` is configured | Choose one type — see [ExtensionServiceAuthConfig](/global-synchronizer/reference/canton-config/types#extensionserviceauthconfig). |
| `parameters.engine.extensions.<extension>.port` | int (Port) | `parameters.engine.extensions.<extension>` is configured | Port of the extension service |
| `parameters.engine.extensions.<extension>.tls.trust-collection-file.pem-bytes` | string | `parameters.engine.extensions.<extension>.tls.trust-collection-file` is configured and `parameters.engine.extensions.<extension>.tls.trust-collection-file.type` is `pem-string` |  |
| `parameters.engine.extensions.<extension>.tls.trust-collection-file.pem-file` | path (ExistingFile) | `parameters.engine.extensions.<extension>.tls.trust-collection-file` is configured and `parameters.engine.extensions.<extension>.tls.trust-collection-file.type` is `pem-file` |  |
| `parameters.engine.extensions.<extension>.tls.trust-collection-file.type` | one of `pem-file`, `pem-string` | `parameters.engine.extensions.<extension>.tls.trust-collection-file` is configured | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). |
| `parameters.engine.transaction-limits.acting-parties` | int | `parameters.engine.transaction-limits` is configured |  |
| `parameters.engine.transaction-limits.choice-authorizers` | int | `parameters.engine.transaction-limits` is configured |  |
| `parameters.engine.transaction-limits.choice-observers` | int | `parameters.engine.transaction-limits` is configured |  |
| `parameters.engine.transaction-limits.contract-signatories` | int | `parameters.engine.transaction-limits` is configured |  |
| `parameters.engine.transaction-limits.contract-stakeholders` | int | `parameters.engine.transaction-limits` is configured |  |
| `parameters.engine.transaction-limits.external-call-result-size` | int | `parameters.engine.transaction-limits` is configured |  |
| `parameters.engine.transaction-limits.external-call-results` | int | `parameters.engine.transaction-limits` is configured |  |
| `parameters.engine.transaction-limits.key-maintainers` | int | `parameters.engine.transaction-limits` is configured |  |
| `parameters.engine.transaction-limits.node-children` | int | `parameters.engine.transaction-limits` is configured |  |
| `parameters.engine.transaction-limits.query-result` | int | `parameters.engine.transaction-limits` is configured |  |
| `parameters.engine.transaction-limits.total-informees` | int | `parameters.engine.transaction-limits` is configured |  |
| `parameters.engine.transaction-limits.transaction-input-contracts` | int | `parameters.engine.transaction-limits` is configured |  |
| `parameters.engine.transaction-limits.transaction-nodes` | int | `parameters.engine.transaction-limits` is configured |  |
| `parameters.engine.transaction-limits.transaction-roots` | int | `parameters.engine.transaction-limits` is configured |  |
| `parameters.engine.transaction-limits.value-size` | int | `parameters.engine.transaction-limits` is configured |  |
| `parameters.ledger-api-server.contract-id-seeding.type` | one of `strong`, `weak`, `static` | `parameters.ledger-api-server.contract-id-seeding` is configured | This type is deprecated and no longer used (see `ledgerApiServerParametersConfigReader` in CantonConfig.scala). It is kept in the code solely for backwards compatibility. |
| `parameters.ledger-api-server.indexer.achs-config.last-populated-distance-target` | int (NonNegativeLong) | `parameters.ledger-api-server.indexer.achs-config` is configured | The target distance (in event sequential ids) between the valid at and the last populated offset of the ACHS. |
| `parameters.ledger-api-server.indexer.achs-config.valid-at-distance-target` | int (NonNegativeLong) | `parameters.ledger-api-server.indexer.achs-config` is configured | The target distance (in event sequential ids) between the current ledger end and the valid at of the ACHS. |
| `parameters.ledger-api-server.indexer.postgres-data-source.synchronous-commit.type` | one of `on`, `off`, `remote-write`, `remote-apply`, `local` | `parameters.ledger-api-server.indexer.postgres-data-source.synchronous-commit` is configured |  |
| `parameters.lsu.handshake.retry.initial-delay` | duration (NonNegativeFiniteDuration) | `parameters.lsu.handshake.retry` is configured | initial delay before the first retry |
| `parameters.lsu.handshake.retry.max-delay` | duration (NonNegativeDuration) | `parameters.lsu.handshake.retry` is configured | max delay between retries |
| `parameters.lsu.handshake.retry.max-retries` | int | `parameters.lsu.handshake.retry` is configured | max number of retries |
| `parameters.lsu.lsu-retry.initial-delay` | duration (NonNegativeFiniteDuration) | `parameters.lsu.lsu-retry` is configured | initial delay before the first retry |
| `parameters.lsu.lsu-retry.max-delay` | duration (NonNegativeDuration) | `parameters.lsu.lsu-retry` is configured | max delay between retries |
| `parameters.lsu.lsu-retry.max-retries` | int | `parameters.lsu.lsu-retry` is configured | max number of retries |
| `parameters.lsu.sequencer-ids-retrieval-retry.initial-delay` | duration (NonNegativeFiniteDuration) | `parameters.lsu.sequencer-ids-retrieval-retry` is configured | initial delay before the first retry |
| `parameters.lsu.sequencer-ids-retrieval-retry.max-delay` | duration (NonNegativeDuration) | `parameters.lsu.sequencer-ids-retrieval-retry` is configured | max delay between retries |
| `parameters.lsu.sequencer-ids-retrieval-retry.max-retries` | int | `parameters.lsu.sequencer-ids-retrieval-retry` is configured | max number of retries |
| `parameters.stores.safe-to-prune-commitment-state.type` | one of `match`, `match-or-mismatch`, `all` | `parameters.stores.safe-to-prune-commitment-state` is configured | Defines which counter-participants cannot block pruning because of the states of their commitments. The modes increasingly relax the pruning conditions. These modes override the default, which is that all counter-participants block pruning when there isn't a timestamp where all counter-participants sent matching commitments, and non-matching participants are not in a no-wait configuration. |
| `parameters.watchdog.enabled` | boolean | `parameters.watchdog` is configured |  |
| `sequencer-client.auth-token.backoff.jitter.type` | one of `equal`, `full` | `sequencer-client.auth-token.backoff.jitter` is configured |  |
| `storage.type` | one of `memory`, `h2`, `postgres` | `storage` is configured | Choose one type — see [StorageConfig](/global-synchronizer/reference/canton-config/types#storageconfig). |
| `testing-time.type` | one of `monotonic-time` | `testing-time` is configured | Optional ledger api time service configuration for demo and testing only |
| `traffic-accounting.traffic-enforcement-server.type` | one of `internal` | `traffic-accounting.traffic-enforcement-server` is configured | Choose one type — see [TrafficEnforcementServerConfig](/global-synchronizer/reference/canton-config/types#trafficenforcementserverconfig). |


</Tab>
</Tabs>

## All options

### `canton.participants.<participant>.admin-api`

<Tabs>
<Tab title="HOCON">

```hocon
canton.participants.<participant>.admin-api {
  address = "127.0.0.1"
  admin-token-config {
    act-as-any-party-claim = false
    admin-claim = false
    admin-token-duration = "5 minutes"
    # fixed-admin-token = <string>
  }
  auth-services = [
    {
      type = wildcard|unsafe-jwt-hmac-256|jwt-rs-256-crt|jwt-es-256-crt|jwt-es-512-crt|jwt-jwks|party-jwt   # required
    }
  ]
  # flow-control-window = <int>
  initial-flow-control-window = 1048576
  jwks-cache-config {
    auto-refresh-after = "0 seconds"
    cache-expiration = "5 minutes"
    cache-max-size = 1000
    connection-timeout = "10 seconds"
    read-timeout = "10 seconds"
  }
  jwt-timestamp-leeway {
    # default = <int>
    # expires-at = <int>
    # issued-at = <int>
    # not-before = <int>
  }
  keep-alive-server {
    permit-keep-alive-time = "20 seconds"
    permit-keep-alive-without-calls = false
    time = "40 seconds"
    timeout = "20 seconds"
  }
  limits {
    active {
      <key> = [ ]
    }
    throttle-logging-rate-per-second = 10
    warn-on-undefined-limits = false
  }
  max-concurrent-calls-per-connection = 100000
  max-inbound-message-size = 10485760
  max-token-lifetime = "5 minutes"
  port = 30002
  tls {
    cert-chain-file {
      type = pem-file|pem-string   # required
    }
    # ciphers = [ ... ]
    client-auth {
      type = require|optional|none   # required
    }
    enable-cert-revocation-checking = false
    minimum-server-protocol-version = "TLSv1.2"
    private-key-file = "..."   # required
    trust-collection-file {
      type = pem-file|pem-string   # required
    }
  }
}
```

`auth-services[]` supports these types: `wildcard`, `unsafe-jwt-hmac-256`, `jwt-rs-256-crt`, `jwt-es-256-crt`, `jwt-es-512-crt`, `jwt-jwks`, `party-jwt`. Each block shows one entry; a list may hold entries of different types.

```hocon
canton.participants.<participant>.admin-api.auth-services = [
  {
    type = wildcard
    # takes no further keys
  }
]
```

```hocon
canton.participants.<participant>.admin-api.auth-services = [
  {
    type = unsafe-jwt-hmac-256
    access-level = wildcard
    # max-token-life = <duration>
    privileged = false
    secret = "..."   # required
    # target-audience = <string>
    # target-scope = <string>
    users = [
      {
        allowed-services = [ ]   # required
        user-id = "..."   # required
      }
    ]
  }
]
```

```hocon
canton.participants.<participant>.admin-api.auth-services = [
  {
    type = jwt-rs-256-crt
    access-level = wildcard
    certificate = "..."   # required
    # max-token-life = <duration>
    privileged = false
    # target-audience = <string>
    # target-scope = <string>
    users = [
      {
        allowed-services = [ ]   # required
        user-id = "..."   # required
      }
    ]
  }
]
```

```hocon
canton.participants.<participant>.admin-api.auth-services = [
  {
    type = jwt-es-256-crt
    access-level = wildcard
    certificate = "..."   # required
    # max-token-life = <duration>
    privileged = false
    # target-audience = <string>
    # target-scope = <string>
    users = [
      {
        allowed-services = [ ]   # required
        user-id = "..."   # required
      }
    ]
  }
]
```

```hocon
canton.participants.<participant>.admin-api.auth-services = [
  {
    type = jwt-es-512-crt
    access-level = wildcard
    certificate = "..."   # required
    # max-token-life = <duration>
    privileged = false
    # target-audience = <string>
    # target-scope = <string>
    users = [
      {
        allowed-services = [ ]   # required
        user-id = "..."   # required
      }
    ]
  }
]
```

```hocon
canton.participants.<participant>.admin-api.auth-services = [
  {
    type = jwt-jwks
    access-level = wildcard
    # max-token-life = <duration>
    privileged = false
    # target-audience = <string>
    # target-scope = <string>
    url = "..."   # required
    users = [
      {
        allowed-services = [ ]   # required
        user-id = "..."   # required
      }
    ]
  }
]
```

```hocon
canton.participants.<participant>.admin-api.auth-services = [
  {
    type = party-jwt
    # takes no further keys
  }
]
```

`tls.cert-chain-file` supports these types: `pem-file`, `pem-string`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.admin-api.tls.cert-chain-file {
  type = pem-file
  pem-file = "/path/to/file"   # required
}
```

```hocon
canton.participants.<participant>.admin-api.tls.cert-chain-file {
  type = pem-string
  pem-bytes = "..."   # required
}
```

`tls.client-auth` supports these types: `require`, `optional`, `none`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.admin-api.tls.client-auth {
  type = require
  admin-client {
    cert-chain-file {
      type = pem-file|pem-string   # required
    }
    private-key-file = "..."   # required
  }
}
```

```hocon
canton.participants.<participant>.admin-api.tls.client-auth {
  type = optional
  # takes no further keys
}
```

```hocon
canton.participants.<participant>.admin-api.tls.client-auth {
  type = none
  # takes no further keys
}
```

`tls.trust-collection-file` supports these types: `pem-file`, `pem-string`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.admin-api.tls.trust-collection-file {
  type = pem-file
  pem-file = "/path/to/file"   # required
}
```

```hocon
canton.participants.<participant>.admin-api.tls.trust-collection-file {
  type = pem-string
  pem-bytes = "..."   # required
}
```

`tls.client-auth.admin-client.cert-chain-file` supports these types: `pem-file`, `pem-string`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.admin-api.tls.client-auth.admin-client.cert-chain-file {
  type = pem-file
  pem-file = "/path/to/file"   # required
}
```

```hocon
canton.participants.<participant>.admin-api.tls.client-auth.admin-client.cert-chain-file {
  type = pem-string
  pem-bytes = "..."   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `address` | string | `127.0.0.1` |  |
| `auth-services` | array of AuthServiceConfig | `[]` |  |
| `flow-control-window` | int (PositiveInt) | `ServerConfig.defaultFlowControlWindow` ‡ |  |
| `initial-flow-control-window` | int (PositiveInt) | `1048576` |  |
| `max-concurrent-calls-per-connection` | int (NonNegativeInt) | `100000` |  |
| `max-inbound-message-size` | int (NonNegativeInt) | `10485760` |  |
| `max-token-lifetime` | duration (NonNegativeDuration) | `5 minutes` |  |
| `port` | int (Port) | `30002` † |  |
| **admin-token-config** |  |  |  |
|     `act-as-any-party-claim` | boolean | `false` |  |
|     `admin-claim` | boolean | `false` |  |
|     `admin-token-duration` | duration (PositiveFiniteDuration) | `5 minutes` |  |
|     `fixed-admin-token` | string | _unset_ |  |
| **auth-services[]** |  |  |  |
|     `type` | one of `wildcard`, `unsafe-jwt-hmac-256`, `jwt-rs-256-crt`, `jwt-es-256-crt`, `jwt-es-512-crt`, `jwt-jwks`, `party-jwt` | **required** | Choose one type — see [AuthServiceConfig](/global-synchronizer/reference/canton-config/types#authserviceconfig). |
| **auth-services[]** `type = unsafe-jwt-hmac-256` |  |  |  |
|     `access-level` | one of `admin`, `wildcard` | `wildcard` |  |
|     `max-token-life` | duration (NonNegativeDuration) | `NonNegativeDuration(Duration.Inf)` ‡ |  |
|     `privileged` | boolean | `false` |  |
|     `secret` | string (NonEmptyString) | **required** |  |
|     `target-audience` | string | _unset_ |  |
|     `target-scope` | string | _unset_ |  |
|     `users` | array of AuthorizedUser | `[]` |  |
|     **users[]** |  |  |  |
|         `allowed-services` | array of string | **required** |  |
|         `user-id` | string | **required** |  |
| **auth-services[]** `type = jwt-rs-256-crt` |  |  |  |
|     `access-level` | one of `admin`, `wildcard` | `wildcard` |  |
|     `certificate` | string | **required** |  |
|     `max-token-life` | duration (NonNegativeDuration) | `NonNegativeDuration(Duration.Inf)` ‡ |  |
|     `privileged` | boolean | `false` |  |
|     `target-audience` | string | _unset_ |  |
|     `target-scope` | string | _unset_ |  |
|     `users` | array of AuthorizedUser | `[]` |  |
|     **users[]** |  |  |  |
|         `allowed-services` | array of string | **required** |  |
|         `user-id` | string | **required** |  |
| **auth-services[]** `type = jwt-es-256-crt` |  |  |  |
|     `access-level` | one of `admin`, `wildcard` | `wildcard` |  |
|     `certificate` | string | **required** |  |
|     `max-token-life` | duration (NonNegativeDuration) | `NonNegativeDuration(Duration.Inf)` ‡ |  |
|     `privileged` | boolean | `false` |  |
|     `target-audience` | string | _unset_ |  |
|     `target-scope` | string | _unset_ |  |
|     `users` | array of AuthorizedUser | `[]` |  |
|     **users[]** |  |  |  |
|         `allowed-services` | array of string | **required** |  |
|         `user-id` | string | **required** |  |
| **auth-services[]** `type = jwt-es-512-crt` |  |  |  |
|     `access-level` | one of `admin`, `wildcard` | `wildcard` |  |
|     `certificate` | string | **required** |  |
|     `max-token-life` | duration (NonNegativeDuration) | `NonNegativeDuration(Duration.Inf)` ‡ |  |
|     `privileged` | boolean | `false` |  |
|     `target-audience` | string | _unset_ |  |
|     `target-scope` | string | _unset_ |  |
|     `users` | array of AuthorizedUser | `[]` |  |
|     **users[]** |  |  |  |
|         `allowed-services` | array of string | **required** |  |
|         `user-id` | string | **required** |  |
| **auth-services[]** `type = jwt-jwks` |  |  |  |
|     `access-level` | one of `admin`, `wildcard` | `wildcard` |  |
|     `max-token-life` | duration (NonNegativeDuration) | `NonNegativeDuration(Duration.Inf)` ‡ |  |
|     `privileged` | boolean | `false` |  |
|     `target-audience` | string | _unset_ |  |
|     `target-scope` | string | _unset_ |  |
|     `url` | string (NonEmptyString) | **required** |  |
|     `users` | array of AuthorizedUser | `[]` |  |
|     **users[]** |  |  |  |
|         `allowed-services` | array of string | **required** |  |
|         `user-id` | string | **required** |  |
| **jwks-cache-config** |  |  |  |
|     `auto-refresh-after` | duration (NonNegativeFiniteDuration) | `0 seconds` |  |
|     `cache-expiration` | duration (NonNegativeFiniteDuration) | `5 minutes` |  |
|     `cache-max-size` | int | `1000` |  |
|     `connection-timeout` | duration (NonNegativeFiniteDuration) | `10 seconds` |  |
|     `read-timeout` | duration (NonNegativeFiniteDuration) | `10 seconds` |  |
| **jwt-timestamp-leeway** |  |  |  |
|     `default` | int | _unset_ |  |
|     `expires-at` | int | _unset_ |  |
|     `issued-at` | int | _unset_ |  |
|     `not-before` | int | _unset_ |  |
| **keep-alive-server** |  |  |  |
|     `permit-keep-alive-time` | duration (NonNegativeFiniteDuration) | `20 seconds` |  |
|     `permit-keep-alive-without-calls` | boolean | `false` |  |
|     `time` | duration (NonNegativeFiniteDuration) | `40 seconds` |  |
|     `timeout` | duration (NonNegativeFiniteDuration) | `20 seconds` |  |
| **limits** |  |  |  |
|     `throttle-logging-rate-per-second` | int (NonNegativeInt) | `10` | maximum rate for logging rejections for requests exceeding the limit (prevents DOS on logs) |
|     `warn-on-undefined-limits` | boolean | `false` | emit warning if a limit is not configured for a stream |
|     **active** |  |  |  |
|         `<key>` | int (NonNegativeInt) | `[]` | map of service name to maximum number of parallel active requests or streams |
| **tls** |  |  |  |
|     `ciphers` | array of string | `TlsConfig.defaultCiphers` ‡ | supported ciphers. Set to None (or null in config file) to use default settings defaultCiphers |
|     `enable-cert-revocation-checking` | boolean | `false` | whether to enable certificate revocation checking per https://tersesystems.com/blog/2014/03/22/fixing-certificate-revocation/ |
|     `minimum-server-protocol-version` | string | `TLSv1.2` | minimum supported TLS protocol. Set None (or null in config file) to use default settings defaultMinimumServerProtocol |
|     `private-key-file` | string (PemFile) | **required** | a file containing the server's private key. The key must not use a password. |
|     **cert-chain-file** |  |  |  |
|         `type` | one of `pem-file`, `pem-string` | **required** | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
|     **cert-chain-file** `type = pem-file` |  |  |  |
|         `pem-file` | path (ExistingFile) | **required** |  |
|     **cert-chain-file** `type = pem-string` |  |  |  |
|         `pem-bytes` | string | **required** |  |
|     **client-auth** |  |  |  |
|         `type` | one of `require`, `optional`, `none` | **required** | Choose one type — see [ServerAuthRequirementConfig](/global-synchronizer/reference/canton-config/types#serverauthrequirementconfig). Configuration on whether server requires auth, requests auth, or no auth |
|     **client-auth** `type = require` |  |  |  |
|         **admin-client** |  |  |  |
|             `private-key-file` | string (PemFile) | **required** |  |
|             **cert-chain-file** |  |  |  |
|                 `type` | one of `pem-file`, `pem-string` | **required** | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
|             **cert-chain-file** `type = pem-file` |  |  |  |
|                 `pem-file` | path (ExistingFile) | **required** |  |
|             **cert-chain-file** `type = pem-string` |  |  |  |
|                 `pem-bytes` | string | **required** |  |
|     **trust-collection-file** |  |  |  |
|         `type` | one of `pem-file`, `pem-string` | **required** | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
|     **trust-collection-file** `type = pem-file` |  |  |  |
|         `pem-file` | path (ExistingFile) | **required** |  |
|     **trust-collection-file** `type = pem-string` |  |  |  |
|         `pem-bytes` | string | **required** |  |

</Tab>
</Tabs>

### `canton.participants.<participant>.alpha-dynamic`

<Tabs>
<Tab title="HOCON">

```hocon
canton.participants.<participant>.alpha-dynamic {
  check-self-consistency = true
  connections = [
    {
      connections {
        <connection> {
          # custom-trust-certificates = <path>
          endpoints = [
            {
              host = "..."   # required
              port = 0   # required
            }
          ]
          transport-security = false
        }
      }
      initialize-from-trusted-synchronizer = false
      # liveness-margin = <int>
      manual-connect = false
      priority = 0
      synchronizer-alias = "..."   # required
      # trust-threshold = <int>
    }
  ]
  dars = [
    {
      # expected-main-package = <string>
      location = "..."   # required
      request-headers {
        # <request-header> = <string>
      }
      synchronizers = [ ]
    }
  ]
  enable-multi-synchronizer-topology-feature-flag = false
  fetched-dar-directory = "/tmp/fetched-dars12534636215432177984"
  idps = [
    {
      # audience = <string>
      identity-provider-id = "..."   # required
      is-deactivated = false
      issuer = "..."   # required
      jwks-url = "..."   # required
    }
  ]
  parties = [
    {
      party = "..."   # required
      # permission = <unresolved>
      synchronizers = [ ]
    }
  ]
  remove-connections = false
  remove-idps = false
  remove-parties = false
  remove-users = false
  users = [
    {
      annotations {
        <annotation> = [ ]
      }
      identity-provider-id = ""
      is-deactivated = false
      # primary-party = <string>
      primary-party-authentication = false
      rights {
        act-as = [ ]
        act-as-any-party = false
        execute-as = [ ]
        execute-as-any-party = false
        identity-provider-admin = false
        participant-admin = false
        read-as = [ ]
        read-as-any-party = false
      }
      user = "..."   # required
    }
  ]
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `check-self-consistency` | boolean | `true` | if set to true (default), then after every sync operation we'll check again if it really succeeded |
| `connections` | array of DeclarativeConnectionConfig | `[]` | which connections should be configured |
| `dars` | array of DeclarativeDarConfig | `[]` | which dars should be uploaded |
| `enable-multi-synchronizer-topology-feature-flag` | boolean | `false` | if true, then the participant will enable multi-synchronizers for all synchronizers it is connected to |
| `fetched-dar-directory` | path | `/tmp/fetched-dars12534636215432177984` | temporary directory to store the dars to |
| `idps` | array of DeclarativeIdpConfig | `[]` | which identity providers should be allocated |
| `parties` | array of DeclarativePartyConfig | `[]` | which parties should be allocated |
| `remove-connections` | boolean | `false` | if true, then any excess connection will be disabled |
| `remove-idps` | boolean | `false` | if true, any excess idp found on the node will be deleted |
| `remove-parties` | boolean | `false` | if true, then any "excess party" found on the node will be deleted |
| `remove-users` | boolean | `false` | if true, then any "excess user" found on the node will be deleted |
| `users` | array of DeclarativeUserConfig | `[]` | which users should be allocated |
| **connections[]** |  |  |  |
|     `initialize-from-trusted-synchronizer` | boolean | `false` | if true then the participant assumes that the synchronizer trust certificate of the participant is already issued |
|     `liveness-margin` | int (NonNegativeInt) | `NonNegativeInt.zero` ‡ |  |
|     `manual-connect` | boolean | `false` | if true then the connection should be manual and require explicitly operator action |
|     `priority` | int | `0` | sets the priority of the connection. if a transaction can be sent to several synchronizers, it will use the one with the highest priority |
|     `synchronizer-alias` | string | **required** | the alias to refer to this connection |
|     `trust-threshold` | int (PositiveInt) | `PositiveInt.one` ‡ | from how many sequencers does the node have to receive a notification to trust that it was really observed |
|     **connections** |  |  |  |
|         **&lt;connection&gt;** |  |  |  |
|             `custom-trust-certificates` | path | _unset_ | if the TLS certificate used cannot be validated against the JVMs trust store, then a trust store can be provided |
|             `endpoints` | array of Endpoint | **required** | the list of endpoints for the given sequencer. all endpoints must be of the same sequencer (same-id) |
|             `transport-security` | boolean | `false` | if true then TLS will be used |
|             **endpoints[]** |  |  |  |
|                 `host` | string | **required** |  |
|                 `port` | int (Port) | **required** |  |
| **dars[]** |  |  |  |
|     `expected-main-package` | string | _unset_ | which package id should be expected as the main package |
|     `location` | string | **required** | the path (or URL) to the dar or dar directory |
|     `synchronizers` | array of string | `[]` |  |
|     **request-headers** |  |  |  |
|         `<request-header>` | string | `Map()` ‡ | optionally add additional request headers to download the dar |
| **idps[]** |  |  |  |
|     `audience` | string | _unset_ |  |
|     `identity-provider-id` | string | **required** |  |
|     `is-deactivated` | boolean | `false` |  |
|     `issuer` | string | **required** |  |
|     `jwks-url` | string | **required** |  |
| **parties[]** |  |  |  |
|     `party` | string | **required** |  |
|     `permission` | ParticipantPermissionConfig (unclassified) | `ParticipantPermissionConfig.Submission` ‡ | the permission of the hosting participant |
|     `synchronizers` | array of string | `[]` | if not empty, the party will be added to the selected synchronizers only, refered to by alias |
| **users[]** |  |  |  |
|     `identity-provider-id` | string | `""` | the idp of the given user |
|     `is-deactivated` | boolean | `false` | if true then the user is deactivatedd |
|     `primary-party` | string | _unset_ | the primary party that should be used for the user |
|     `primary-party-authentication` | boolean | `false` | if we allow Party JWT authentication for the user through the primary party |
|     `user` | string | **required** | the user id |
|     **annotations** |  |  |  |
|         `<annotation>` | string | `[]` | a property bag of annotations that can be stored alongside the user |
|     **rights** |  |  |  |
|         `act-as` | array of string | `[]` | the name of the parties the user can act as. parties must exist. if they don't contain a namespace, then the participants namespace will be used |
|         `act-as-any-party` | boolean | `false` |  |
|         `execute-as` | array of string | `[]` |  |
|         `execute-as-any-party` | boolean | `false` |  |
|         `identity-provider-admin` | boolean | `false` | if true, then the user can act as an identity provider admin |
|         `participant-admin` | boolean | `false` | if true then the user can act as a participant admin |
|         `read-as` | array of string | `[]` | the name of the parties the user can read as. |
|         `read-as-any-party` | boolean | `false` | if true then the user can read as any party |

</Tab>
</Tabs>

### `canton.participants.<participant>.crypto`

<Tabs>
<Tab title="HOCON">

```hocon
canton.participants.<participant>.crypto {
  enable-experimental = false
  encryption {
    algorithms {
      # allowed = [ ... ]
      # default = <generic>
    }
    keys {
      # allowed = [ ... ]
      # default = <generic>
    }
  }
  hash {
    # allowed = [ ... ]
    # default = <generic>
  }
  kms {
    retries {
      create-key-check {
        initial-delay = "30 seconds"   # required
        max-delay = "30 seconds"   # required
        max-retries = 0   # required
      }
      failures {
        initial-delay = "30 seconds"   # required
        max-delay = "30 seconds"   # required
        max-retries = 0   # required
      }
    }
    type = driver|aws|gcp   # required
  }
  parallelism {
    encryption-parallelism = 10
    signature-verification-parallelism = 10
  }
  pbkdf {
    # allowed = [ ... ]
    # default = <generic>
  }
  private-key-store {
    encryption {
      reverted = false
      type = kms   # required
      # wrapper-key-id = <string>
    }
  }
  provider = jce
  session-signing-keys {
    cut-off-duration = "1 minute"
    disable-bound-checks = false
    enabled = false   # required
    key-eviction-period = "10 minutes"
    key-validity-duration = "5 minutes"
    signing-algorithm-spec = ed-25519
    signing-key-spec = ec-curve-25519
    tolerance-shift-duration = "2 minutes"
  }
  signing {
    algorithms {
      # allowed = [ ... ]
      # default = <generic>
    }
    keys {
      # allowed = [ ... ]
      # default = <generic>
    }
  }
  symmetric {
    # allowed = [ ... ]
    # default = <generic>
  }
}
```

`kms` supports these types: `driver`, `aws`, `gcp`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.crypto.kms {
  type = driver
  config = { }   # required
  health-check-period = "10 seconds"
  name = "..."   # required
  retries {
    create-key-check {
      initial-delay = "30 seconds"   # required
      max-delay = "30 seconds"   # required
      max-retries = 0   # required
    }
    failures {
      initial-delay = "30 seconds"   # required
      max-delay = "30 seconds"   # required
      max-retries = 0   # required
    }
  }
}
```

```hocon
canton.participants.<participant>.crypto.kms {
  type = aws
  audit-logging = false
  custom-tags {
    <custom-tag> = [ ]
  }
  disable-ssl-verification = false
  # endpoint-override = <string>
  multi-region-key = false
  region = "..."   # required
  retries {
    create-key-check {
      initial-delay = "30 seconds"   # required
      max-delay = "30 seconds"   # required
      max-retries = 0   # required
    }
    failures {
      initial-delay = "30 seconds"   # required
      max-delay = "30 seconds"   # required
      max-retries = 0   # required
    }
  }
}
```

```hocon
canton.participants.<participant>.crypto.kms {
  type = gcp
  audit-logging = false
  # endpoint-override = <string>
  key-ring-id = "..."   # required
  location-id = "..."   # required
  project-id = "..."   # required
  retries {
    create-key-check {
      initial-delay = "30 seconds"   # required
      max-delay = "30 seconds"   # required
      max-retries = 0   # required
    }
    failures {
      initial-delay = "30 seconds"   # required
      max-delay = "30 seconds"   # required
      max-retries = 0   # required
    }
  }
}
```

`private-key-store.encryption` supports these types: `kms`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.crypto.private-key-store.encryption {
  type = kms
  reverted = false
  # wrapper-key-id = <string>
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `enable-experimental` | boolean | `false` | Whether to enable experimental cryptographic schemes. Should not be used in production environments. |
| `provider` | one of `jce`, `kms`, `delegation-crypto-provider` | `jce` | the crypto provider implementation to use |
| **encryption** |  |  |  |
|     **algorithms** |  |  |  |
|         `allowed` | array of S | _unset_ | The optional allowed schemes to use. If none is specified, all the provider's supported schemes of kind S are allowed. |
|         `default` | depends on use (S) | _unset_ | The optional scheme to use. If none is specified, use the provider's default scheme of kind S. |
|     **keys** |  |  |  |
|         `allowed` | array of S | _unset_ | The optional allowed schemes to use. If none is specified, all the provider's supported schemes of kind S are allowed. |
|         `default` | depends on use (S) | _unset_ | The optional scheme to use. If none is specified, use the provider's default scheme of kind S. |
| **hash** |  |  |  |
|     `allowed` | array of S | _unset_ | The optional allowed schemes to use. If none is specified, all the provider's supported schemes of kind S are allowed. |
|     `default` | depends on use (S) | _unset_ | The optional scheme to use. If none is specified, use the provider's default scheme of kind S. |
| **kms** |  |  |  |
|     `type` | one of `driver`, `aws`, `gcp` | **required** | Choose one type — see [KmsConfig](/global-synchronizer/reference/canton-config/types#kmsconfig). |
|     **retries** |  |  |  |
|         **create-key-check** |  |  |  |
|             `initial-delay` | duration (NonNegativeFiniteDuration) | **required** | initial delay before the first retry |
|             `max-delay` | duration (NonNegativeDuration) | **required** | max delay between retries |
|             `max-retries` | int | **required** | max number of retries |
|         **failures** |  |  |  |
|             `initial-delay` | duration (NonNegativeFiniteDuration) | **required** | initial delay before the first retry |
|             `max-delay` | duration (NonNegativeDuration) | **required** | max delay between retries |
|             `max-retries` | int | **required** | max number of retries |
| **kms** `type = driver` |  |  |  |
|     `config` | config-value | **required** | The driver specific raw config section |
|     `health-check-period` | duration (PositiveFiniteDuration) | `10 seconds` | How long to wait between health checks of the KMS driver |
|     `name` | string | **required** | The name of the external KMS driver |
| **kms** `type = aws` |  |  |  |
|     `audit-logging` | boolean | `false` | when enabled, all calls to KMS will be logged. Defaults to false. |
|     `disable-ssl-verification` | boolean | `false` | When set to true, SSL verification is disabled. Mostly for testing purposes. Can only be used if non-standard-config is enabled. |
|     `endpoint-override` | string | _unset_ | the [optional] endpoint for a proxy to be used by the KMS client. |
|     `multi-region-key` | boolean | `false` | flag to enable multiRegion keys (Canton will generate single region keys by default) |
|     `region` | string | **required** | defines the AWS region to be use (e.g. us-east-1) |
|     **custom-tags** |  |  |  |
|         `<custom-tag>` | string | `[]` | custom tags to attach to AWS KMS keys created by Canton. |
| **kms** `type = gcp` |  |  |  |
|     `audit-logging` | boolean | `false` | when enabled, all calls to KMS will be logged. Defaults to false. |
|     `endpoint-override` | string | _unset_ | the [optional] endpoint for a proxy to be used by the KMS client. |
|     `key-ring-id` | string | **required** | defines a key-ring to where keys will be added. This can be created as a multi-region key-ring, which enables multi-region keys |
|     `location-id` | string | **required** | defines the GCP region to use (e.g. us-east1) |
|     `project-id` | string | **required** | defines a GCP project to use (e.g. gcp-kms-testing) |
| **parallelism** |  |  |  |
|     `encryption-parallelism` | int (PositiveInt) | `10` |  |
|     `signature-verification-parallelism` | int (PositiveInt) | `10` | Maximum number of signature verification tasks to run in parallel per signed payload (when multiple signatures are provided). |
| **pbkdf** |  |  |  |
|     `allowed` | array of S | _unset_ | The optional allowed schemes to use. If none is specified, all the provider's supported schemes of kind S are allowed. |
|     `default` | depends on use (S) | _unset_ | The optional scheme to use. If none is specified, use the provider's default scheme of kind S. |
| **private-key-store** |  |  |  |
|     **encryption** |  |  |  |
|         `reverted` | boolean | `false` | when true decrypts the stored private keys and stores them in clear, disabling the encrypted crypto private key store in the process |
|         `type` | one of `kms` | **required** | Choose one type — see [EncryptedPrivateStoreConfig](/global-synchronizer/reference/canton-config/types#encryptedprivatestoreconfig). Encapsulates possible configurations for different encrypted private stores |
|         `wrapper-key-id` | string (KmsKeyId) | _unset_ | defines key identifier for the wrapper key (e.g. ARN for AWS SDK). When it's None Canton will either create a new key or use a previous existent key |
| **session-signing-keys** |  |  |  |
|     `cut-off-duration` | duration (NonNegativeFiniteDuration) | `1 minute` | A cut-off duration is applied only when we need to sign something without knowing the exact topology timestamp that will later be used for verification (e.g., when using a currentSnapshotApproximation). The cutoff measures the amount of clock skew that can be tolerated and therefore should be longer than our `ledgerRecordTimeTolerance`. The participant makes a guess t0 at the topology timestamp for verification and will use an existing session signing key k only if the interval [t0 - cutoff, t0 + cutoff] lies fully within k’s validity period. If the full validity interval is known a priori (e.g., we know the maximum sequencing time for the message we want to sign), we need to ensure that we use an existing session signing key k only if the interval [t0 - cutoff, endValidityPeriod] is fully covered. As a result, newly created session signing keys must have a validity period that starts at least cutoffDuration before ts. A typical example of this uncertainty is signing submission requests, where the sequencer-assigned timestamp is unknown in advance. If a new session key is created only after the previous key’s validity interval ends, multiple submissions may fail verification if any clock skew exists between the nodes. |
|     `disable-bound-checks` | boolean | `false` | Flag to disable parameter bounds, allowing any values. Should only be used for testing and requires `nonStandardConfig` to be enabled. |
|     `enabled` | boolean | `false` † | Enables the usage of session signing keys in the protocol. |
|     `key-eviction-period` | duration (PositiveFiniteDuration) | `10 minutes` | This defines how long the private session signing key remains in memory. This is distinct from the validity period in the sense that we can be asked to sign arbitrarily old timestamps, and so we want to persist the key for longer times so we can re-use it. The eviction period should be longer than keyValidityDuration and at least as long as the majority of confirmation request decision latencies (for the mediator) or confirmation request response latencies (for participants). It should also be longer than a participant's response timeout. For example, a participant may create a submission at time ts0, which is sequenced at ts1. Later, it may need to send a response at ts2 using the original timestamp ts1. Under normal operation, ts2 − ts0 should not exceed the participant response timeout. Therefore, if the eviction period is longer than this timeout, the participant won’t need to create a new session signing key just to send the confirmation response. |
|     `key-validity-duration` | duration (PositiveFiniteDuration) | `5 minutes` | Specifies the validity duration for each session signing key. Its lifespan MUST be configured to be: keyValidityDuration &gt; 2 * cutoffDuration. The validity duration should also not be too short, so that a session signing key can be reused across, for example, multiple submission requests. |
|     `signing-algorithm-spec` | one of `ed-25519`, `ec-dsa-sha-256`, `ec-dsa-sha-384`, `ml-dsa-65` | `ed-25519` | Defines the signing algorithm when using session signing keys. It defaults to Ed25519. |
|     `signing-key-spec` | one of `ec-curve-25519`, `ec-p-256`, `ec-p-384`, `ec-secp-256k-1`, `ml-dsa-65` | `ec-curve-25519` | Defines the key scheme to use for the session signing keys. It defaults to EcCurve25519. Both algorithm and key scheme must be supported and allowed by the node. |
|     `tolerance-shift-duration` | duration (NonNegativeFiniteDuration) | `2 minutes` | This defines the maximum value we will attempt to use when shifting the validity interval from '[ts, ts+keyValidityDuration]' to '[ts-tolerance, ts+keyValidityDuration-tolerance]'. It MUST respect the following constraints: (1) keyValidityDuration - toleranceShiftDuration &gt;= cutoffDuration; (2) toleranceShiftDuration &gt;= cutoffDuration. What we are trying to solve with this shift is the following: Given a sequence of timestamps `tss`, find a set of intervals of fixed length `keyValidityDuration` such that each timestamp in `tss` falls within at least one interval. The session signing key selection algorithm picks intervals as it goes through the list of timestamps from left to right, without any lookahead. As such, the goal is to minimize the number of intervals needed. For example, consider the sequence of timestamps: `ts0`, `ts0-1us`, `ts0-2us`, `ts0-3us`. Without applying this shift, a new interval (i.e., a new session signing key) would be created for each of these slightly different timestamps. Shifting the interval allows us to cover multiple timestamps with a single interval, reducing the number of session keys created. This value can be adjusted: reduced if timestamps are mostly increasing, or increased if timestamps are mostly decreasing. A small note: if we know exactly until when our key must be valid (e.g., the maximum sequencing time), then we do not shift the validity interval centered on ts. Instead, we select a key validity period that takes this end validity time into account. |
| **signing** |  |  |  |
|     **algorithms** |  |  |  |
|         `allowed` | array of S | _unset_ | The optional allowed schemes to use. If none is specified, all the provider's supported schemes of kind S are allowed. |
|         `default` | depends on use (S) | _unset_ | The optional scheme to use. If none is specified, use the provider's default scheme of kind S. |
|     **keys** |  |  |  |
|         `allowed` | array of S | _unset_ | The optional allowed schemes to use. If none is specified, all the provider's supported schemes of kind S are allowed. |
|         `default` | depends on use (S) | _unset_ | The optional scheme to use. If none is specified, use the provider's default scheme of kind S. |
| **symmetric** |  |  |  |
|     `allowed` | array of S | _unset_ | The optional allowed schemes to use. If none is specified, all the provider's supported schemes of kind S are allowed. |
|     `default` | depends on use (S) | _unset_ | The optional scheme to use. If none is specified, use the provider's default scheme of kind S. |

</Tab>
</Tabs>

### `canton.participants.<participant>.http-ledger-api`

<Tabs>
<Tab title="HOCON">

```hocon
canton.participants.<participant>.http-ledger-api {
  address = "127.0.0.1"
  client-request-timeout {
    lower-bound = "1 second"
    upper-bound = "60 seconds"
  }
  daml-definitions-service-enabled = false
  debug-logging-of-http-bodies = false
  enabled = true
  # max-inbound-message-size = <int>
  # path-prefix = <string>
  port = 30003
  # port-file = <unresolved>
  request-timeout = "20 seconds"
  websocket-config {
    # heartbeat-period = <duration>
    # http-list-max-elements-limit = <int>
    # http-list-wait-time = <duration>
    # max-burst = <int>
    # max-duration = <duration>
    # mode = <string>
    # throttle-elem = <int>
    # throttle-per = <duration>
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `address` | string | `127.0.0.1` |  |
| `daml-definitions-service-enabled` | boolean | `false` |  |
| `debug-logging-of-http-bodies` | boolean | `false` |  |
| `enabled` | boolean | `true` |  |
| `max-inbound-message-size` | int (NonNegativeInt) | _unset_ |  |
| `path-prefix` | string | _unset_ |  |
| `port` | int (Port) | `30003` † |  |
| `port-file` | Array (unclassified) | _unset_ |  |
| `request-timeout` | duration | `20 seconds` (in practice `20s`) ◊ |  |
| **client-request-timeout** |  |  |  |
|     `lower-bound` | duration | `1 second` |  |
|     `upper-bound` | duration | `60 seconds` |  |
| **websocket-config** |  |  |  |
|     `heartbeat-period` | duration | `WSC.DefaultHeartbeatPeriod` ‡ |  |
|     `http-list-max-elements-limit` | int | `WSC.DefaultHttpListMaxElementsLimit` ‡ |  |
|     `http-list-wait-time` | duration | `WSC.DefaultHttpListWaitTime` ‡ |  |
|     `max-burst` | int | `WSC.DefaultMaxBurst` ‡ |  |
|     `max-duration` | duration | `WSC.DefaultMaxDuration` ‡ |  |
|     `mode` | string (ThrottleMode) | `WSC.DefaultThrottleMode` ‡ |  |
|     `throttle-elem` | int | `WSC.DefaultThrottleElem` ‡ |  |
|     `throttle-per` | duration | `WSC.DefaultThrottlePer` ‡ |  |

</Tab>
</Tabs>

### `canton.participants.<participant>.init`

<Tabs>
<Tab title="HOCON">

```hocon
canton.participants.<participant>.init {
  generate-intermediate-key = false
  generate-topology-transactions-and-keys = true
  identity {
    type = external|auto|manual   # required
  }
  ledger-api {
    max-deduplication-duration = "7 days"
  }
}
```

`identity` supports these types: `external`, `auto`, `manual`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.init.identity {
  type = external
  certificates = [ ]
  identifier = "..."   # required
  # namespace = <string>
}
```

```hocon
canton.participants.<participant>.init.identity {
  type = auto
  identifier {
    type = random|explicit|config   # required
  }
}
```

```hocon
canton.participants.<participant>.init.identity {
  type = manual
  # takes no further keys
}
```

`identity.identifier` supports these types: `random`, `explicit`, `config`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.init.identity.identifier {
  type = random
  # takes no further keys
}
```

```hocon
canton.participants.<participant>.init.identity.identifier {
  type = explicit
  name = "..."   # required
}
```

```hocon
canton.participants.<participant>.init.identity.identifier {
  type = config
  # takes no further keys
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `generate-intermediate-key` | boolean | `false` |  |
| `generate-topology-transactions-and-keys` | boolean | `true` |  |
| **identity** |  |  |  |
|     `type` | one of `external`, `auto`, `manual` | `auto` † | Choose one type — see [IdentityConfig](/global-synchronizer/reference/canton-config/types#identityconfig). Control how the identity of the node is determined. At startup, we need to determine the node's identity. We support various modes of operations for different deployments. We distinguish between setting the node-id and subsequently generating the topology transactions and the remaining keys. As such it is possible to manually set the node-id but let the system automatically generate the remaining topology transactions. |
| **identity** `type = external` |  |  |  |
|     `certificates` | array of File | `[]` | optional certificates to use (in case we have external root keys) |
|     `identifier` | string | **required** | the string to use as identifier of the node |
|     `namespace` | string | _unset_ | optional fingerprint to use as the namespace (default is to extract it from the first certificate) |
| **identity** `type = auto` |  |  |  |
|     **identifier** |  |  |  |
|         `type` | one of `random`, `explicit`, `config` | `config` † | Choose one type — see [NodeIdentifierConfig](/global-synchronizer/reference/canton-config/types#nodeidentifierconfig). |
|     **identifier** `type = explicit` |  |  |  |
|         `name` | string | **required** | name to use as identifier |
| **ledger-api** |  |  |  |
|     `max-deduplication-duration` | duration (NonNegativeFiniteDuration) | `7 days` | The max deduplication duration reported by the participant's ledger configuration service. The participant's command and command submission services accept all command deduplication durations up to this duration. Acceptance of longer ones is at the discretion of the participant and may vary over time. |

</Tab>
</Tabs>

### `canton.participants.<participant>.ledger-api`

<Tabs>
<Tab title="HOCON">

```hocon
canton.participants.<participant>.ledger-api {
  address = "127.0.0.1"
  admin-token-config {
    act-as-any-party-claim = false
    admin-claim = false
    admin-token-duration = "5 minutes"
    # fixed-admin-token = <string>
  }
  auth-services = [
    {
      type = wildcard|unsafe-jwt-hmac-256|jwt-rs-256-crt|jwt-es-256-crt|jwt-es-512-crt|jwt-jwks|party-jwt   # required
    }
  ]
  command-service {
    contract-prefetching-depth = 3
    default-tracking-timeout = "5 minutes"
    max-commands-in-flight = 256
  }
  database-connection-timeout = "30 seconds"
  enable-command-inspection = true
  # flow-control-window = <int>
  identity-provider-management {
    cache-expiry-after-write = "5 minutes"
  }
  index-service {
    acs-filtering-parallelism = 16
    active-contracts-service-streams {
      contract-processing-parallelism = 8
      id-filter-query-parallelism = 2
      max-ids-per-id-page = 20000
      max-incomplete-page-size = 20
      max-pages-per-id-pages-buffer = 1
      max-parallel-active-id-queries = 4
      max-parallel-payload-create-queries = 2
      max-payloads-per-payloads-page = 1000
      max-working-memory-in-bytes-for-id-pages = 104857600
    }
    # api-query-services-thread-pool-size = <int>
    api-stream-shutdown-timeout = "5s"
    buffered-events-processing-parallelism = 8
    buffered-streams-page-size = 100
    completions-page-size = 1000
    contract-pruning-db-lock-timeout = "20 seconds"
    contract-pruning-delay-before-retry = "2 seconds"
    contract-pruning-max-retries = 10
    global-max-event-id-queries = 20
    global-max-event-payload-queries = 10
    idle-stream-offset-checkpoint-timeout = "1 minute"
    in-memory-state-updater-parallelism = 2
    max-contract-key-state-cache-size = 10000
    max-contract-state-cache-size = 10000
    max-lookup-limit = 1000
    max-rejected-completions-by-hash = 10
    max-transactions-in-memory-fan-out-buffer-size = 1100
    offset-checkpoint-cache-update-interval = "15 seconds"
    prepare-package-metadata-time-out-warning = "5 seconds"
    pruning-db-lock-timeout = "20 seconds"
    updates-streams {
      id-filter-query-parallelism = 2
      max-ids-per-id-page = 20000
      max-pages-per-id-pages-buffer = 1
      max-parallel-id-activate-queries = 4
      max-parallel-id-deactivate-queries = 4
      max-parallel-id-topology-events-queries = 4
      max-parallel-id-various-witnessed-queries = 4
      max-parallel-payload-activate-queries = 2
      max-parallel-payload-deactivate-queries = 2
      max-parallel-payload-queries = 2
      max-parallel-payload-topology-events-queries = 2
      max-parallel-payload-various-witnessed-queries = 2
      max-payloads-per-payloads-page = 1000
      max-working-memory-in-bytes-for-id-pages = 104857600
      transactions-processing-parallelism = 8
    }
  }
  initial-flow-control-window = 1048576
  interactive-submission-service {
    contract-lookup-parallelism = 5
    enable-verbose-hashing = false
    enforce-single-root-node = true
    maximum-number-of-signatures-per-party = 50
  }
  jwks-cache-config {
    auto-refresh-after = "0 seconds"
    cache-expiration = "5 minutes"
    cache-max-size = 1000
    connection-timeout = "10 seconds"
    read-timeout = "10 seconds"
  }
  jwt-timestamp-leeway {
    # default = <int>
    # expires-at = <int>
    # issued-at = <int>
    # not-before = <int>
  }
  keep-alive-server {
    permit-keep-alive-time = "10 seconds"
    permit-keep-alive-without-calls = false
    time = "10 minutes"
    timeout = "20 seconds"
  }
  limits {
    active {
      <key> = [ ]
    }
    throttle-logging-rate-per-second = 10
    warn-on-undefined-limits = false
  }
  management-service-timeout = "2 minutes"
  max-concurrent-calls-per-connection = 100000
  max-inbound-message-size = 10485760
  max-inbound-metadata-size = 8192
  max-token-lifetime = "5 minutes"
  package-service {
    max-vetted-packages-page-size = 100
  }
  party-management-service {
    max-parties-page-size = 10000
    max-self-allocated-parties = 0
  }
  port = 30001
  postgres-data-source {
    client-connection-check-interval = "5 seconds"
    network-timeout = "60 seconds"
    synchronous-commit {
      type = on|off|remote-write|remote-apply|local   # required
    }
    tcp-keepalives-count = 5
    tcp-keepalives-idle = 10
    tcp-keepalives-interval = 1
  }
  rate-limit {
    max-api-services-index-db-queue-size = 1000
    max-api-services-queue-size = 10000
    max-used-heap-space-percentage = 100
    min-free-heap-space-bytes = 0
  }
  state-service {
    default-acs-page-size = 2000
    max-acs-page-size = 10000
  }
  tls {
    cert-chain-file {
      type = pem-file|pem-string   # required
    }
    # ciphers = [ ... ]
    client-auth {
      type = require|optional|none   # required
    }
    enable-cert-revocation-checking = false
    minimum-server-protocol-version = "TLSv1.2"
    private-key-file = "..."   # required
    trust-collection-file {
      type = pem-file|pem-string   # required
    }
  }
  topology-aware-package-selection {
    enabled = true
    max-passes-default = 3
    max-passes-limit = 4
  }
  update-service {
    ascending-first-page-dynamic-bound-overfetch = 4
    default-updates-page-size = 100
    max-updates-page-size = 2000
  }
  user-management-service {
    # additional-admin-user-id = <string>
    cache-expiry-after-write-in-seconds = 5
    enabled = true
    max-cache-size = 100
    max-rights-per-user = 1000
    max-users-page-size = 1000
  }
}
```

`auth-services[]` supports these types: `wildcard`, `unsafe-jwt-hmac-256`, `jwt-rs-256-crt`, `jwt-es-256-crt`, `jwt-es-512-crt`, `jwt-jwks`, `party-jwt`. Each block shows one entry; a list may hold entries of different types.

```hocon
canton.participants.<participant>.ledger-api.auth-services = [
  {
    type = wildcard
    # takes no further keys
  }
]
```

```hocon
canton.participants.<participant>.ledger-api.auth-services = [
  {
    type = unsafe-jwt-hmac-256
    access-level = wildcard
    # max-token-life = <duration>
    privileged = false
    secret = "..."   # required
    # target-audience = <string>
    # target-scope = <string>
    users = [
      {
        allowed-services = [ ]   # required
        user-id = "..."   # required
      }
    ]
  }
]
```

```hocon
canton.participants.<participant>.ledger-api.auth-services = [
  {
    type = jwt-rs-256-crt
    access-level = wildcard
    certificate = "..."   # required
    # max-token-life = <duration>
    privileged = false
    # target-audience = <string>
    # target-scope = <string>
    users = [
      {
        allowed-services = [ ]   # required
        user-id = "..."   # required
      }
    ]
  }
]
```

```hocon
canton.participants.<participant>.ledger-api.auth-services = [
  {
    type = jwt-es-256-crt
    access-level = wildcard
    certificate = "..."   # required
    # max-token-life = <duration>
    privileged = false
    # target-audience = <string>
    # target-scope = <string>
    users = [
      {
        allowed-services = [ ]   # required
        user-id = "..."   # required
      }
    ]
  }
]
```

```hocon
canton.participants.<participant>.ledger-api.auth-services = [
  {
    type = jwt-es-512-crt
    access-level = wildcard
    certificate = "..."   # required
    # max-token-life = <duration>
    privileged = false
    # target-audience = <string>
    # target-scope = <string>
    users = [
      {
        allowed-services = [ ]   # required
        user-id = "..."   # required
      }
    ]
  }
]
```

```hocon
canton.participants.<participant>.ledger-api.auth-services = [
  {
    type = jwt-jwks
    access-level = wildcard
    # max-token-life = <duration>
    privileged = false
    # target-audience = <string>
    # target-scope = <string>
    url = "..."   # required
    users = [
      {
        allowed-services = [ ]   # required
        user-id = "..."   # required
      }
    ]
  }
]
```

```hocon
canton.participants.<participant>.ledger-api.auth-services = [
  {
    type = party-jwt
    # takes no further keys
  }
]
```

`tls.cert-chain-file` supports these types: `pem-file`, `pem-string`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.ledger-api.tls.cert-chain-file {
  type = pem-file
  pem-file = "/path/to/file"   # required
}
```

```hocon
canton.participants.<participant>.ledger-api.tls.cert-chain-file {
  type = pem-string
  pem-bytes = "..."   # required
}
```

`tls.client-auth` supports these types: `require`, `optional`, `none`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.ledger-api.tls.client-auth {
  type = require
  admin-client {
    cert-chain-file {
      type = pem-file|pem-string   # required
    }
    private-key-file = "..."   # required
  }
}
```

```hocon
canton.participants.<participant>.ledger-api.tls.client-auth {
  type = optional
  # takes no further keys
}
```

```hocon
canton.participants.<participant>.ledger-api.tls.client-auth {
  type = none
  # takes no further keys
}
```

`tls.trust-collection-file` supports these types: `pem-file`, `pem-string`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.ledger-api.tls.trust-collection-file {
  type = pem-file
  pem-file = "/path/to/file"   # required
}
```

```hocon
canton.participants.<participant>.ledger-api.tls.trust-collection-file {
  type = pem-string
  pem-bytes = "..."   # required
}
```

`tls.client-auth.admin-client.cert-chain-file` supports these types: `pem-file`, `pem-string`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.ledger-api.tls.client-auth.admin-client.cert-chain-file {
  type = pem-file
  pem-file = "/path/to/file"   # required
}
```

```hocon
canton.participants.<participant>.ledger-api.tls.client-auth.admin-client.cert-chain-file {
  type = pem-string
  pem-bytes = "..."   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `address` | string | `127.0.0.1` | ledger api server host name. |
| `auth-services` | array of AuthServiceConfig | `[]` | type of authentication services used by ledger-api server. If empty, we use a wildcard. Otherwise, the first service response that does not say "unauthenticated" will be used. |
| `database-connection-timeout` | duration (NonNegativeFiniteDuration) | `30 seconds` | database connection timeout |
| `enable-command-inspection` | boolean | `true` | enable command inspection service over the ledger api |
| `flow-control-window` | int (PositiveInt) | `ServerConfig.defaultFlowControlWindow` ‡ |  |
| `initial-flow-control-window` | int (PositiveInt) | `1048576` |  |
| `management-service-timeout` | duration (NonNegativeFiniteDuration) | `2 minutes` | ledger api server management service maximum duration. Duration has to be finite as the ledger api server uses java.time.duration that does not support infinite scala durations. |
| `max-concurrent-calls-per-connection` | int (NonNegativeInt) | `100000` |  |
| `max-inbound-message-size` | int (NonNegativeInt) | `10485760` | maximum inbound message size on the ledger api |
| `max-inbound-metadata-size` | int (NonNegativeInt) | `8192` |  |
| `max-token-lifetime` | duration (NonNegativeDuration) | `5 minutes` |  |
| `port` | int (Port) | `30001` † | ledger api server port. |
| **admin-token-config** |  |  |  |
|     `act-as-any-party-claim` | boolean | `false` |  |
|     `admin-claim` | boolean | `false` |  |
|     `admin-token-duration` | duration (PositiveFiniteDuration) | `5 minutes` |  |
|     `fixed-admin-token` | string | _unset_ |  |
| **auth-services[]** |  |  |  |
|     `type` | one of `wildcard`, `unsafe-jwt-hmac-256`, `jwt-rs-256-crt`, `jwt-es-256-crt`, `jwt-es-512-crt`, `jwt-jwks`, `party-jwt` | **required** | Choose one type — see [AuthServiceConfig](/global-synchronizer/reference/canton-config/types#authserviceconfig). |
| **auth-services[]** `type = unsafe-jwt-hmac-256` |  |  |  |
|     `access-level` | one of `admin`, `wildcard` | `wildcard` |  |
|     `max-token-life` | duration (NonNegativeDuration) | `NonNegativeDuration(Duration.Inf)` ‡ |  |
|     `privileged` | boolean | `false` |  |
|     `secret` | string (NonEmptyString) | **required** |  |
|     `target-audience` | string | _unset_ |  |
|     `target-scope` | string | _unset_ |  |
|     `users` | array of AuthorizedUser | `[]` |  |
|     **users[]** |  |  |  |
|         `allowed-services` | array of string | **required** |  |
|         `user-id` | string | **required** |  |
| **auth-services[]** `type = jwt-rs-256-crt` |  |  |  |
|     `access-level` | one of `admin`, `wildcard` | `wildcard` |  |
|     `certificate` | string | **required** |  |
|     `max-token-life` | duration (NonNegativeDuration) | `NonNegativeDuration(Duration.Inf)` ‡ |  |
|     `privileged` | boolean | `false` |  |
|     `target-audience` | string | _unset_ |  |
|     `target-scope` | string | _unset_ |  |
|     `users` | array of AuthorizedUser | `[]` |  |
|     **users[]** |  |  |  |
|         `allowed-services` | array of string | **required** |  |
|         `user-id` | string | **required** |  |
| **auth-services[]** `type = jwt-es-256-crt` |  |  |  |
|     `access-level` | one of `admin`, `wildcard` | `wildcard` |  |
|     `certificate` | string | **required** |  |
|     `max-token-life` | duration (NonNegativeDuration) | `NonNegativeDuration(Duration.Inf)` ‡ |  |
|     `privileged` | boolean | `false` |  |
|     `target-audience` | string | _unset_ |  |
|     `target-scope` | string | _unset_ |  |
|     `users` | array of AuthorizedUser | `[]` |  |
|     **users[]** |  |  |  |
|         `allowed-services` | array of string | **required** |  |
|         `user-id` | string | **required** |  |
| **auth-services[]** `type = jwt-es-512-crt` |  |  |  |
|     `access-level` | one of `admin`, `wildcard` | `wildcard` |  |
|     `certificate` | string | **required** |  |
|     `max-token-life` | duration (NonNegativeDuration) | `NonNegativeDuration(Duration.Inf)` ‡ |  |
|     `privileged` | boolean | `false` |  |
|     `target-audience` | string | _unset_ |  |
|     `target-scope` | string | _unset_ |  |
|     `users` | array of AuthorizedUser | `[]` |  |
|     **users[]** |  |  |  |
|         `allowed-services` | array of string | **required** |  |
|         `user-id` | string | **required** |  |
| **auth-services[]** `type = jwt-jwks` |  |  |  |
|     `access-level` | one of `admin`, `wildcard` | `wildcard` |  |
|     `max-token-life` | duration (NonNegativeDuration) | `NonNegativeDuration(Duration.Inf)` ‡ |  |
|     `privileged` | boolean | `false` |  |
|     `target-audience` | string | _unset_ |  |
|     `target-scope` | string | _unset_ |  |
|     `url` | string (NonEmptyString) | **required** |  |
|     `users` | array of AuthorizedUser | `[]` |  |
|     **users[]** |  |  |  |
|         `allowed-services` | array of string | **required** |  |
|         `user-id` | string | **required** |  |
| **command-service** |  |  |  |
|     `contract-prefetching-depth` | int (PositiveInt) | `3` | Levels of pre-fetching before interpretation. This allows the engine to preload all referenced contract ids before interpretation to avoid single contract lookups that have to be done in the database. |
|     `default-tracking-timeout` | duration (NonNegativeFiniteDuration) | `5 minutes` | The duration that the command service will keep tracking an active command by default. This value will be used if a timeout is not specified on a gRPC request. |
|     `max-commands-in-flight` | int | `256` | Maximum number of submitted commands waiting to be completed in parallel. Commands submitted after this limit is reached will be rejected. |
| **identity-provider-management** |  |  |  |
|     `cache-expiry-after-write` | duration (NonNegativeFiniteDuration) | `5 minutes` |  |
| **index-service** |  |  |  |
|     `acs-filtering-parallelism` | int | `16` | The parallelism for filtering the active contracts service stream by synchronizer id and stakeholders. |
|     `api-query-services-thread-pool-size` | int | _unset_ | size of the thread-pool backing the Ledger API query-services (fe not the command submission/interpretation). If not set, defaults to ((number of thread)/4 + 1) |
|     `api-stream-shutdown-timeout` | duration | `5s` | shutdown timeout for a graceful completion of ledger api server's streams |
|     `buffered-events-processing-parallelism` | int | `8` | parallelism for loading and decoding ledger events for populating ledger api server's internal buffers |
|     `buffered-streams-page-size` | int | `100` | the page size for streams created from ledger api server's in-memory buffers |
|     `completions-page-size` | int | `1000` | database / pekko page size for batching of ledger api server index ledger completion queries |
|     `contract-pruning-db-lock-timeout` | duration (PositiveFiniteDuration) | `20 seconds` | This timeout controls how long the system waits to acquire the table lock on the contract pruning service table. |
|     `contract-pruning-delay-before-retry` | duration (NonNegativeFiniteDuration) | `2 seconds` |  |
|     `contract-pruning-max-retries` | int | `10` |  |
|     `global-max-event-id-queries` | int | `20` | maximum number of concurrent event id queries across all stream types |
|     `global-max-event-payload-queries` | int | `10` | maximum number of concurrent event payload queries across all stream types |
|     `idle-stream-offset-checkpoint-timeout` | duration (NonNegativeFiniteDuration) | `1 minute` | the timeout duration for checking if a new OffsetCheckpoint is created |
|     `in-memory-state-updater-parallelism` | int | `2` | the processing parallelism of the Ledger API server in-memory state updater |
|     `max-contract-key-state-cache-size` | int | `10000` | maximum caffeine cache size of mutable state cache of contract keys |
|     `max-contract-state-cache-size` | int | `10000` | maximum caffeine cache size of mutable state cache of contracts |
|     `max-lookup-limit` | int | `1000` | the maximum limit for contract key lookups. Requests will be capped at this value. |
|     `max-rejected-completions-by-hash` | int | `10` | maximum number of rejected completions returned by a lookup-by-hash query. |
|     `max-transactions-in-memory-fan-out-buffer-size` | int | `1100` (in practice `1000`) ◊ | maximum number of transactions to hold in the "in-memory fanout" (if enabled) |
|     `offset-checkpoint-cache-update-interval` | duration (NonNegativeFiniteDuration) | `15 seconds` | the interval duration for OffsetCheckpoint cache updates |
|     `prepare-package-metadata-time-out-warning` | duration (NonNegativeFiniteDuration) | `5 seconds` | timeout for package metadata preparation after which a warning will be logged. deprecated |
|     `pruning-db-lock-timeout` | duration (PositiveFiniteDuration) | `20 seconds` | This timeout controls how long the system waits to acquire the table lock on the pruning service table. |
|     **active-contracts-service-streams** |  |  |  |
|         `contract-processing-parallelism` | int | `8` | The parallelism for contract processing. |
|         `id-filter-query-parallelism` | int | `2` |  |
|         `max-ids-per-id-page` | int | `20000` | Number of event ids to retrieve in a single query (a page of event ids). |
|         `max-incomplete-page-size` | int | `20` | The maximum size of an incomplete page. Stream is chunked up this into groups of this size. |
|         `max-pages-per-id-pages-buffer` | int | `1` | Number of id pages to store in a buffer. There is a buffer for each decomposed filtering constraint. |
|         `max-parallel-active-id-queries` | int | `4` | Number of parallel queries that fetch ids of create events. Per single stream. |
|         `max-parallel-payload-create-queries` | int | `2` | Number of event payloads to retrieve in a single query (a page of event payloads). |
|         `max-payloads-per-payloads-page` | int | `1000` | Number of parallel queries that fetch payloads of create events. Per single stream. |
|         `max-working-memory-in-bytes-for-id-pages` | int | `104857600` | Memory for storing id pages across all id pages buffers. Per single stream. |
|     **updates-streams** |  |  |  |
|         `id-filter-query-parallelism` | int | `2` |  |
|         `max-ids-per-id-page` | int | `20000` | Number of event ids to retrieve in a single query (a page of event ids). |
|         `max-pages-per-id-pages-buffer` | int | `1` | Number of id pages to store in a buffer. There is a buffer for each decomposed filtering constraint. |
|         `max-parallel-id-activate-queries` | int | `4` | Number of parallel queries that fetch ids of create events. Per single stream. |
|         `max-parallel-id-deactivate-queries` | int | `4` | Number of parallel queries that fetch ids of consuming events. Per single stream. |
|         `max-parallel-id-topology-events-queries` | int | `4` | Number of parallel queries that fetch payloads of topology events. Per single stream. |
|         `max-parallel-id-various-witnessed-queries` | int | `4` |  |
|         `max-parallel-payload-activate-queries` | int | `2` | Number of parallel queries that fetch payloads of create events. Per single stream. |
|         `max-parallel-payload-deactivate-queries` | int | `2` | Number of parallel queries that fetch payloads of consuming events. Per single stream. |
|         `max-parallel-payload-queries` | int | `2` | Upper bound on the number of parallel queries that fetch payloads. Per single stream. |
|         `max-parallel-payload-topology-events-queries` | int | `2` | Number of parallel queries that fetch ids of topology events. Per single stream. |
|         `max-parallel-payload-various-witnessed-queries` | int | `2` |  |
|         `max-payloads-per-payloads-page` | int | `1000` | Number of event payloads to retrieve in a single query (a page of event payloads). |
|         `max-working-memory-in-bytes-for-id-pages` | int | `104857600` | Memory for storing id pages across all id pages buffers. Per single stream. |
|         `transactions-processing-parallelism` | int | `8` | Number of transactions to process in parallel. Per single stream. |
| **interactive-submission-service** |  |  |  |
|     `contract-lookup-parallelism` | int (PositiveInt) | `5` | When the transaction uses input contracts, the preparing participant will attempt to look them up from its local store if they are not explicitly disclosed. This limits the parallelism at which this lookup will be done to avoid potentially overwhelming the database |
|     `enable-verbose-hashing` | boolean | `false` | if true, the prepare RPC will gather information about the hashing process of the transaction and return it as part of the response, if the "verboseHashing" flag was set on the request. If false (default), the "verboseHashing" flag on the prepare request has no effect. |
|     `enforce-single-root-node` | boolean | `true` | Reject early requests with multiple commands (prepare) or multiple root nodes (execute). Such transactions are not supported and would be rejected anyway during confirmation. Disabling this check won't provide functional support for those transactions, the flag only rejects them early and prevent their submission to the synchronizer. |
|     `maximum-number-of-signatures-per-party` | int (PositiveInt) | `50` | Maximum number of external signatures that will be allowed per party per submission. |
| **jwks-cache-config** |  |  |  |
|     `auto-refresh-after` | duration (NonNegativeFiniteDuration) | `0 seconds` |  |
|     `cache-expiration` | duration (NonNegativeFiniteDuration) | `5 minutes` |  |
|     `cache-max-size` | int | `1000` |  |
|     `connection-timeout` | duration (NonNegativeFiniteDuration) | `10 seconds` |  |
|     `read-timeout` | duration (NonNegativeFiniteDuration) | `10 seconds` |  |
| **jwt-timestamp-leeway** |  |  |  |
|     `default` | int | _unset_ |  |
|     `expires-at` | int | _unset_ |  |
|     `issued-at` | int | _unset_ |  |
|     `not-before` | int | _unset_ |  |
| **keep-alive-server** |  |  |  |
|     `permit-keep-alive-time` | duration (NonNegativeFiniteDuration) | `10 seconds` |  |
|     `permit-keep-alive-without-calls` | boolean | `false` |  |
|     `time` | duration (NonNegativeFiniteDuration) | `10 minutes` |  |
|     `timeout` | duration (NonNegativeFiniteDuration) | `20 seconds` |  |
| **limits** |  |  |  |
|     `throttle-logging-rate-per-second` | int (NonNegativeInt) | `10` | maximum rate for logging rejections for requests exceeding the limit (prevents DOS on logs) |
|     `warn-on-undefined-limits` | boolean | `false` | emit warning if a limit is not configured for a stream |
|     **active** |  |  |  |
|         `<key>` | int (NonNegativeInt) | `[]` | map of service name to maximum number of parallel active requests or streams |
| **package-service** |  |  |  |
|     `max-vetted-packages-page-size` | int (PositiveInt) | `100` | maximum number of VettedPackages results returned |
| **party-management-service** |  |  |  |
|     `max-parties-page-size` | int (PositiveInt) | `10000` | maximum number of parties returned |
|     `max-self-allocated-parties` | int (NonNegativeInt) | `0` |  |
| **postgres-data-source** |  |  |  |
|     `client-connection-check-interval` | duration (NonNegativeFiniteDuration) | `5 seconds` | Interval for client connection checks. Corresponds to `client_connection_check_interval` (Postgres &gt;= 14 only). Millisecond granularity is the lowest supported precision. A value of 0 disables connection checks. On macOS with Postgres 14, this must be set to `None` (disabled), since in Postgres 14, this setting requires the non-standard `POLLRDHUP` extension to the `poll` system call, which is only available on Linux. See html for details. |
|     `network-timeout` | duration (NonNegativeFiniteDuration) | `60 seconds` | Network timeout for database operations. Millisecond granularity is the lowest supported precision. A value of 0 indicates no timeout. |
|     `tcp-keepalives-count` | int | `5` | TCP keepalive count. Corresponds to `tcp_keepalives_count`. A value of 0 selects the operating system's default. |
|     `tcp-keepalives-idle` | int | `10` | TCP keepalive idle time in seconds. Corresponds to `tcp_keepalives_idle`. See html#RUNTIME-CONFIG-CONNECTION-SETTINGS for details. A value of 0 selects the operating system's default. |
|     `tcp-keepalives-interval` | int | `1` | TCP keepalive interval in seconds. Corresponds to `tcp_keepalives_interval`. A value of 0 selects the operating system's default. |
|     **synchronous-commit** |  |  |  |
|         `type` | one of `on`, `off`, `remote-write`, `remote-apply`, `local` | **required** |  |
| **rate-limit** |  |  |  |
|     `max-api-services-index-db-queue-size` | int | `1000` | The maximum number of non-running items in the IndexDb execution service |
|     `max-api-services-queue-size` | int | `10000` | The maximum number of non-running items in the ApiServices execution service |
|     `max-used-heap-space-percentage` | int | `100` | If, following a garbage collection of the 'tenured' memory pool, the percentage of used pool memory is above this percentage the system will be rate limited until additional space is freed up. |
|     `min-free-heap-space-bytes` | int | `0` | If, following a garbage collection of the 'tenured' memory pool, the amount of free space is below this value the system will be rate limited until additional space is freed up. |
| **state-service** |  |  |  |
|     `default-acs-page-size` | int (PositiveInt) | `2000` | max_page_size value for GetActiveContractsPage request without max_page_size specified |
|     `max-acs-page-size` | int (PositiveInt) | `10000` | inclusive limit for max_page_size for GetActiveContractsPage. Requests exceeding this value will be rejected. |
| **tls** |  |  |  |
|     `ciphers` | array of string | `TlsConfig.defaultCiphers` ‡ | supported ciphers. Set to None (or null in config file) to use default settings defaultCiphers |
|     `enable-cert-revocation-checking` | boolean | `false` | whether to enable certificate revocation checking per https://tersesystems.com/blog/2014/03/22/fixing-certificate-revocation/ |
|     `minimum-server-protocol-version` | string | `TLSv1.2` | minimum supported TLS protocol. Set None (or null in config file) to use default settings defaultMinimumServerProtocol |
|     `private-key-file` | string (PemFile) | **required** | a file containing the server's private key. The key must not use a password. |
|     **cert-chain-file** |  |  |  |
|         `type` | one of `pem-file`, `pem-string` | **required** | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
|     **cert-chain-file** `type = pem-file` |  |  |  |
|         `pem-file` | path (ExistingFile) | **required** |  |
|     **cert-chain-file** `type = pem-string` |  |  |  |
|         `pem-bytes` | string | **required** |  |
|     **client-auth** |  |  |  |
|         `type` | one of `require`, `optional`, `none` | **required** | Choose one type — see [ServerAuthRequirementConfig](/global-synchronizer/reference/canton-config/types#serverauthrequirementconfig). Configuration on whether server requires auth, requests auth, or no auth |
|     **client-auth** `type = require` |  |  |  |
|         **admin-client** |  |  |  |
|             `private-key-file` | string (PemFile) | **required** |  |
|             **cert-chain-file** |  |  |  |
|                 `type` | one of `pem-file`, `pem-string` | **required** | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
|             **cert-chain-file** `type = pem-file` |  |  |  |
|                 `pem-file` | path (ExistingFile) | **required** |  |
|             **cert-chain-file** `type = pem-string` |  |  |  |
|                 `pem-bytes` | string | **required** |  |
|     **trust-collection-file** |  |  |  |
|         `type` | one of `pem-file`, `pem-string` | **required** | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
|     **trust-collection-file** `type = pem-file` |  |  |  |
|         `pem-file` | path (ExistingFile) | **required** |  |
|     **trust-collection-file** `type = pem-string` |  |  |  |
|         `pem-bytes` | string | **required** |  |
| **topology-aware-package-selection** |  |  |  |
|     `enabled` | boolean | `true` | whether to enable topology-aware package selection in command interpretation |
|     `max-passes-default` | int (PositiveInt) | `3` |  |
|     `max-passes-limit` | int (PositiveInt) | `4` |  |
| **update-service** |  |  |  |
|     `ascending-first-page-dynamic-bound-overfetch` | number (DoubleGreaterEqual1) | `4` (in practice `4.0`) ◊ | A multiplier by which the page size is multiplied when fetching an ascending updates page with dynamic lower bound to avoid interference with pruning offset. The implementation fetches (pageSize+1)*ascendingFirstPageDynamicBoundOverfetch elements and if pruning offset in a meantime moved in a way that less than pageSize+1 elements are after the pruning offset, the page fetch fails, otherwise the first page is successfully returned. Try increasing this value if fetching the first page with dynamic lower bound fails ofter. |
|     `default-updates-page-size` | int (PositiveInt) | `100` | max_page_size value for getUpdatesPage request without max_page_size specified |
|     `max-updates-page-size` | int (PositiveInt) | `2000` | inclusive limit for max_page_size for getUpdatesPage. Requests exceeding this value will be rejected. |
| **user-management-service** |  |  |  |
|     `additional-admin-user-id` | string | _unset_ | adds an extra admin |
|     `cache-expiry-after-write-in-seconds` | int | `5` | determines the maximum delay for propagating user management state changes |
|     `enabled` | boolean | `true` | whether to enable participant user management |
|     `max-cache-size` | int | `100` | maximum in-memory cache size for user management state |
|     `max-rights-per-user` | int | `1000` | maximum number of rights per user |
|     `max-users-page-size` | int | `1000` | maximum number of users returned |

</Tab>
</Tabs>

### `canton.participants.<participant>.monitoring`

<Tabs>
<Tab title="HOCON">

```hocon
canton.participants.<participant>.monitoring {
  grpc-health-server {
    address = "0.0.0.0"
    jwt-timestamp-leeway {
      # default = <int>
      # expires-at = <int>
      # issued-at = <int>
      # not-before = <int>
    }
    keep-alive-server {
      permit-keep-alive-time = "20 seconds"
      permit-keep-alive-without-calls = false
      time = "40 seconds"
      timeout = "20 seconds"
    }
    parallelism = 4
    # port = <int>
  }
  http-health-server {
    address = "0.0.0.0"
    port = 0   # required
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| **grpc-health-server** |  |  |  |
|     `address` | string | `0.0.0.0` |  |
|     `parallelism` | int | `4` | number of threads to be used in the gRPC server |
|     `port` | int (Port) | _unset_ |  |
|     **jwt-timestamp-leeway** |  |  |  |
|         `default` | int | _unset_ |  |
|         `expires-at` | int | _unset_ |  |
|         `issued-at` | int | _unset_ |  |
|         `not-before` | int | _unset_ |  |
|     **keep-alive-server** |  |  |  |
|         `permit-keep-alive-time` | duration (NonNegativeFiniteDuration) | `20 seconds` |  |
|         `permit-keep-alive-without-calls` | boolean | `false` |  |
|         `time` | duration (NonNegativeFiniteDuration) | `40 seconds` |  |
|         `timeout` | duration (NonNegativeFiniteDuration) | `20 seconds` |  |
| **http-health-server** |  |  |  |
|     `address` | string | `0.0.0.0` |  |
|     `port` | int (Port) | **required** |  |

</Tab>
</Tabs>

### `canton.participants.<participant>.parameters`

<Tabs>
<Tab title="HOCON">

```hocon
canton.participants.<participant>.parameters {
  acs-commitments {
    acs-fetch-parallelism = 16
    classification-parallelism = 8
    consistency-check {
      journal-snapshot-query-limit = 100
    }
    contract-change-classification-batch-size = 100
    contract-change-classification-parallelism = 8
    counterparty-batch-size = 1000
    digest-compute-parallelism = 8
    digest-load-parallelism = 1000
    # digest-pipeline-buffer-size = <int>
    # digest-update-persistence-batch-factor = <int>
    disable-old-acs-commitment-processor = on-new-protocol-versions
    enable-new-acs-commitment-processor = true
    load-batching {
      maximum-batch-size = 500
      type = batching|no-batching   # required
    }
    matching-parallelism = 20
    max-num-loaded-digests = 10000
    max-num-updates-between-checkpoints = 100000
    max-parallel-active-id-queries = 12
    max-parallel-payload-create-queries = 6
    period-store {
      writer-page-size = 100
    }
    received-commitment-validation-parallelism = 1
    reinitializing-journal-tombstones-batch-size = 1000
    sender {
      max-batch-size = 100
      # max-retry-delay = <duration>
      max-send-delay-fraction = 0.9
      min-send-delay-fraction = 0
      parallelism = 10
    }
    tracing = disabled
    use-sequential-digest-accumulator = false
  }
  activation-frequency-for-warn-about-consistency-checks = 1000
  admin-workflow {
    auto-load-dar = true
    bong-test-max-level = 0
    max-bong-duration = "15 minutes"
    ping-response-timeout = "30 seconds"
    retries = true
  }
  alpha-online-party-replication-support {
    pause-synchronizer-indexing-during-party-replication = false
    # test-interceptor = <string>
    unsafe-sequencer-channel-support = false
  }
  alpha-version-support = false
  auto-sync-protocol-feature-flags = true
  batching {
    aggregator {
      maximum-batch-size = 500
      type = batching|no-batching   # required
    }
    contract-store-aggregator {
      maximum-batch-size = 500
      type = batching|no-batching   # required
    }
    # in-flight-aggregations-query-interval = <duration>
    ledger-api-pruning-batch-size = 50000
    max-acs-import-batch-size = 1000
    max-items-in-batch = 100
    max-pruning-batch-size = 1000
    max-pruning-time-interval = "30 minutes"
    max-stakeholder-groups-batch-size = 1000
    max-topology-update-batch-size = 1000
    max-topology-write-batch-size = 1000
    mediator-fetch-finalized-responses-aggregator {
      maximum-batch-size = 500
      type = batching|no-batching   # required
    }
    mediator-store-finalized-responses-aggregator {
      maximum-batch-size = 500
      type = batching|no-batching   # required
    }
    parallelism = 8
    pruning-parallelism = 2
    topology-cache-aggregator {
      maximum-batch-size = 500
      type = batching|no-batching   # required
    }
  }
  beta-version-support = false
  caching {
    bft-ordering-batch-cache {
      expire-after-access = "1 minute"
      maximum-memory = "..."   # required
    }
    contract-store {
      expire-after-access = "1 minute"
      maximum-size = 0   # required
    }
    finalized-mediator-confirmation-requests {
      expire-after-access = "1 minute"
      maximum-size = 0   # required
    }
    indexed-strings {
      expire-after-access = "1 minute"
      maximum-size = 0   # required
    }
    key-cache {
      expire-after-access = "1 minute"
      maximum-size = 0   # required
    }
    kms-metadata-cache {
      expire-after-access = "1 minute"
      maximum-size = 0   # required
    }
    member-cache {
      expire-after-access = "1 minute"
      maximum-size = 0   # required
    }
    package-upgrade-cache {
      maximum-size = 0   # required
    }
    package-vetting-cache {
      expire-after-access = "1 minute"
      maximum-size = 0   # required
    }
    participant-cache {
      expire-after-access = "1 minute"
      maximum-size = 0   # required
    }
    party-cache {
      expire-after-access = "1 minute"
      maximum-size = 0   # required
    }
    public-key-conversion-cache {
      expire-after-access = "1 minute"
      maximum-size = 0   # required
    }
    sequencer-catchup-payload-cache {
      expire-after-access = "1 minute"
      maximum-memory = "..."   # required
    }
    sequencer-payload-cache {
      expire-after-access = "1 minute"
      maximum-memory = "..."   # required
    }
    session-encryption-key-cache {
      enabled = true
      receiver-cache {
        expire-after-timeout = "10 minutes"
        maximum-size = 0   # required
      }
      sender-cache {
        expire-after-timeout = "10 minutes"
        maximum-size = 0   # required
      }
    }
    synchronizer-client-max-timestamp {
      expire-after-access = "1 minute"
      maximum-size = 0   # required
    }
    topology-snapshot {
      expire-after-access = "1 minute"
      maximum-size = 0   # required
    }
  }
  command-progress-tracker {
    enabled = true
    max-failed = 100
    max-pending = 1000
    max-succeeded = 100
  }
  commitment-asynchronous-initialization = false
  commitment-checkpoint-interval = "1 minute"
  commitment-mismatch-debugging = false
  # commitment-processor-nr-acs-changes-behind-to-trigger-catch-up = <int>
  commitment-reduce-parallelism = 1
  commitment-use-db-snapshot-for-participant-lookup = false
  connect-to-synchronizers-on-startup = true
  crash-after-failed-validation = true
  dev-version-support = false
  disable-upgrade-validation = false
  do-not-await-on-checking-incoming-commitments = false
  dont-warn-on-deprecated-pv = false
  enable-all-ledger-api-reassignments = false
  enable-strict-dar-validation = true
  engine {
    enable-additional-consistency-checks = false
    enable-engine-stack-traces = false
    extensions {
      <extension> {
        address = "..."   # required
        auth {
          type = none|bearer-token-file   # required
        }
        connect-timeout = "500 milliseconds"
        # max-response-body-bytes = <int>
        max-retries = 3
        port = 0   # required
        request-timeout = "8 seconds"
        retry-initial-delay = "1000 milliseconds"
        retry-max-delay = "10 seconds"
        tls {
          enabled = true
          trust-collection-file {
            type = pem-file|pem-string   # required
          }
        }
        validate-on-startup = false
        version = "v1"
      }
    }
    iterations-between-interruptions = 10000
    # profile-dir = <unresolved>
    # snapshot-dir = <unresolved>
    submission-phase-logging {
      enabled = true
      log-level = "DEBUG"
      matching = [ ]
    }
    transaction-limits {
      acting-parties = 0   # required
      choice-authorizers = 0   # required
      choice-observers = 0   # required
      contract-signatories = 0   # required
      contract-stakeholders = 0   # required
      external-call-result-size = 0   # required
      external-call-results = 0   # required
      key-maintainers = 0   # required
      node-children = 0   # required
      query-result = 0   # required
      total-informees = 0   # required
      transaction-input-contracts = 0   # required
      transaction-nodes = 0   # required
      transaction-roots = 0   # required
      value-size = 0   # required
    }
    validation-phase-logging {
      enabled = true
      log-level = "DEBUG"
      matching = [ ]
    }
  }
  journal-garbage-collection-delay = "0 seconds"
  journal-garbage-collection-minimum-gap = "30 minutes"
  ledger-api-server {
    contract-id-seeding {
      type = strong|weak|static   # required
    }
    contract-loader {
      max-batch-size = 50
      max-queue-size = 10000
      parallelism = 5
    }
    indexer {
      achs-config {
        aggregation-threshold = 5
        buffer-size = 256
        init-aggregation-threshold = 5
        init-parallelism = 8
        last-populated-distance-target = 0   # required
        population-parallelism = 4
        removal-parallelism = 4
        valid-at-distance-target = 0   # required
      }
      batching-parallelism = 4
      contract-pruning-db-lock-timeout = "20 seconds"
      contract-read-row-db-lock-timeout = "20 seconds"
      db-prepare-parallelism = 4
      disable-monotonicity-checks = false
      enable-compression = false
      enable-compression-consuming-exercise = false
      enable-compression-non-consuming-exercise = false
      ingestion-parallelism = 16
      input-mapping-parallelism = 16
      max-input-buffer-size = 50
      max-output-batched-buffer-size = 16
      max-tailer-batch-size = 10
      post-processing-parallelism = 8
      postgres-data-source {
        client-connection-check-interval = "5 seconds"
        network-timeout = "60 seconds"
        synchronous-commit {
          type = on|off|remote-write|remote-apply|local   # required
        }
        tcp-keepalives-count = 5
        tcp-keepalives-idle = 10
        tcp-keepalives-interval = 1
      }
      queue-buffer-size = 50
      queue-max-blocked-offer = 1000
      queue-recovery-retry-attempt-error-threshold = 100
      queue-recovery-retry-attempt-warn-threshold = 50
      queue-recovery-retry-max-wait-millis = 5000
      queue-recovery-retry-min-wait-millis = 50
      queue-uncommitted-warn-threshold = 14000
      restart-delay = "10 seconds"
      submission-batch-insertion-size = 5000
      submission-batch-size = 50
      use-weighted-batching = false
    }
    # token-expiry-grace-period-for-streams = <duration>
  }
  lsu {
    automatically-perform-lsu = true
    handshake {
      minimum-duration = "5 seconds"
      periodic-check = "1 second"
      retry {
        initial-delay = "30 seconds"   # required
        max-delay = "30 seconds"   # required
        max-retries = 0   # required
      }
    }
    lsu-retry {
      initial-delay = "30 seconds"   # required
      max-delay = "30 seconds"   # required
      max-retries = 0   # required
    }
    purge-obsolete-topology {
      chunk-size = 1000
      cron = "0 0 0 * * ?"
      max-duration = "30 minutes"
      purgeable-stores-list-validity = "1 minute"
    }
    sequencer-ids-retrieval-retry {
      initial-delay = "30 seconds"   # required
      max-delay = "30 seconds"   # required
      max-retries = 0   # required
    }
  }
  max-unzipped-dar-size = 1073741824
  minimum-protocol-version = 34
  package-metadata-view {
    init-load-parallelism = 16
    init-process-parallelism = 16
    init-takes-too-long-initial-delay = "1 minute"
    init-takes-too-long-interval = "10 seconds"
  }
  stores {
    journal-pruning {
      initial-interval = "5 seconds"
      max-buckets = 100
      max-items-expected-to-prune-per-batch = 100000
      target-batch-size = 5000
    }
    pruning-metric-update-interval = "1 hour"
    safe-to-prune-commitment-state {
      type = match|match-or-mismatch|all   # required
    }
  }
  validate-legacy-contracts-v-11 = true
  warn-if-overloaded-for = "20 seconds"
  watchdog {
    # check-interval = <duration>
    enabled = false   # required
    # kill-delay = <duration>
  }
}
```

`acs-commitments.load-batching` supports these types: `batching`, `no-batching`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.parameters.acs-commitments.load-batching {
  type = batching
  maximum-batch-size = 500
  maximum-in-flight = 2
}
```

```hocon
canton.participants.<participant>.parameters.acs-commitments.load-batching {
  type = no-batching
  max-parallel-batches = 2
  maximum-batch-size = 500
}
```

`batching.aggregator` supports these types: `batching`, `no-batching`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.parameters.batching.aggregator {
  type = batching
  maximum-batch-size = 500
  maximum-in-flight = 2
}
```

```hocon
canton.participants.<participant>.parameters.batching.aggregator {
  type = no-batching
  max-parallel-batches = 2
  maximum-batch-size = 500
}
```

`batching.contract-store-aggregator` supports these types: `batching`, `no-batching`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.parameters.batching.contract-store-aggregator {
  type = batching
  maximum-batch-size = 500
  maximum-in-flight = 2
}
```

```hocon
canton.participants.<participant>.parameters.batching.contract-store-aggregator {
  type = no-batching
  max-parallel-batches = 2
  maximum-batch-size = 500
}
```

`batching.mediator-fetch-finalized-responses-aggregator` supports these types: `batching`, `no-batching`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.parameters.batching.mediator-fetch-finalized-responses-aggregator {
  type = batching
  maximum-batch-size = 500
  maximum-in-flight = 2
}
```

```hocon
canton.participants.<participant>.parameters.batching.mediator-fetch-finalized-responses-aggregator {
  type = no-batching
  max-parallel-batches = 2
  maximum-batch-size = 500
}
```

`batching.mediator-store-finalized-responses-aggregator` supports these types: `batching`, `no-batching`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.parameters.batching.mediator-store-finalized-responses-aggregator {
  type = batching
  maximum-batch-size = 500
  maximum-in-flight = 2
}
```

```hocon
canton.participants.<participant>.parameters.batching.mediator-store-finalized-responses-aggregator {
  type = no-batching
  max-parallel-batches = 2
  maximum-batch-size = 500
}
```

`batching.topology-cache-aggregator` supports these types: `batching`, `no-batching`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.parameters.batching.topology-cache-aggregator {
  type = batching
  maximum-batch-size = 500
  maximum-in-flight = 2
}
```

```hocon
canton.participants.<participant>.parameters.batching.topology-cache-aggregator {
  type = no-batching
  max-parallel-batches = 2
  maximum-batch-size = 500
}
```

`engine.extensions.<extension>.auth` supports these types: `none`, `bearer-token-file`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.parameters.engine.extensions.<extension>.auth {
  type = none
  # takes no further keys
}
```

```hocon
canton.participants.<participant>.parameters.engine.extensions.<extension>.auth {
  type = bearer-token-file
  token-file = "/path/to/file"   # required
}
```

`engine.extensions.<extension>.tls.trust-collection-file` supports these types: `pem-file`, `pem-string`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.parameters.engine.extensions.<extension>.tls.trust-collection-file {
  type = pem-file
  pem-file = "/path/to/file"   # required
}
```

```hocon
canton.participants.<participant>.parameters.engine.extensions.<extension>.tls.trust-collection-file {
  type = pem-string
  pem-bytes = "..."   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `activation-frequency-for-warn-about-consistency-checks` | int | `1000` | controls how often warning messages about enableAdditionalConsistencyChecks being enabled are logged, measured in the number of contract activations during a single connection to a synchronizer. Used only for database storage. |
| `alpha-version-support` **alpha** | boolean | `false` | If set to true, will allow the participant to connect to a synchronizer with alpha protocol version. |
| `auto-sync-protocol-feature-flags` | boolean | `true` | When true (default), protocol feature flags will be automatically updated when the node connects to a synchronizer. |
| `beta-version-support` **beta** | boolean | `false` |  |
| `commitment-asynchronous-initialization` | boolean | `false` | Enables asynchronous initialization of the ACS commitment processor. This speeds up reconnection to a synchronizer at the expense of potentially increased memory and resource consumption while the initialization is running in the background. |
| `commitment-checkpoint-interval` | duration (PositiveDurationSeconds) | `1 minute` | Checkpoint interval for commitments. Smaller intervals lead to less resource-intensive crash recovery, at the cost of more frequent DB writing of checkpoints. Regardless of this checkpoint interval, checkpointing is also performed at reconciliation interval boundaries. |
| `commitment-mismatch-debugging` | boolean | `false` | Enables fine-grained logs of the changes the ACS commitment processor applies. It also enables consistency checks in the ACS commitment processor. Should only be enabled for debugging purposes, and never in prod. |
| `commitment-processor-nr-acs-changes-behind-to-trigger-catch-up` | int (PositiveInt) | _unset_ | Optional parameter tuning how aggressively the participant's ACS commitment processor can catch up when it needs to process many ACS changes. If None, the standard catch-up settings apply, i.e., the processor catches up if it's behind in processing incoming commitments and commitment computation is slow. If set, the participant triggers catch-up if it's behind in processing incoming commitments, and it's behind in processing ACS changes, regardless of whether its commitment computation is slow or not. |
| `commitment-reduce-parallelism` | int (NonNegativeInt) | `1` |  |
| `commitment-use-db-snapshot-for-participant-lookup` | boolean | `false` |  |
| `connect-to-synchronizers-on-startup` | boolean | `true` | If true, connects to synchronizers that have manualConnect=false on startup. Default: true. Has impact only if manual-start is false. |
| `crash-after-failed-validation` | boolean | `true` | If true (default), the participant crashes when it detects a protocol violation that a correct node cannot cause, such as an approved request whose activeness check failed locally. The corresponding `SyncServiceAlarm` is logged either way. Temporary, until availability under attack is supported. |
| `dev-version-support` | boolean | `false` | If set to true, will allow the participant to connect to a synchronizer with dev protocol version, it will turn on Daml LF dev version, and applies the dev database schema, which does not provide data continuity and must not be used in production. |
| `disable-upgrade-validation` | boolean | `false` | Disable the package upgrade verification on DAR upload |
| `do-not-await-on-checking-incoming-commitments` | boolean | `false` | Enable fully asynchronous checking of incoming commitments. This may result in some incoming commitments not being checked in case of crashes or HA failovers. |
| `dont-warn-on-deprecated-pv` | boolean | `false` | If true, then this participant will not emit a warning when connecting to a sequencer using a deprecated protocol version (such as 2.0.0). |
| `enable-all-ledger-api-reassignments` | boolean | `false` | Determines whether ACS imports use Created or Assigned events. Similarly, determines whether the repair service uses Created/Archive events or Assigned/Unassigned events for add and purge respectively. Only enable if your Ledger API consumers can process (un)assigned events and require non-zero reassignment counters. - false (Default): Uses Created/Archive; resets reassignment counters to zero. - true: Uses Assigned/Unassigned; preserves existing reassignment counters. Note: If multi-synchronizer is enabled via the EnableMultiSynchronizer flag, then Assigned and Unassigned event will be emitted when processing reassignments messages from the synchronizer regardless of the value of enableAllLedgerApiReassignments. |
| `enable-strict-dar-validation` | boolean | `true` | Enables the throwing of an error if lf&gt;2.2 and the dar is not self-sufficient (i.e. throw an error if the set of included packages does not equal the set of imported packages). Normally, packages produced by damlc targeting 2.2 or above are self-sufficient. If set to false, errors are logged as warning instead. |
| `journal-garbage-collection-delay` | duration (NonNegativeFiniteDuration) | `0 seconds` | How much time to delay the canton journal garbage collection |
| `journal-garbage-collection-minimum-gap` | duration (PositiveFiniteDuration) | `30 minutes` | The minimum gap in observed sequencing times between two journal garbage collection runs. |
| `max-unzipped-dar-size` | int | `1073741824` | maximum allowed size of unzipped DAR files (in bytes) the participant can accept for uploading. Defaults to 1GB. |
| `minimum-protocol-version` | string (ParticipantProtocolVersion) | `34` | The minimum protocol version that this participant will speak when connecting to a synchronizer |
| `validate-legacy-contracts-v-11` | boolean | `true` | Enables an extra validation for contracts with contract id version V11. Keep this enabled in production. |
| `warn-if-overloaded-for` | duration (NonNegativeFiniteDuration) | `20 seconds` | If all incoming commands have been rejected due to PARTICIPANT_BACKPRESSURE during this interval, the participant will log a warning. |
| **acs-commitments** |  |  |  |
|     `acs-fetch-parallelism` | int (PositiveInt) | `16` | the number of concurrent acs streams that are prepared. This counteracts the high fixed cost of establishing an ACS stream for many parties. The ACS stream of the first batch of counterparties is fully consumed before the pipeline starts to consume the ACS stream of the second batch of counterparties. |
|     `classification-parallelism` | int (PositiveInt) | `8` | the number of concurrent ACS update classifications. Default is 8. |
|     `contract-change-classification-batch-size` | int (PositiveInt) | `100` | the maximum number of batched contract change classifications. Should be at most `maxNumLoadedDigests`. Default is 100. These classifications can be batched in 3 situations: - When processing an AcsChangeUpdate - When ingesting active contracts during reinitialization - When ingesting active contracts while processing a locally onboarded party |
|     `contract-change-classification-parallelism` | int (PositiveInt) | `8` | the number of concurrent contract change classifications when loading the ACS for parties. Default is 8. |
|     `counterparty-batch-size` | int (PositiveInt) | `1000` | how many counterparties get their digest updated at a time in case of a local party onboarding or offboarding. With the assumption that a party may have a lot of counterparties, but each counterparty is only hosted on a small number of participants, this parameter essentially limits how many digests are loaded into memory: `numDigestsInMemory = counterPartyBatchSize * hostingParticipantsOfCounterparties`. Default is 1000. |
|     `digest-compute-parallelism` | int (PositiveInt) | `8` | The maximum number of parallel digest computations. Default is 8. |
|     `digest-load-parallelism` | int (PositiveInt) | `1000` | The maximum number of concurrently buffered reads for loading digests. Default is 1000. If loadBatching enables batching, this number should be at least as large as the batch size times the batch parallelism; otherwise batches will not be filled up. If batching is disabled, this number directly controls the number of parallel DB reads and should therefore be much smaller. |
|     `digest-pipeline-buffer-size` | int (NonNegativeInt) | `NonNegativeInt.zero` ‡ | The size of intermediate buffers in the digest processor pipeline. If the size is 0, no buffers are added. These buffers implicitly increase the `digestLoadParallelism` by twice the configured size. Default is 0. |
|     `digest-update-persistence-batch-factor` | int (PositiveInt) | `PositiveInt.two` ‡ | the maximum size of digest update batches is calculated by multiplying `maxNumLoadedDigests * digestUpdatePersistenceBatchFactor`. Default is 2. |
|     `disable-old-acs-commitment-processor` | one of `always`, `on-new-protocol-versions`, `never` | `on-new-protocol-versions` | whether the old ACS commitment processor should be disabled. Default is on new protocol versions. |
|     `enable-new-acs-commitment-processor` | boolean | `true` | whether the new ACS digest processor should be enabled or not. Default is false. |
|     `matching-parallelism` | int (PositiveInt) | `20` | the maximum number of parallel processing of received ACS commitments for matching against locally computed commitments. Default is 20. |
|     `max-num-loaded-digests` | int (PositiveInt) | `10000` | the maximum number of digests that may be loaded into memory during processing. Default is 10000. |
|     `max-num-updates-between-checkpoints` | int (PositiveInt) | `100000` | the maximum number of acs updates after which a checkpoint should be written. Default is 100000. |
|     `max-parallel-active-id-queries` | int (PositiveInt) | `12` | the number of parallel queries to fetch the IDs of active contracts. The higher the parallelism, the higher the potential load on the DB. Default is 12. |
|     `max-parallel-payload-create-queries` | int (PositiveInt) | `6` | the number of parallel queries to fetch the contract payload of active contracts. The * higher the parallelism, the higher the potential load on the DB. Default is 6. |
|     `received-commitment-validation-parallelism` | int (PositiveInt) | `1` | the number of parallel threads to use for verifying incoming commitments. Default is 1. |
|     `reinitializing-journal-tombstones-batch-size` | int (PositiveInt) | `1000` | the number of digest updates in a batch during the ACS digest reinitializing process. Default is 1000. |
|     `tracing` | one of `disabled`, `incremental`, `full` | `disabled` | the tracing mode. Default is disabled. |
|     `use-sequential-digest-accumulator` | boolean | `false` | whether to use the sequential or the batching digest accumulator. Default is true. |
|     **consistency-check** |  |  |  |
|         `journal-snapshot-query-limit` | int (PositiveInt) | `100` | The limit (page size) passed to the DigestJournal#snapshot when retrieving ACS digest updates continously. Default is 100. |
|     **load-batching** |  |  |  |
|         `maximum-batch-size` | int (PositiveNumeric) | `500` | Maximum number of queries in a batch. |
|         `type` | one of `batching`, `no-batching` | **required** | Choose one type — see [BatchAggregatorConfig](/global-synchronizer/reference/canton-config/types#batchaggregatorconfig). Parameters for that batcher that batches queries (e.g., to a DB). |
|     **load-batching** `type = batching` |  |  |  |
|         `maximum-in-flight` | int (PositiveNumeric) | `2` | Maximum number of in-flight get queries. |
|     **load-batching** `type = no-batching` |  |  |  |
|         `max-parallel-batches` | int (PositiveNumeric) | `2` | Maximum number of batches to execute in parallel when using runMany. |
|     **period-store** |  |  |  |
|         `writer-page-size` | int (PositiveInt) | `100` |  |
|     **sender** |  |  |  |
|         `max-batch-size` | int (PositiveInt) | `100` | the max number of envelopes that will be put in a submission request. |
|         `max-retry-delay` | duration (NonNegativeFiniteDuration) | `AcsCommitmentSenderConfig.defaultMaxRetryDelay` ‡ |  |
|         `max-send-delay-fraction` | number | `0.9` | The fraction of the reconciliation interval to maximally delay the sending of a commitment beyond the period end. If the commitment production is delayed, this fraction may be exceeded. Default is 0.9. |
|         `min-send-delay-fraction` | number | `0` | the fraction of the reconciliation interval to minimally delay the sending of a commitment beyond the period end. Default is 0. |
|         `parallelism` | int (PositiveInt) | `10` | the number of parallel threads to use when parallelism is used (at the moment signing messages only). Default is 10. |
| **admin-workflow** |  |  |  |
|     `auto-load-dar` | boolean | `true` | If set to true (default), we will load the admin workflow package automatically. Setting this to false will break some admin workflows. |
|     `bong-test-max-level` | int (NonNegativeInt) | `0` (in practice `20`) ◊ | Upper bound (exclusive) on the level of a bong that the participant can participate. Any bong with higher level will be vacuumed immediately. Default 0, which means it won't participate in any bongs. For security reasons, do not change in production. (If changed, the node could suffer a denial-of-service attack.) |
|     `max-bong-duration` | duration (NonNegativeFiniteDuration) | `15 minutes` | Cap the maximum duration of a bong. Default is 15 minutes. |
|     `ping-response-timeout` | duration (NonNegativeFiniteDuration) | `30 seconds` | How long we will attempt to respond to a ping request before giving up |
|     `retries` | boolean | `true` | If false (default true), we will not retry sending commands in case of failures |
| **alpha-online-party-replication-support** |  |  |  |
|     `pause-synchronizer-indexing-during-party-replication` **alpha** | boolean | `false` |  |
|     `test-interceptor` **alpha** | string (TestInterceptor) | _unset_ |  |
|     `unsafe-sequencer-channel-support` **alpha** | boolean | `false` |  |
| **batching** |  |  |  |
|     `in-flight-aggregations-query-interval` | duration (PositiveFiniteDuration) | `BatchingConfig.defaultInFlightAggregationsQueryInterval` ‡ | (optionally) split aggregator queries into intervals of this duration to avoid sequential scans. When too many query intervals are generated, query interval splitting does not occur. |
|     `ledger-api-pruning-batch-size` | int (PositiveNumeric) | `50000` | Number of events to prune from the ledger api server index-database at a time during automatic background pruning. Canton-internal store pruning happens at the smaller batch size of "maxPruningBatchSize" to minimize memory usage whereas ledger-api-server index-db pruning needs sufficiently large batches to amortize the database overhead of "skipping over" active contracts. |
|     `max-acs-import-batch-size` | int (PositiveNumeric) | `1000` | maximum number of active contracts in a batch to be imported |
|     `max-items-in-batch` | int (PositiveNumeric) | `100` | maximum number of items in a batch (default value) |
|     `max-pruning-batch-size` | int (PositiveNumeric) | `1000` | maximum number of events to prune from a participant at a time, used to break up canton participant-internal batches |
|     `max-pruning-time-interval` | duration (PositiveFiniteDuration) | `30 minutes` | split pruning queries into intervals of this duration to avoid sequential scans |
|     `max-stakeholder-groups-batch-size` | int (PositiveNumeric) | `1000` | maximum number of stakeholder groups in a batch when reading commitments from a snapshot |
|     `max-topology-update-batch-size` | int (PositiveNumeric) | `1000` | maximum number of items in an update batch |
|     `max-topology-write-batch-size` | int (PositiveNumeric) | `1000` | maximum number of items in a write batch |
|     `parallelism` | int (PositiveNumeric) | `8` | number of parallel queries to the db. defaults to 8 |
|     `pruning-parallelism` | int (PositiveNumeric) | `2` | number of parallel pruning queries to the db. defaults to 2 |
|     **aggregator** |  |  |  |
|         `maximum-batch-size` | int (PositiveNumeric) | `500` | Maximum number of queries in a batch. |
|         `type` | one of `batching`, `no-batching` | `batching` † | Choose one type — see [BatchAggregatorConfig](/global-synchronizer/reference/canton-config/types#batchaggregatorconfig). Parameters for that batcher that batches queries (e.g., to a DB). |
|     **aggregator** `type = batching` |  |  |  |
|         `maximum-in-flight` | int (PositiveNumeric) | `2` | Maximum number of in-flight get queries. |
|     **aggregator** `type = no-batching` |  |  |  |
|         `max-parallel-batches` | int (PositiveNumeric) | `2` | Maximum number of batches to execute in parallel when using runMany. |
|     **contract-store-aggregator** |  |  |  |
|         `maximum-batch-size` | int (PositiveNumeric) | `500` (in practice `50`) ◊ | Maximum number of queries in a batch. |
|         `type` | one of `batching`, `no-batching` | `batching` † | Choose one type — see [BatchAggregatorConfig](/global-synchronizer/reference/canton-config/types#batchaggregatorconfig). Parameters for that batcher that batches queries (e.g., to a DB). |
|     **contract-store-aggregator** `type = batching` |  |  |  |
|         `maximum-in-flight` | int (PositiveNumeric) | `2` (in practice `5`) ◊ | Maximum number of in-flight get queries. |
|     **contract-store-aggregator** `type = no-batching` |  |  |  |
|         `max-parallel-batches` | int (PositiveNumeric) | `2` | Maximum number of batches to execute in parallel when using runMany. |
|     **mediator-fetch-finalized-responses-aggregator** |  |  |  |
|         `maximum-batch-size` | int (PositiveNumeric) | `500` | Maximum number of queries in a batch. |
|         `type` | one of `batching`, `no-batching` | `batching` † | Choose one type — see [BatchAggregatorConfig](/global-synchronizer/reference/canton-config/types#batchaggregatorconfig). Parameters for that batcher that batches queries (e.g., to a DB). |
|     **mediator-fetch-finalized-responses-aggregator** `type = batching` |  |  |  |
|         `maximum-in-flight` | int (PositiveNumeric) | `2` | Maximum number of in-flight get queries. |
|     **mediator-fetch-finalized-responses-aggregator** `type = no-batching` |  |  |  |
|         `max-parallel-batches` | int (PositiveNumeric) | `2` | Maximum number of batches to execute in parallel when using runMany. |
|     **mediator-store-finalized-responses-aggregator** |  |  |  |
|         `maximum-batch-size` | int (PositiveNumeric) | `500` | Maximum number of queries in a batch. |
|         `type` | one of `batching`, `no-batching` | `batching` † | Choose one type — see [BatchAggregatorConfig](/global-synchronizer/reference/canton-config/types#batchaggregatorconfig). Parameters for that batcher that batches queries (e.g., to a DB). |
|     **mediator-store-finalized-responses-aggregator** `type = batching` |  |  |  |
|         `maximum-in-flight` | int (PositiveNumeric) | `2` | Maximum number of in-flight get queries. |
|     **mediator-store-finalized-responses-aggregator** `type = no-batching` |  |  |  |
|         `max-parallel-batches` | int (PositiveNumeric) | `2` | Maximum number of batches to execute in parallel when using runMany. |
|     **topology-cache-aggregator** |  |  |  |
|         `maximum-batch-size` | int (PositiveNumeric) | `500` | Maximum number of queries in a batch. |
|         `type` | one of `batching`, `no-batching` | `batching` † | Choose one type — see [BatchAggregatorConfig](/global-synchronizer/reference/canton-config/types#batchaggregatorconfig). Parameters for that batcher that batches queries (e.g., to a DB). |
|     **topology-cache-aggregator** `type = batching` |  |  |  |
|         `maximum-in-flight` | int (PositiveNumeric) | `2` | Maximum number of in-flight get queries. |
|     **topology-cache-aggregator** `type = no-batching` |  |  |  |
|         `max-parallel-batches` | int (PositiveNumeric) | `2` | Maximum number of batches to execute in parallel when using runMany. |
| **caching** |  |  |  |
|     **bft-ordering-batch-cache** |  |  |  |
|         `expire-after-access` | duration (NonNegativeFiniteDuration) | `1 minute` (in practice `10 minutes`) ◊ |  |
|         `maximum-memory` | string (PositiveNumeric) | `67108864` † |  |
|     **contract-store** |  |  |  |
|         `expire-after-access` | duration (NonNegativeFiniteDuration) | `1 minute` | how quickly after last access items should be expired from the cache |
|         `maximum-size` | int (PositiveNumeric) | `10000` † | the maximum size of the cache |
|     **finalized-mediator-confirmation-requests** |  |  |  |
|         `expire-after-access` | duration (NonNegativeFiniteDuration) | `1 minute` | how quickly after last access items should be expired from the cache |
|         `maximum-size` | int (PositiveNumeric) | `1000` † | the maximum size of the cache |
|     **indexed-strings** |  |  |  |
|         `expire-after-access` | duration (NonNegativeFiniteDuration) | `1 minute` | how quickly after last access items should be expired from the cache |
|         `maximum-size` | int (PositiveNumeric) | `10000` † | the maximum size of the cache |
|     **key-cache** |  |  |  |
|         `expire-after-access` | duration (NonNegativeFiniteDuration) | `1 minute` | how quickly after last access items should be expired from the cache |
|         `maximum-size` | int (PositiveNumeric) | `1000` † | the maximum size of the cache |
|     **kms-metadata-cache** |  |  |  |
|         `expire-after-access` | duration (NonNegativeFiniteDuration) | `1 minute` | how quickly after last access items should be expired from the cache |
|         `maximum-size` | int (PositiveNumeric) | `20` † | the maximum size of the cache |
|     **member-cache** |  |  |  |
|         `expire-after-access` | duration (NonNegativeFiniteDuration) | `1 minute` | how quickly after last access items should be expired from the cache |
|         `maximum-size` | int (PositiveNumeric) | `1000` † | the maximum size of the cache |
|     **package-upgrade-cache** |  |  |  |
|         `maximum-size` | int (PositiveNumeric) | `10000` † | the maximum size of the cache |
|     **package-vetting-cache** |  |  |  |
|         `expire-after-access` | duration (NonNegativeFiniteDuration) | `1 minute` | how quickly after last access items should be expired from the cache |
|         `maximum-size` | int (PositiveNumeric) | `10000` † | the maximum size of the cache |
|     **participant-cache** |  |  |  |
|         `expire-after-access` | duration (NonNegativeFiniteDuration) | `1 minute` | how quickly after last access items should be expired from the cache |
|         `maximum-size` | int (PositiveNumeric) | `1000` † | the maximum size of the cache |
|     **party-cache** |  |  |  |
|         `expire-after-access` | duration (NonNegativeFiniteDuration) | `1 minute` | how quickly after last access items should be expired from the cache |
|         `maximum-size` | int (PositiveNumeric) | `10000` † | the maximum size of the cache |
|     **public-key-conversion-cache** |  |  |  |
|         `expire-after-access` | duration (NonNegativeFiniteDuration) | `1 minute` (in practice `60 minutes`) ◊ | how quickly after last access items should be expired from the cache |
|         `maximum-size` | int (PositiveNumeric) | `10000` † | the maximum size of the cache |
|     **sequencer-catchup-payload-cache** |  |  |  |
|         `expire-after-access` | duration (NonNegativeFiniteDuration) | `1 minute` (in practice `10 minutes`) ◊ |  |
|         `maximum-memory` | string (PositiveNumeric) | `67108864` † |  |
|     **sequencer-payload-cache** |  |  |  |
|         `expire-after-access` | duration (NonNegativeFiniteDuration) | `1 minute` (in practice `10 minutes`) ◊ |  |
|         `maximum-memory` | string (PositiveNumeric) | `67108864` † |  |
|     **session-encryption-key-cache** |  |  |  |
|         `enabled` | boolean | `true` | enable/disable caching of the session key. Caching is enabled by default, offering a trade-off between secrecy and performance |
|         **receiver-cache** |  |  |  |
|             `expire-after-timeout` | duration (PositiveFiniteDuration) | `10 minutes` (in practice `10 seconds`) ◊ | how quickly after creation items should be expired from the cache |
|             `maximum-size` | int (PositiveNumeric) | `10000` † | the maximum size of the cache |
|         **sender-cache** |  |  |  |
|             `expire-after-timeout` | duration (PositiveFiniteDuration) | `10 minutes` (in practice `10 seconds`) ◊ | how quickly after creation items should be expired from the cache |
|             `maximum-size` | int (PositiveNumeric) | `10000` † | the maximum size of the cache |
|     **synchronizer-client-max-timestamp** |  |  |  |
|         `expire-after-access` | duration (NonNegativeFiniteDuration) | `1 minute` | how quickly after last access items should be expired from the cache |
|         `maximum-size` | int (PositiveNumeric) | `100` † | the maximum size of the cache |
|     **topology-snapshot** |  |  |  |
|         `expire-after-access` | duration (NonNegativeFiniteDuration) | `1 minute` | how quickly after last access items should be expired from the cache |
|         `maximum-size` | int (PositiveNumeric) | `100` † | the maximum size of the cache |
| **command-progress-tracker** |  |  |  |
|     `enabled` | boolean | `true` |  |
|     `max-failed` | int (NonNegativeInt) | `100` |  |
|     `max-pending` | int (NonNegativeInt) | `1000` |  |
|     `max-succeeded` | int (NonNegativeInt) | `100` |  |
| **engine** |  |  |  |
|     `enable-additional-consistency-checks` | boolean | `false` (in practice `true`) ◊ | When true, extra consistency checks are performed for transactions, ensuring that serialization/deserialization and enrichment/impoverishment processes remain idempotent on transaction outputs generated by the engine. Requires canton.parameters.non-standard-config = true |
|     `enable-engine-stack-traces` | boolean | `false` | If true, DAMLe stack traces will be enabled |
|     `iterations-between-interruptions` | int | `10000` | Number of engine iterations between forced interruptions (outside needs of information). |
|     `profile-dir` | Array (unclassified) | _unset_ | path to the directory used for Daml profiling WARNING: The profiler is not intended to be run in production. Enabling it has potential security consequences. |
|     `snapshot-dir` | Array (unclassified) | _unset_ | path to the directory used for saving transaction tree snapshots |
|     **extensions** |  |  |  |
|         **&lt;extension&gt;** |  |  |  |
|             `address` | string | **required** | Hostname of the extension service |
|             `connect-timeout` | duration (PositiveFiniteDuration) | `500 milliseconds` | Connection timeout |
|             `max-response-body-bytes` | int (PositiveInt) | `ExtensionServiceConfig.DefaultMaxResponseBodyBytes` ‡ | Maximum HTTP response body size accepted from the extension service. |
|             `max-retries` | int (NonNegativeInt) | `3` | Maximum number of retry attempts |
|             `port` | int (Port) | **required** | Port of the extension service |
|             `request-timeout` | duration (PositiveFiniteDuration) | `8 seconds` | Request timeout for individual HTTP requests |
|             `retry-initial-delay` | duration (NonNegativeFiniteDuration) | `1000 milliseconds` | Initial delay before first retry |
|             `retry-max-delay` | duration (NonNegativeFiniteDuration) | `10 seconds` | Maximum delay between retries |
|             `validate-on-startup` | boolean | `false` | Whether participant startup should validate that the configured extension service endpoint is reachable. |
|             `version` | string | `v1` | API version path segment exposed by the extension service |
|             **auth** |  |  |  |
|                 `type` | one of `none`, `bearer-token-file` | **required** | Choose one type — see [ExtensionServiceAuthConfig](/global-synchronizer/reference/canton-config/types#extensionserviceauthconfig). Authentication configuration for outbound calls to an extension service. |
|             **auth** `type = bearer-token-file` |  |  |  |
|                 `token-file` | path (ExistingFile) | **required** |  |
|             **tls** |  |  |  |
|                 `enabled` | boolean | `true` | allows enabling TLS without `trustCollectionFile` |
|                 **trust-collection-file** |  |  |  |
|                     `type` | one of `pem-file`, `pem-string` | **required** | Choose one type — see [PemFileOrString](/global-synchronizer/reference/canton-config/types#pemfileorstring). A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
|                 **trust-collection-file** `type = pem-file` |  |  |  |
|                     `pem-file` | path (ExistingFile) | **required** |  |
|                 **trust-collection-file** `type = pem-string` |  |  |  |
|                     `pem-bytes` | string | **required** |  |
|     **submission-phase-logging** |  |  |  |
|         `enabled` | boolean | `true` | set to true to enable |
|         `log-level` | Level (unclassified) | `DEBUG` | the log level to use |
|         `matching` | array of string | `[]` | if non-empty, then only output lines that match one of the given string. If the supporting * and ? wildcard characters are used, then the string must match, otherwise, the string must be included in the log message. |
|     **transaction-limits** |  |  |  |
|         `acting-parties` | int | **required** |  |
|         `choice-authorizers` | int | **required** |  |
|         `choice-observers` | int | **required** |  |
|         `contract-signatories` | int | **required** |  |
|         `contract-stakeholders` | int | **required** |  |
|         `external-call-result-size` | int | **required** |  |
|         `external-call-results` | int | **required** |  |
|         `key-maintainers` | int | **required** |  |
|         `node-children` | int | **required** |  |
|         `query-result` | int | **required** |  |
|         `total-informees` | int | **required** |  |
|         `transaction-input-contracts` | int | **required** |  |
|         `transaction-nodes` | int | **required** |  |
|         `transaction-roots` | int | **required** |  |
|         `value-size` | int | **required** |  |
|     **validation-phase-logging** |  |  |  |
|         `enabled` | boolean | `true` (in practice `false`) ◊ | set to true to enable |
|         `log-level` | Level (unclassified) | `DEBUG` | the log level to use |
|         `matching` | array of string | `[]` | if non-empty, then only output lines that match one of the given string. If the supporting * and ? wildcard characters are used, then the string must match, otherwise, the string must be included in the log message. |
| **ledger-api-server** |  |  |  |
|     `token-expiry-grace-period-for-streams` | duration (NonNegativeDuration) | _unset_ | grace periods for streams that postpone termination beyond the JWT expiry |
|     **contract-id-seeding** |  |  |  |
|         `type` | one of `strong`, `weak`, `static` | **required** | This type is deprecated and no longer used (see `ledgerApiServerParametersConfigReader` in CantonConfig.scala). It is kept in the code solely for backwards compatibility. |
|     **contract-loader** |  |  |  |
|         `max-batch-size` | int (PositiveInt) | `50` | how many contract lookups to batch together |
|         `max-queue-size` | int (PositiveInt) | `10000` | how many parallel lookups can be queued before we start to backpressure loading |
|         `parallelism` | int (PositiveInt) | `5` | how many parallel contract lookup requests should be sent to the db when prepopulating the cache |
|     **indexer** |  |  |  |
|         `batching-parallelism` | int (NonNegativeInt) | `4` |  |
|         `contract-pruning-db-lock-timeout` | duration (PositiveFiniteDuration) | `20 seconds` |  |
|         `contract-read-row-db-lock-timeout` | duration (PositiveFiniteDuration) | `20 seconds` |  |
|         `db-prepare-parallelism` | int (NonNegativeInt) | `4` |  |
|         `disable-monotonicity-checks` | boolean | `false` |  |
|         `enable-compression` | boolean | `false` |  |
|         `enable-compression-consuming-exercise` | boolean | `false` |  |
|         `enable-compression-non-consuming-exercise` | boolean | `false` |  |
|         `ingestion-parallelism` | int (NonNegativeInt) | `16` |  |
|         `input-mapping-parallelism` | int (NonNegativeInt) | `16` |  |
|         `max-input-buffer-size` | int (NonNegativeInt) | `50` |  |
|         `max-output-batched-buffer-size` | int | `16` |  |
|         `max-tailer-batch-size` | int | `10` |  |
|         `post-processing-parallelism` | int | `8` |  |
|         `queue-buffer-size` | int | `50` |  |
|         `queue-max-blocked-offer` | int | `1000` |  |
|         `queue-recovery-retry-attempt-error-threshold` | int | `100` |  |
|         `queue-recovery-retry-attempt-warn-threshold` | int | `50` |  |
|         `queue-recovery-retry-max-wait-millis` | int | `5000` |  |
|         `queue-recovery-retry-min-wait-millis` | int | `50` |  |
|         `queue-uncommitted-warn-threshold` | int | `14000` (in practice `5000`) ◊ |  |
|         `restart-delay` | duration (NonNegativeFiniteDuration) | `10 seconds` |  |
|         `submission-batch-insertion-size` | int | `5000` |  |
|         `submission-batch-size` | int | `50` |  |
|         `use-weighted-batching` | boolean | `false` |  |
|         **achs-config** |  |  |  |
|             `aggregation-threshold` | int | `5` | Aggregation threshold for ACHS maintenance batches. |
|             `buffer-size` | int | `256` | Size of the ACHS buffer. |
|             `init-aggregation-threshold` | int | `5` | Aggregation threshold for ACHS maintenance batches during initialization. |
|             `init-parallelism` | int (NonNegativeInt) | `8` | Parallelism for ACHS population/removal during initialization. |
|             `last-populated-distance-target` | int (NonNegativeLong) | `5` † | The target distance (in event sequential ids) between the valid at and the last populated offset of the ACHS. |
|             `population-parallelism` | int (NonNegativeInt) | `4` | Parallelism for ACHS population during normal operation. |
|             `removal-parallelism` | int (NonNegativeInt) | `4` | Parallelism for ACHS removal during normal operation. |
|             `valid-at-distance-target` | int (NonNegativeLong) | `10` † | The target distance (in event sequential ids) between the current ledger end and the valid at of the ACHS. |
|         **postgres-data-source** |  |  |  |
|             `client-connection-check-interval` | duration (NonNegativeFiniteDuration) | `5 seconds` | Interval for client connection checks. Corresponds to `client_connection_check_interval` (Postgres &gt;= 14 only). Millisecond granularity is the lowest supported precision. A value of 0 disables connection checks. On macOS with Postgres 14, this must be set to `None` (disabled), since in Postgres 14, this setting requires the non-standard `POLLRDHUP` extension to the `poll` system call, which is only available on Linux. See html for details. |
|             `network-timeout` | duration (NonNegativeFiniteDuration) | `60 seconds` (in practice `40 seconds`) ◊ | Network timeout for database operations. Millisecond granularity is the lowest supported precision. A value of 0 indicates no timeout. |
|             `tcp-keepalives-count` | int | `5` | TCP keepalive count. Corresponds to `tcp_keepalives_count`. A value of 0 selects the operating system's default. |
|             `tcp-keepalives-idle` | int | `10` | TCP keepalive idle time in seconds. Corresponds to `tcp_keepalives_idle`. See html#RUNTIME-CONFIG-CONNECTION-SETTINGS for details. A value of 0 selects the operating system's default. |
|             `tcp-keepalives-interval` | int | `1` | TCP keepalive interval in seconds. Corresponds to `tcp_keepalives_interval`. A value of 0 selects the operating system's default. |
|             **synchronous-commit** |  |  |  |
|                 `type` | one of `on`, `off`, `remote-write`, `remote-apply`, `local` | **required** |  |
| **lsu** |  |  |  |
|     `automatically-perform-lsu` | boolean | `true` | Whether to automatically perform LSU. Default is true. |
|     **handshake** |  |  |  |
|         `minimum-duration` | duration (NonNegativeFiniteDuration) | `5 seconds` | If defined: after a successful handshake, will continue to perform handshake with the sequencers for the specified duration. Should not be too big (in the order of a few seconds). |
|         `periodic-check` | duration (NonNegativeFiniteDuration) | `1 second` | Duration between two checks whether the wait should be interrupted. Has an impact only if the following two conditions hold: - minimumDuration is non-empty - is smaller than minimumDuration |
|         **retry** |  |  |  |
|             `initial-delay` | duration (NonNegativeFiniteDuration) | `1 minute` † | initial delay before the first retry |
|             `max-delay` | duration (NonNegativeDuration) | `5 minutes` † | max delay between retries |
|             `max-retries` | int | `2147483647` † | max number of retries |
|     **lsu-retry** |  |  |  |
|         `initial-delay` | duration (NonNegativeFiniteDuration) | `200 milliseconds` † | initial delay before the first retry |
|         `max-delay` | duration (NonNegativeDuration) | `5 seconds` † | max delay between retries |
|         `max-retries` | int | `2147483647` † | max number of retries |
|     **purge-obsolete-topology** |  |  |  |
|         `chunk-size` | int (PositiveInt) | `1000` | The amount of data that should be removed per purge iteration. |
|         `cron` | string | `0 0 0 * * ?` | A cron expression, defining when the purges can take place |
|         `max-duration` | duration (PositiveFiniteDuration) | `30 minutes` | Once triggered, the amount of time to repeatedly purge chunks, before having to wait for the next cron-defined trigger. |
|         `purgeable-stores-list-validity` | duration (NonNegativeFiniteDuration) | `1 minute` |  |
|     **sequencer-ids-retrieval-retry** |  |  |  |
|         `initial-delay` | duration (NonNegativeFiniteDuration) | `10 seconds` † | initial delay before the first retry |
|         `max-delay` | duration (NonNegativeDuration) | `30 seconds` † | max delay between retries |
|         `max-retries` | int | `2147483647` † | max number of retries |
| **package-metadata-view** |  |  |  |
|     `init-load-parallelism` | int | `16` |  |
|     `init-process-parallelism` | int | `16` |  |
|     `init-takes-too-long-initial-delay` | duration | `1 minute` (in practice `1m`) ◊ |  |
|     `init-takes-too-long-interval` | duration | `10 seconds` (in practice `10s`) ◊ |  |
| **stores** |  |  |  |
|     `pruning-metric-update-interval` | duration (PositiveDurationSeconds) | `1 hour` | How frequently to update the `max-event-age` pruning progress metric in the background. A setting of None disables background metric updating. |
|     **journal-pruning** |  |  |  |
|         `initial-interval` | duration (NonNegativeFiniteDuration) | `5 seconds` | The initial interval size for pruning |
|         `max-buckets` | int (PositiveInt) | `100` | The maximum number of buckets used for any pruning interval |
|         `max-items-expected-to-prune-per-batch` | int (PositiveInt) | `100000` | The maximum on the number of items expected to prune per batch. Implemented by select stores (e.g. ActiveContractStore) to help motivate database filtered index selection. Should be at least an order of magnitude larger than targetBatchSize to not interfere with dynamic bucketing after a participant node has been inactive for a while, but not excessively large to discourage scanning the entire table. |
|         `target-batch-size` | int (PositiveInt) | `5000` | The target batch size for pruning. The actual batch size will evolve under load. |
|     **safe-to-prune-commitment-state** |  |  |  |
|         `type` | one of `match`, `match-or-mismatch`, `all` | **required** | Defines which counter-participants cannot block pruning because of the states of their commitments. The modes increasingly relax the pruning conditions. These modes override the default, which is that all counter-participants block pruning when there isn't a timestamp where all counter-participants sent matching commitments, and non-matching participants are not in a no-wait configuration. |
| **watchdog** |  |  |  |
|     `check-interval` | duration (PositiveFiniteDuration) | `PositiveFiniteDuration(15.seconds)` ‡ |  |
|     `enabled` | boolean | **required** |  |
|     `kill-delay` | duration (NonNegativeFiniteDuration) | `NonNegativeFiniteDuration(30.seconds)` ‡ |  |

</Tab>
</Tabs>

### `canton.participants.<participant>.replication`

<Tabs>
<Tab title="HOCON">

```hocon
canton.participants.<participant>.replication {
  connection-pool {
    active-timeout = "15 seconds"
    connection {
      client-connection-check-interval = "5 seconds"
      connection-timeout = "10 seconds"
      health-check-period = "5 seconds"
      health-check-timeout = "15 seconds"
      initial-acquisition-interval = "200 milliseconds"
      initial-acquisition-max-retries = 5
      keep-alive-count = 5
      keep-alive-idle = "10 seconds"
      keep-alive-interval = "1 second"
      lock {
        health-check-period = "5 seconds"
        health-check-timeout = "15 seconds"
      }
      max-inconclusive-read-only-checks = 3
      passive-check-period = "15 seconds"
    }
    health-check-period = "5 seconds"
    initial-locked-connection-timeout = "15 seconds"
    initial-must-remain-active-connection-timeout = "1 minute"
  }
  # enabled = <boolean>
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `enabled` | boolean | _unset_ | Needs to be set to true for a replicated node, which shares the database with other replicas. |
| **connection-pool** |  |  |  |
|     `active-timeout` | duration (PositiveFiniteDuration) | `15 seconds` | Time to wait until the first connection in the pool becomes active during failover. |
|     `health-check-period` | duration (PositiveFiniteDuration) | `5 seconds` | Health check period, i.e., how long to wait between health checks of the connection pool. |
|     `initial-locked-connection-timeout` | duration (PositiveFiniteDuration) | `15 seconds` | Time to wait until the initial connection is either active or passive (external sequencer does not use this) |
|     `initial-must-remain-active-connection-timeout` | duration (PositiveFiniteDuration) | `1 minute` | If using an external sequencer we want to make sure it is always active, unless it is closing. This timeout is the initial time to wait before first becoming active, otherwise it is shutdown. |
|     **connection** |  |  |  |
|         `client-connection-check-interval` | duration (NonNegativeFiniteDuration) | `5 seconds` | Interval for client connection checks. Corresponds to the Postgres `client_connection_check_interval` configuration parameter (Postgres &gt;= 14 only). Millisecond granularity is the lowest supported precision. `None` leaves the server default unchanged whereas a Some(0) explicitly disables the check. On macOS with Postgres 14, this must be disabled, since in Postgres 14, this setting requires the non-standard `POLLRDHUP` extension to the `poll` system call, which is only available on Linux. See html for details. |
|         `connection-timeout` | duration (PositiveFiniteDuration) | `10 seconds` | Timeout for requesting a new connection from the underlying database driver in seconds granularity. |
|         `health-check-period` | duration (PositiveFiniteDuration) | `5 seconds` | Health check period, i.e., how long to wait between one health check completed and the next to start. |
|         `health-check-timeout` | duration (PositiveFiniteDuration) | `15 seconds` | Timeout for running a health check in seconds granularity. |
|         `initial-acquisition-interval` | duration (PositiveFiniteDuration) | `200 milliseconds` | Retry intervals during the initial attempts to acquire the lock. |
|         `initial-acquisition-max-retries` | int | `5` | Maximum number of retries when trying to acquire the lock for the first time before trying to acquire the lock in a background task. |
|         `keep-alive-count` | int | `5` | TCP keep-alive count, i.e., how many unanswered keep-alive messages required to consider the connection lost. |
|         `keep-alive-idle` | duration (PositiveFiniteDuration) | `10 seconds` | TCP keep-alive idle time, i.e., how long to wait until sending a keep-alive message when idle. |
|         `keep-alive-interval` | duration (PositiveFiniteDuration) | `1 second` | TCP keep-alive interval, i.e., how long to wait until resending an unanswered keep-alive message. |
|         `max-inconclusive-read-only-checks` | int | `3` | Number of consecutive inconclusive (timed-out or failed) read-only checks tolerated before the connection is considered lost. A single inconclusive check can be due to transient contention, but a persistently inconclusive check is problematic. |
|         `passive-check-period` | duration (PositiveFiniteDuration) | `15 seconds` | How long to wait between trying to become active. |
|         **lock** |  |  |  |
|             `health-check-period` | duration (PositiveFiniteDuration) | `5 seconds` | Health check period, i.e., how long to wait between one health check completed and the next to start. |
|             `health-check-timeout` | duration (PositiveFiniteDuration) | `15 seconds` | Timeout for running a health check in seconds granularity. |

</Tab>
</Tabs>

### `canton.participants.<participant>.sequencer-client`

<Tabs>
<Tab title="HOCON">

```hocon
canton.participants.<participant>.sequencer-client {
  acknowledgement-interval = "1 minute"
  amplify-on-max-sequencing-time-too-far = true
  auth-token {
    backoff {
      base = 2
      jitter {
        type = equal|full   # required
      }
      max-retry-interval = "2 minutes"
    }
    min-retry-interval = "500 milliseconds"
    refresh-auth-token-before-expiry = "20 seconds"
    retries = 20
  }
  # channel-flow-control-window = <int>
  channel-initial-flow-control-window = 1048576
  channel-max-inbound-message-size = 134217728
  default-max-sequencing-time-offset = "2 minutes"
  enable-amplification-improvements = true
  event-inbox-size = 100
  handshake-retry-attempts = 50
  handshake-retry-delay = "5 seconds"
  initial-connection-retry-delay = "10 milliseconds"
  keep-alive-client {
    idle-timeout = "30 minutes"
    keep-alive-without-calls = false
    time = "40 seconds"
    timeout = "15 seconds"
  }
  max-connection-retry-delay = "30 seconds"
  maximum-in-flight-event-batches = 20
  # override-max-request-size = <int>
  past-events-cache-size = 1000
  skip-sequenced-event-validation = false
  startup-connection-retry-delay = "1 second"
  time-readings-retention = "5 minutes"
  use-new-aggregator = true
  use-new-connection-pool = true
  warn-disconnect-delay = "10 seconds"
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `acknowledgement-interval` | duration (NonNegativeFiniteDuration) | `1 minute` | Controls how frequently the client acknowledges how far it has successfully processed to the sequencer which allows the sequencer to remove this data when pruning. |
| `amplify-on-max-sequencing-time-too-far` | boolean | `true` |  |
| `channel-flow-control-window` | int (PositiveInt) | `ClientChannelParams.DefaultFlowControlWindow` ‡ | switches to manual gRPC flow control and sets its window; if `None`, then it is not configured and the implementation default is used. Switches to automatic gRPC flow control and sets its initial window; if `None`, then it is not |
| `channel-initial-flow-control-window` | int (PositiveInt) | `1048576` | Switches to automatic gRPC flow control and sets its initial window; if `None`, then it is not configured and the implementation default is used. If present, it is set after the `flowControlWindow` parameter, so it overrides it. |
| `channel-max-inbound-message-size` | int (NonNegativeInt) | `134217728` | max inbound request size for the grpc channel to the sequencer. |
| `default-max-sequencing-time-offset` | duration (NonNegativeFiniteDuration) | `2 minutes` | if no max-sequencing-time is supplied to send, our current time will be offset by this amount |
| `enable-amplification-improvements` | boolean | `true` | Enables some improvements in the amplification behavior: - When sending a submission request with amplification, if we receive a synchronous error from the sequencer that is classified as `RequestRefused`, we abort amplification and bubble up the error to the caller. If this parameter is enabled, in the specific case of a sequencer refusing the submission because it is overloaded (Overloaded), we will immediately continue to the next step of amplification. - Scheduling of the next amplification takes into account the time taken by the transport to provide a response. |
| `event-inbox-size` | int (PositiveInt) | `100` | The size of the inbox queue used to store received events. Must be at least one. Events in the inbox are processed in parallel. A larger inbox may result in higher throughput at the price of higher memory consumption, larger database queries, and longer crash recovery. |
| `handshake-retry-attempts` | int (NonNegativeInt) | `50` | How many attempts should we make to get a handshake response |
| `handshake-retry-delay` | duration (NonNegativeFiniteDuration) | `5 seconds` | How long to delay between attempts to fetch a handshake response |
| `initial-connection-retry-delay` | duration (NonNegativeFiniteDuration) | `10 milliseconds` | Initial delay before a reconnect attempt |
| `max-connection-retry-delay` | duration (NonNegativeFiniteDuration) | `30 seconds` | Maximum delay before a reconnect attempt |
| `maximum-in-flight-event-batches` | int (PositiveInt) | `20` | The maximum number of event batches that the system will process concurrently. Setting the `maximumInFlightEventBatches` parameter limits the number of event batches that the system will process simultaneously, preventing overload and ensuring that the system can handle the workload effectively. A higher value of `maximumInFlightEventBatches` can lead to increased throughput, but at the cost of higher memory consumption and longer processing times for each batch. A lower value of `maximumInFlightEventBatches` may limit throughput, but can result in more stable and predictable system behavior. |
| `override-max-request-size` | int (NonNegativeInt) | _unset_ | overrides the maxRequestSize configured in the dynamic synchronizer parameters. If overrideMaxRequestSize is set, modifying the maxRequestSize won't have any effect. This is only used for testing. |
| `past-events-cache-size` | int (PositiveInt) | `1000` | If a subscription receives an event for aggregation which has already been decided, a cache of past successfully aggregated events is used to determine whether this is legitimate (possibly due to load or catching up) or not. Only the pastEventsCacheSize most recent events are kept in the cache. Must be at least 1. Larger values reduce the likelihood of falsely classifying an old event as illegitimate, at the cost of more memory consumption. Only effective if useNewAggregator is enabled. |
| `skip-sequenced-event-validation` | boolean | `false` | if true, sequenced event validation will be skipped. the default setting is false. this option should only be enabled if a defective validation is blocking processing. therefore, unless you know what you are doing, you shouldn't touch this setting. |
| `startup-connection-retry-delay` | duration (NonNegativeFiniteDuration) | `1 second` | Initial delay before we attempt to establish an initial connection |
| `time-readings-retention` | duration (PositiveFiniteDuration) | `5 minutes` | The duration for which sequencing time readings are retained. This setting depends on the assumptions about dynamic faults and ensures that faulty time readings skewed towards the future do not linger in the system for too long. Regular eviction also ensures that time readings from offboarded sequencers are not taken into account indefinitely. |
| `use-new-aggregator` | boolean | `true` | Use the new BFT sequencer aggregator instead of the former one. |
| `use-new-connection-pool` | boolean | `true` | Use the new sequencer connection pool instead of the former transports. |
| `warn-disconnect-delay` | duration (NonNegativeFiniteDuration) | `10 seconds` | Consider sequencer to be degraded after delay |
| **auth-token** |  |  |  |
|     `min-retry-interval` | duration (NonNegativeFiniteDuration) | `500 milliseconds` |  |
|     `refresh-auth-token-before-expiry` | duration (NonNegativeFiniteDuration) | `20 seconds` | how much time before the auth token expires should we fetch a new one? |
|     `retries` | int (NonNegativeInt) | `20` |  |
|     **backoff** |  |  |  |
|         `base` | int (NonNegativeInt) | `2` |  |
|         `max-retry-interval` | duration (NonNegativeFiniteDuration) | `2 minutes` |  |
|         **jitter** |  |  |  |
|             `type` | one of `equal`, `full` | **required** |  |
| **keep-alive-client** |  |  |  |
|     `idle-timeout` | duration (NonNegativeFiniteDuration) | `30 minutes` | If there are no RPCs during this duration, the channel will close all connections and switch to Idle state (default is 30 min) |
|     `keep-alive-without-calls` | boolean | `false` | Enables sending of keep alive even when there are no RPCs on the channel (default to false). This increases network resource consumption for idle connections and requires the server to enable permitKeepAliveWithoutCalls. Prefer using idleTimeout. |
|     `time` | duration (NonNegativeFiniteDuration) | `40 seconds` | Sets the time without read activity before sending a keepalive ping. Do not set to small numbers (default is 40s) |
|     `timeout` | duration (NonNegativeFiniteDuration) | `15 seconds` | Sets the time waiting for read activity after sending a keepalive ping (default is 15s). The default timeout was previously 20s and was lowered to 15s, because besides configuring the gRPC KeepAliveManager, it also sets up the socket's TCP_USER_TIMEOUT (see html). 15s gives a larger margin to detect a faulty connection earlier and retry a submission on another sequencer via amplification, thereby avoiding a request failure. |

</Tab>
</Tabs>

### `canton.participants.<participant>.storage`

<Tabs>
<Tab title="HOCON">

```hocon
canton.participants.<participant>.storage {
  parameters {
    connection-allocation {
      # num-ledger-api = <int>
      # num-reads = <int>
      # num-writes = <int>
    }
    connection-timeout = "5 seconds"
    fail-fast-on-startup = true
    failed-to-fatal-delay = "5 minutes"
    # max-connections = <int>
    migrate-and-start = false
    migrations-paths = [ ]
    partitions {
      initial-bft-orderer-tables-partition-size = 1500
    }
    repeatable-migrations-paths = [ ]
    unsafe-baseline-on-migrate = false
    unsafe-clean-on-validation-error = false
    # warn-on-slow-query = <duration>
    warn-on-slow-query-interval = "5 seconds"
  }
  type = memory|h2|postgres   # required
}
```

`` supports these types: `memory`, `h2`, `postgres`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.storage {
  type = memory
  parameters {
    connection-allocation {
      # num-ledger-api = <int>
      # num-reads = <int>
      # num-writes = <int>
    }
    connection-timeout = "5 seconds"
    fail-fast-on-startup = true
    failed-to-fatal-delay = "5 minutes"
    # max-connections = <int>
    migrate-and-start = false
    migrations-paths = [ ]
    partitions {
      initial-bft-orderer-tables-partition-size = 1500
    }
    repeatable-migrations-paths = [ ]
    unsafe-baseline-on-migrate = false
    unsafe-clean-on-validation-error = false
    # warn-on-slow-query = <duration>
    warn-on-slow-query-interval = "5 seconds"
  }
}
```

```hocon
canton.participants.<participant>.storage {
  type = h2
  parameters {
    connection-allocation {
      # num-ledger-api = <int>
      # num-reads = <int>
      # num-writes = <int>
    }
    connection-timeout = "5 seconds"
    fail-fast-on-startup = true
    failed-to-fatal-delay = "5 minutes"
    # max-connections = <int>
    migrate-and-start = false
    migrations-paths = [ ]
    partitions {
      initial-bft-orderer-tables-partition-size = 1500
    }
    repeatable-migrations-paths = [ ]
    unsafe-baseline-on-migrate = false
    unsafe-clean-on-validation-error = false
    # warn-on-slow-query = <duration>
    warn-on-slow-query-interval = "5 seconds"
  }
}
```

```hocon
canton.participants.<participant>.storage {
  type = postgres
  parameters {
    connection-allocation {
      # num-ledger-api = <int>
      # num-reads = <int>
      # num-writes = <int>
    }
    connection-timeout = "5 seconds"
    fail-fast-on-startup = true
    failed-to-fatal-delay = "5 minutes"
    # max-connections = <int>
    migrate-and-start = false
    migrations-paths = [ ]
    partitions {
      initial-bft-orderer-tables-partition-size = 1500
    }
    repeatable-migrations-paths = [ ]
    unsafe-baseline-on-migrate = false
    unsafe-clean-on-validation-error = false
    # warn-on-slow-query = <duration>
    warn-on-slow-query-interval = "5 seconds"
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `type` | one of `memory`, `h2`, `postgres` | `memory` † | Choose one type — see [StorageConfig](/global-synchronizer/reference/canton-config/types#storageconfig). Determines how a node stores persistent data. |
| **parameters** |  |  |  |
|     `connection-timeout` | duration (NonNegativeFiniteDuration) | `5 seconds` (in practice `30 seconds`) ◊ | How long to wait for acquiring a database connection |
|     `fail-fast-on-startup` | boolean | `true` | If true, the node will fail-fast when the database cannot be connected to If false, the node will wait indefinitely for the database to come up |
|     `failed-to-fatal-delay` | duration (NonNegativeFiniteDuration) | `5 minutes` | Delay after which, if the storage is continuously in a Failed state, it will escalate to Fatal. The default value is 5 minutes. Components that use the storage as a health dependency can then determine how to react. Currently, the sequencer declares it as a fatal dependency for its liveness health, which means it will transition to NOT_SERVING if this delay is exceeded, allowing a monitoring infrastructure to restart it. **NOTE**: Currently this only applies to DbStorageSingle, which is only used by the sequencer. TODO(i24240): Apply the same behavior to `DbStorageMulti` |
|     `max-connections` | int (PositiveInt) | _unset_ | Allows for setting the maximum number of db connections used by Canton and the ledger API server. If None, the value will be auto-detected from the number of processors. Has no effect, if the number of connections is already set via slick options (i.e., `config.numThreads`). |
|     `migrate-and-start` | boolean | `false` | if true, db migrations will be applied to the database (default is to abort start if db migrates are pending to force an explicit upgrade) |
|     `migrations-paths` | array of string | `[]` | Where should database migrations be read from. Enables specialized DDL for different database servers (e.g. Postgres). |
|     `repeatable-migrations-paths` | array of string | `[]` | Additional locations to scan for migrations. The goal is to be able to provide additional database table settings under this path, i.e. vacuum/analyze settings for Postgres. The paths should only contain repeatable migrations (files like `R__table_settings.sql`). |
|     `unsafe-baseline-on-migrate` | boolean | `false` | TO BE USED ONLY FOR TESTING! &lt;p&gt;Whether to automatically call baseline when migrate is executed against a non-empty schema with no schema history table. This schema will then be baselined with the \{@code baselineVersion\} before executing the migrations. Only migrations above \{@code baselineVersion\} will then be applied.&lt;/p&gt; &lt;p&gt;This is useful for databases projects where the initial vendor schema is not empty&lt;/p&gt; If baseline should be called on migrate for non-empty schemas, \{ @code false\} if not. (default: \{ @code false\}) |
|     `unsafe-clean-on-validation-error` | boolean | `false` | TO BE USED ONLY FOR TESTING! Clean the database if validation during DB migration fails. |
|     `warn-on-slow-query` | duration (PositiveFiniteDuration) | _unset_ | Optional time when we start logging a query as slow. |
|     `warn-on-slow-query-interval` | duration (PositiveFiniteDuration) | `5 seconds` | How often to repeat the logging statement for slow queries. |
|     **connection-allocation** |  |  |  |
|         `num-ledger-api` | int (PositiveInt) | _unset_ |  |
|         `num-reads` | int (PositiveInt) | _unset_ |  |
|         `num-writes` | int (PositiveInt) | _unset_ |  |
|     **partitions** |  |  |  |
|         `initial-bft-orderer-tables-partition-size` | int | `1500` | Initial partition size for bft-orderer tables. Note that this config is only read once, during the initial database setup and later changes to this value won't have any effect. This is also only used in Postgres setups. |

</Tab>
</Tabs>

### `canton.participants.<participant>.testing-time`

<Tabs>
<Tab title="HOCON">

```hocon
canton.participants.<participant>.testing-time {
  type = monotonic-time   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `type` | one of `monotonic-time` | **required** | Optional ledger api time service configuration for demo and testing only |

</Tab>
</Tabs>

### `canton.participants.<participant>.topology`

<Tabs>
<Tab title="HOCON">

```hocon
canton.participants.<participant>.topology {
  broadcast-batch-size = 100
  broadcast-retry-delay = "10 seconds"
  disable-optional-topology-checks = false
  dispatch-queue-backpressure-limit = 100
  enable-topology-state-cache-consistency-checks = false
  max-topology-state-cache-items = 10000
  topology-state-cache-eviction-threshold = 250
  topology-transaction-observation-timeout = "30 seconds"
  topology-transaction-registration-timeout = "20 seconds"
  use-time-proofs-to-observe-effective-time = false
  validate-initial-topology-snapshot = true
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `broadcast-batch-size` | int (PositiveInt) | `100` | The maximum number of topology transactions sent in a topology transaction broadcast |
| `broadcast-retry-delay` | duration (NonNegativeFiniteDuration) | `10 seconds` | The delay after which a failed dispatch cycle will be triggered again. |
| `disable-optional-topology-checks` | boolean | `false` | if true (default is false), don't run the optional checks which prevent accidental damage to this node |
| `dispatch-queue-backpressure-limit` | int (NonNegativeInt) | `100` | new topology requests will be backpressured if the number of existing requests exceeds this number |
| `enable-topology-state-cache-consistency-checks` | boolean | `false` | If true, the topology state cache runs additional consistency checks. This is costly and should not be enabled in production environments. |
| `max-topology-state-cache-items` | int (PositiveInt) | `10000` | The maximum number of (UIDs + namespaces, transaction types) that can be cached in the write through cache |
| `topology-state-cache-eviction-threshold` | int (PositiveInt) | `250` | How far should the topology cache be "emptied" during an eviction run |
| `topology-transaction-observation-timeout` | duration (NonNegativeFiniteDuration) | `30 seconds` | Determines up to how long the node waits for observing the dispatched topology transactions in its own local synchronizer store. The observation timeout is checked against the node's wall clock. After this timeout, the node fails the dispatch cycle. This timeout is only triggered, if the sequencer accepts the topology transaction broadcast submission request, but drops the message during ordering (for whatever reason). |
| `topology-transaction-registration-timeout` | duration (NonNegativeFiniteDuration) | `20 seconds` | Used to determine the max sequencing time for topology transaction broadcasts. |
| `use-time-proofs-to-observe-effective-time` | boolean | `false` | Whether the node will use time proofs to observe when an effective time has been reached. If false, no time proofs will be sent to the sequencers by any Canton node. |
| `validate-initial-topology-snapshot` | boolean | `true` | if false, the validation is skipped and the snapshot is directly imported. this is risky as it might create a fork if the validation was changed. therefore, we only use this with great care. the proper solution is to make validation so fast that it doesn't impact performance. |

</Tab>
</Tabs>

### `canton.participants.<participant>.traffic-accounting`

<Tabs>
<Tab title="HOCON">

```hocon
canton.participants.<participant>.traffic-accounting {
  allow-submissions-on-degradation = false
  enabled = false
  enforce-cost-on-submissions = false
  reject-multi-party-submissions = false
  traffic-enforcement-server {
    account-lookup-timeout = "20 seconds"
    database-query-timeout = "1 second"
    in-process-tea-server-name = "TeaGrpcInProcServer"
    projection {
      handler-retry-delay = "1 second"
      # initial-completion-offset-begin-exclusive = <int>
      # max-handler-retries = <int>
      max-projection-restart-backoff = "30 seconds"
      min-projection-restart-backoff = "1 second"
      # projection-max-restarts = <int>
      projection-restart-random-factor = 0.2
    }
    type = internal   # required
  }
}
```

`traffic-enforcement-server` supports these types: `internal`. Pick one; each block is a complete example of that choice.

```hocon
canton.participants.<participant>.traffic-accounting.traffic-enforcement-server {
  type = internal
  account-lookup-timeout = "20 seconds"
  database-query-timeout = "1 second"
  in-process-tea-server-name = "TeaGrpcInProcServer"
  projection {
    handler-retry-delay = "1 second"
    # initial-completion-offset-begin-exclusive = <int>
    # max-handler-retries = <int>
    max-projection-restart-backoff = "30 seconds"
    min-projection-restart-backoff = "1 second"
    # projection-max-restarts = <int>
    projection-restart-random-factor = 0.2
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `allow-submissions-on-degradation` | boolean | `false` | Whether to allow a submission to proceed without a balance check when the balance cannot be determined, for example due to a database outage. The submission is still charged, so an account that didn't have enough traffic balance might end up with a negative balance until it is topped up. Doesn't apply when the traffic service refuses the request. A bypassed check is logged at WARN. Disabled by default, so a failed lookup fails the submission. |
| `enabled` | boolean | `false` | Whether to enable the traffic enforcement feature. Note: This feature is experimental, unstable and is disabled by default. Enabling it in production environments is not recommended. Disabled by default |
| `enforce-cost-on-submissions` | boolean | `false` | Whether to enforce traffic cost on submissions. If enabled, the participant will validate that the account associated with the submission is correctly permissioned and has sufficient balance to cover the expected traffic cost. If the account has insufficient balance, the submission will be rejected. When disabled, the participant does not contact the traffic service on the submission path at all. Disabled by default. |
| `reject-multi-party-submissions` | boolean | `false` | Whether to reject submissions whose actAs has more than one party. TEA accounts are bound to a single party, so by default such submissions bypass traffic enforcement entirely (validation is skipped and an informational message is logged). When enabled, such submissions are rejected instead. Disabled by default. |
| **traffic-enforcement-server** |  |  |  |
|     `account-lookup-timeout` | duration (PositiveFiniteDuration) | `20 seconds` | Total per-call budget for the `GetAccount` RPC, covering the client's retries around `databaseQueryTimeout`. Must be strictly greater than `databaseQueryTimeout`, so a timed-out query still leaves room for a retry. |
|     `database-query-timeout` | duration (PositiveFiniteDuration) | `1 second` | Per-attempt deadline for the single database read backing `GetAccount`. Enforced on PostgreSQL via `SET LOCAL statement_timeout`, which is expressed in whole milliseconds, so this must be at least one millisecond. Has no effect on H2 (no millisecond-level query timeout setting). |
|     `in-process-tea-server-name` | string | `TeaGrpcInProcServer` | The name of the in-process gRPC serving the traffic service API of the traffic enforcement server |
|     `type` | one of `internal` | **required** | Choose one type — see [TrafficEnforcementServerConfig](/global-synchronizer/reference/canton-config/types#trafficenforcementserverconfig). |
|     **projection** |  |  |  |
|         `handler-retry-delay` | duration (PositiveFiniteDuration) | `1 second` | delay between restarts of retrying a failed event processing |
|         `initial-completion-offset-begin-exclusive` | int | _unset_ | the first ever time the TEA is started it will have no prior offset to start its completion stream from. By default it will start at ledgerEnd. Use this config to override the offset at which it will start. |
|         `max-handler-retries` | int (PositiveInt) | `PositiveInt.MaxValue` ‡ | max number of retries on projection handler failure |
|         `max-projection-restart-backoff` | duration (PositiveFiniteDuration) | `30 seconds` | maximum amount of time before between retries of the projection on failure |
|         `min-projection-restart-backoff` | duration (PositiveFiniteDuration) | `1 second` | minimum amount of time before retrying the projection on failure |
|         `projection-max-restarts` | int | `Int.MaxValue` ‡ |  |
|         `projection-restart-random-factor` | number | `0.2` |  |

</Tab>
</Tabs>
