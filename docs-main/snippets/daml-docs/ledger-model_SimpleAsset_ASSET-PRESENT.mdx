
```haskell
nonconsuming choice Present : SimpleAsset
  with
    actor : Party
    viewer : Party
  observer viewer
  controller actor
  do
    assert $ actor == issuer || actor == owner
    pure this
```
