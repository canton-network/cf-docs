---
title: "Overview"
description: "Object index for package com.digitalasset.canton.concurrent."
---

## Package `com.digitalasset.canton.concurrent`

## Table of Contents

| NAME | STATUS | SUMMARY |
| --- | --- | --- |
| [`DirectExecutionContext`](directexecutioncontext) | `stable` | A light-weight execution context that runs tasks on the thread... |
| [`ExecutionContextIdlenessExecutorService`](executioncontextidlenessexecutorservice) | `stable` | Waits until all threads in the executor service are idle. The... |
| [`ExecutionContextMonitor`](executioncontextmonitor) | `stable` | Debugging utility used to write at regular intervals the executor... |
| [`ExecutorServiceExtensions`](executorserviceextensions) | `stable` | Cleanly shuts down an executor service as best we can. |
| [`ForkJoinIdlenessExecutorService`](forkjoinidlenessexecutorservice) | `stable` | Waits until all threads in the executor service are idle. The... |
| [`FutureSupervisor`](futuresupervisor) | `stable` | Alert if a future does not complete within the prescribed duration |
| [`HasFutureSupervision`](hasfuturesupervision) | `stable` | Mixin-trait for classes that want to run some futures with... |
| [`IdlenessExecutorService`](idlenessexecutorservice) | `stable` | Waits until all threads in the executor service are idle. The... |
| [`SupervisedPromise`](supervisedpromise) | `stable` | Promise that will log a message after logAfter if it has not been... |
| [`ThreadPoolIdlenessExecutorService`](threadpoolidlenessexecutorservice) | `stable` | Waits until all threads in the executor service are idle. The... |
| [`Threading`](threading) | `stable` | Factories and utilities for dealing with threading. |
