{/* source: canton-network-utilities / canton-network-utilities/devnet/overview/registry-user-guide/onboarding-offboarding.rst */}

# Onboarding/Offboarding

The Registry Utility supports onboarding and offboarding for both Providers and Registrars.
Below is an overview of the onboarding and offboarding process for a Provider, managed by the
Operator. For Registrars, the process is similar, except Providers are responsible for onboarding
Registrars.

<a id="id1"></a>

## Provider Service Request/Accept

Onboarding starts when a user requests a `ProviderService` contract. The Operator may accept the
request, provided that the user’s credentials satisfy their onboarding Credentiaal requirements for
providers. Both parties become signatories to the contract.

![image](overview/registry-user-guide/pictures/user-service-request-accept.png)

## Cancel/Reject Request

A service request may be canceled by the user before acceptance, or rejected by the Operator during
review. In either case, no service contract is created.

![image](overview/registry-user-guide/pictures/user-service-cancel-accept.png)

## Termination

A `ProviderService` instance may be terminated at any time by either the Operator or the user.
Upon termination, the contract is archived, leaving credentials intact.

![image](overview/registry-user-guide/pictures/user-service-termination.png)
