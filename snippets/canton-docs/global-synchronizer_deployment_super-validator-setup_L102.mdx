```bash
kubectl create secret generic postgres-secrets \
  --from-literal=participantPassword="${PARTICIPANT_DB_PASSWORD}" \
  --from-literal=validatorPassword="${VALIDATOR_DB_PASSWORD}" \
  --from-literal=svPassword="${SV_DB_PASSWORD}" \
  --from-literal=scanPassword="${SCAN_DB_PASSWORD}" \
  -n sv
```
