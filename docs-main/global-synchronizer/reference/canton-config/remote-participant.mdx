---
title: "Remote participant"
description: "How the console reaches a participant running elsewhere."
---

{/* GENERATED_FROM source="digital-asset/canton" ref="624f9e9f5ff3e350acdc8f4c289ac50e1265821c" version="3.7.0-SNAPSHOT" path="docs-open/target/config-reference.json" */}

Configuration under `canton.remote-participants.<remote-participant>` for Canton 3.7.0-SNAPSHOT: 42 keys. Part of the [Canton configuration reference](/global-synchronizer/reference/canton-config/overview).

<Accordion title="How to read this page">

Canton is configured with [HOCON](https://github.com/lightbend/config/blob/master/HOCON.md). Scala field names appear as lowercase-with-dashes, so `maxInboundMessageSize` is written `max-inbound-message-size`. **The key set is closed**: an unrecognised key fails startup rather than being ignored, so a typo is caught immediately.

Paths are rooted at `canton`. Segments in angle brackets are names you choose:

- `<remote-participant>` — `user-chosen name of each entry under canton.remote-participants`

A `[]` suffix marks the fields of a list element. Keys marked **alpha** or **beta** are not covered by compatibility guarantees.

**Minimum Required Fields** is the smallest configuration that starts: only keys with no default inside sections that are not constructed by default, and when each applies: always, or once you have chosen a particular `type`. A key marked **required** elsewhere on the page is required only if you write the section that holds it. **All options** covers every key, one section per heading, where the heading is the section's absolute path. Each is shown two ways behind tabs: **HOCON** as you would write it from the root, every key with its default, and **Table**, the same keys relative to the heading with their descriptions. Where a section takes a `type`, the HOCON tab enumerates one complete block per type after the section block, and the table groups that type's keys under a `type = …` header; in the table a **bold** row names a section and the keys indented beneath it live inside it. In the Required skeleton a key needed only under a particular `type` is a comment carrying that condition.

Some sections take a `type` that decides which other keys they accept. **Configuration types** at the end of each page documents every such type the page uses, one block and one table per type, and the [types page](/global-synchronizer/reference/canton-config/types) collects them all.

In the Default column:

- a value is what the key holds when you do not set it;
- **required** means startup fails unless you set it;
- _unset_ means the feature is off, or the surrounding section is absent, until you set it;
- † marks a value assigned while the configuration loads (a port, for example). The number shown is an example from the environment the defaults were captured in, not a guaranteed default;
- ‡ marks a default written as an expression in the source that has not been evaluated; the expression is shown so you can find it;
- ◊ marks a field that declares one default but was seen holding another in a running configuration, because the section that builds it passes an explicit value. Both are shown.

</Accordion>

## Minimum Required Fields

<Tabs>
<Tab title="HOCON">

```hocon
canton.remote-participants.<remote-participant> {
  admin-api {
    port = 0   # required
  }
  ledger-api {
    port = 0   # required
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required when | Description |
|---|---|---|---|
| `admin-api.port` | int (Port) | always |  |
| `ledger-api.port` | int (Port) | always |  |


</Tab>
</Tabs>

## All options

### `canton.remote-participants.<remote-participant>`

<Tabs>
<Tab title="HOCON">

```hocon
canton.remote-participants.<remote-participant> {
  # token = <string>
}
```

`admin-api.tls.trust-collection-file` supports these types: `pem-file`, `pem-string`. Pick one; each block is a complete example of that choice.

```hocon
canton.remote-participants.<remote-participant>.admin-api.tls.trust-collection-file {
  type = pem-file
  pem-file = "/path/to/file"   # required
}
```

```hocon
canton.remote-participants.<remote-participant>.admin-api.tls.trust-collection-file {
  type = pem-string
  pem-bytes = "..."   # required
}
```

`ledger-api.tls.trust-collection-file` supports these types: `pem-file`, `pem-string`. Pick one; each block is a complete example of that choice.

```hocon
canton.remote-participants.<remote-participant>.ledger-api.tls.trust-collection-file {
  type = pem-file
  pem-file = "/path/to/file"   # required
}
```

```hocon
canton.remote-participants.<remote-participant>.ledger-api.tls.trust-collection-file {
  type = pem-string
  pem-bytes = "..."   # required
}
```

`admin-api.tls.client-cert.cert-chain-file` supports these types: `pem-file`, `pem-string`. Pick one; each block is a complete example of that choice.

```hocon
canton.remote-participants.<remote-participant>.admin-api.tls.client-cert.cert-chain-file {
  type = pem-file
  pem-file = "/path/to/file"   # required
}
```

```hocon
canton.remote-participants.<remote-participant>.admin-api.tls.client-cert.cert-chain-file {
  type = pem-string
  pem-bytes = "..."   # required
}
```

`ledger-api.tls.client-cert.cert-chain-file` supports these types: `pem-file`, `pem-string`. Pick one; each block is a complete example of that choice.

```hocon
canton.remote-participants.<remote-participant>.ledger-api.tls.client-cert.cert-chain-file {
  type = pem-file
  pem-file = "/path/to/file"   # required
}
```

```hocon
canton.remote-participants.<remote-participant>.ledger-api.tls.client-cert.cert-chain-file {
  type = pem-string
  pem-bytes = "..."   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `token` | string | _unset_ | optional bearer token to use on the ledger-api if jwt authorization is enabled |

</Tab>
</Tabs>

### `canton.remote-participants.<remote-participant>.admin-api`

<Tabs>
<Tab title="HOCON">

```hocon
canton.remote-participants.<remote-participant>.admin-api {
  address = "127.0.0.1"
  channel {
    # flow-control-window = <int>
    # initial-flow-control-window = <int>
    keep-alive-client {
      idle-timeout = "30 minutes"
      keep-alive-without-calls = false
      time = "40 seconds"
      timeout = "15 seconds"
    }
    max-inbound-message-size = 0   # required
    trace-context-propagation = enabled|disabled   # required
  }
  port = 0   # required
  tls {
    client-cert {
      cert-chain-file {
        type = pem-file|pem-string   # required
      }
      private-key-file = "..."   # required
    }
    enabled = true
    trust-collection-file {
      type = pem-file|pem-string   # required
    }
  }
}
```

`tls.trust-collection-file` supports these types: `pem-file`, `pem-string`. Pick one; each block is a complete example of that choice.

```hocon
canton.remote-participants.<remote-participant>.admin-api.tls.trust-collection-file {
  type = pem-file
  pem-file = "/path/to/file"   # required
}
```

```hocon
canton.remote-participants.<remote-participant>.admin-api.tls.trust-collection-file {
  type = pem-string
  pem-bytes = "..."   # required
}
```

`tls.client-cert.cert-chain-file` supports these types: `pem-file`, `pem-string`. Pick one; each block is a complete example of that choice.

```hocon
canton.remote-participants.<remote-participant>.admin-api.tls.client-cert.cert-chain-file {
  type = pem-file
  pem-file = "/path/to/file"   # required
}
```

```hocon
canton.remote-participants.<remote-participant>.admin-api.tls.client-cert.cert-chain-file {
  type = pem-string
  pem-bytes = "..."   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `address` | string | `127.0.0.1` |  |
| `port` | int (Port) | **required** |  |
| **channel** |  |  |  |
|     `flow-control-window` | int (PositiveInt) | _unset_ | Switches to manual gRPC flow control and sets its window; if `None`, then it is not configured and the implementation default is used. At most one of `flowControlWindow` and `initialFlowControlWindow` can be set. |
|     `initial-flow-control-window` | int (PositiveInt) | _unset_ | Switches to automatic gRPC flow control and sets its initial window; if `None`, then it is not configured and the implementation default is used. At most one of `flowControlWindow` and `initialFlowControlWindow` can be set. |
|     `max-inbound-message-size` | int (NonNegativeInt) | **required** |  |
|     `trace-context-propagation` | one of `enabled`, `disabled` | **required** |  |
|     **keep-alive-client** |  |  |  |
|         `idle-timeout` | duration (NonNegativeFiniteDuration) | `30 minutes` | If there are no RPCs during this duration, the channel will close all connections and switch to Idle state (default is 30 min) |
|         `keep-alive-without-calls` | boolean | `false` | Enables sending of keep alive even when there are no RPCs on the channel (default to false). This increases network resource consumption for idle connections and requires the server to enable permitKeepAliveWithoutCalls. Prefer using idleTimeout. |
|         `time` | duration (NonNegativeFiniteDuration) | `40 seconds` | Sets the time without read activity before sending a keepalive ping. Do not set to small numbers (default is 40s) |
|         `timeout` | duration (NonNegativeFiniteDuration) | `15 seconds` | Sets the time waiting for read activity after sending a keepalive ping (default is 15s). The default timeout was previously 20s and was lowered to 15s, because besides configuring the gRPC KeepAliveManager, it also sets up the socket's TCP_USER_TIMEOUT (see html). 15s gives a larger margin to detect a faulty connection earlier and retry a submission on another sequencer via amplification, thereby avoiding a request failure. |
| **tls** |  |  |  |
|     `enabled` | boolean | `true` | allows enabling TLS without `trustCollectionFile` or `clientCert` |
|     **client-cert** |  |  |  |
|         `private-key-file` | string (PemFile) | **required** |  |
|         **cert-chain-file** |  |  |  |
|             `type` | one of `pem-file`, `pem-string` | **required** | Choose one type — see [PemFileOrString](#pemfileorstring). A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
|         **cert-chain-file** `type = pem-file` |  |  |  |
|             `pem-file` | path (ExistingFile) | **required** |  |
|         **cert-chain-file** `type = pem-string` |  |  |  |
|             `pem-bytes` | string | **required** |  |
|     **trust-collection-file** |  |  |  |
|         `type` | one of `pem-file`, `pem-string` | **required** | Choose one type — see [PemFileOrString](#pemfileorstring). A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
|     **trust-collection-file** `type = pem-file` |  |  |  |
|         `pem-file` | path (ExistingFile) | **required** |  |
|     **trust-collection-file** `type = pem-string` |  |  |  |
|         `pem-bytes` | string | **required** |  |

</Tab>
</Tabs>

### `canton.remote-participants.<remote-participant>.http-health`

<Tabs>
<Tab title="HOCON">

```hocon
canton.remote-participants.<remote-participant>.http-health {
  address = "0.0.0.0"
  port = 0   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `address` | string | `0.0.0.0` |  |
| `port` | int (Port) | **required** |  |

</Tab>
</Tabs>

### `canton.remote-participants.<remote-participant>.ledger-api`

<Tabs>
<Tab title="HOCON">

```hocon
canton.remote-participants.<remote-participant>.ledger-api {
  address = "127.0.0.1"
  channel {
    # flow-control-window = <int>
    # initial-flow-control-window = <int>
    keep-alive-client {
      idle-timeout = "30 minutes"
      keep-alive-without-calls = false
      time = "40 seconds"
      timeout = "15 seconds"
    }
    max-inbound-message-size = 0   # required
    trace-context-propagation = enabled|disabled   # required
  }
  port = 0   # required
  tls {
    client-cert {
      cert-chain-file {
        type = pem-file|pem-string   # required
      }
      private-key-file = "..."   # required
    }
    enabled = true
    trust-collection-file {
      type = pem-file|pem-string   # required
    }
  }
}
```

`tls.trust-collection-file` supports these types: `pem-file`, `pem-string`. Pick one; each block is a complete example of that choice.

```hocon
canton.remote-participants.<remote-participant>.ledger-api.tls.trust-collection-file {
  type = pem-file
  pem-file = "/path/to/file"   # required
}
```

```hocon
canton.remote-participants.<remote-participant>.ledger-api.tls.trust-collection-file {
  type = pem-string
  pem-bytes = "..."   # required
}
```

`tls.client-cert.cert-chain-file` supports these types: `pem-file`, `pem-string`. Pick one; each block is a complete example of that choice.

```hocon
canton.remote-participants.<remote-participant>.ledger-api.tls.client-cert.cert-chain-file {
  type = pem-file
  pem-file = "/path/to/file"   # required
}
```

```hocon
canton.remote-participants.<remote-participant>.ledger-api.tls.client-cert.cert-chain-file {
  type = pem-string
  pem-bytes = "..."   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `address` | string | `127.0.0.1` |  |
| `port` | int (Port) | **required** |  |
| **channel** |  |  |  |
|     `flow-control-window` | int (PositiveInt) | _unset_ | Switches to manual gRPC flow control and sets its window; if `None`, then it is not configured and the implementation default is used. At most one of `flowControlWindow` and `initialFlowControlWindow` can be set. |
|     `initial-flow-control-window` | int (PositiveInt) | _unset_ | Switches to automatic gRPC flow control and sets its initial window; if `None`, then it is not configured and the implementation default is used. At most one of `flowControlWindow` and `initialFlowControlWindow` can be set. |
|     `max-inbound-message-size` | int (NonNegativeInt) | **required** |  |
|     `trace-context-propagation` | one of `enabled`, `disabled` | **required** |  |
|     **keep-alive-client** |  |  |  |
|         `idle-timeout` | duration (NonNegativeFiniteDuration) | `30 minutes` | If there are no RPCs during this duration, the channel will close all connections and switch to Idle state (default is 30 min) |
|         `keep-alive-without-calls` | boolean | `false` | Enables sending of keep alive even when there are no RPCs on the channel (default to false). This increases network resource consumption for idle connections and requires the server to enable permitKeepAliveWithoutCalls. Prefer using idleTimeout. |
|         `time` | duration (NonNegativeFiniteDuration) | `40 seconds` | Sets the time without read activity before sending a keepalive ping. Do not set to small numbers (default is 40s) |
|         `timeout` | duration (NonNegativeFiniteDuration) | `15 seconds` | Sets the time waiting for read activity after sending a keepalive ping (default is 15s). The default timeout was previously 20s and was lowered to 15s, because besides configuring the gRPC KeepAliveManager, it also sets up the socket's TCP_USER_TIMEOUT (see html). 15s gives a larger margin to detect a faulty connection earlier and retry a submission on another sequencer via amplification, thereby avoiding a request failure. |
| **tls** |  |  |  |
|     `enabled` | boolean | `true` | allows enabling TLS without `trustCollectionFile` or `clientCert` |
|     **client-cert** |  |  |  |
|         `private-key-file` | string (PemFile) | **required** |  |
|         **cert-chain-file** |  |  |  |
|             `type` | one of `pem-file`, `pem-string` | **required** | Choose one type — see [PemFileOrString](#pemfileorstring). A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
|         **cert-chain-file** `type = pem-file` |  |  |  |
|             `pem-file` | path (ExistingFile) | **required** |  |
|         **cert-chain-file** `type = pem-string` |  |  |  |
|             `pem-bytes` | string | **required** |  |
|     **trust-collection-file** |  |  |  |
|         `type` | one of `pem-file`, `pem-string` | **required** | Choose one type — see [PemFileOrString](#pemfileorstring). A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
|     **trust-collection-file** `type = pem-file` |  |  |  |
|         `pem-file` | path (ExistingFile) | **required** |  |
|     **trust-collection-file** `type = pem-string` |  |  |  |
|         `pem-bytes` | string | **required** |  |

</Tab>
</Tabs>

### `canton.remote-participants.<remote-participant>.ledger-json-api`

<Tabs>
<Tab title="HOCON">

```hocon
canton.remote-participants.<remote-participant>.ledger-json-api {
  address = "127.0.0.1"
  # path-prefix = <string>
  port = 0   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `address` | string | `127.0.0.1` |  |
| `path-prefix` | string | _unset_ |  |
| `port` | int (Port) | **required** |  |

</Tab>
</Tabs>

## Configuration types

Sections under this page that take a `type`, and the keys each type accepts. A key belonging to another type fails startup.

### PemFileOrString

Set `type` to one of `pem-file`, `pem-string`.

Occurs at:

- `canton.remote-participants.<remote-participant>.admin-api.tls.client-cert.cert-chain-file`
- `canton.remote-participants.<remote-participant>.admin-api.tls.trust-collection-file`
- `canton.remote-participants.<remote-participant>.ledger-api.tls.client-cert.cert-chain-file`
- `canton.remote-participants.<remote-participant>.ledger-api.tls.trust-collection-file`

#### `type = pem-file`

<Tabs>
<Tab title="HOCON">

```hocon
canton.remote-participants.<remote-participant>.admin-api.tls.trust-collection-file {
  type = pem-file
  pem-file = "/path/to/file"   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `pem-file` | path (ExistingFile) | **required** |  |

</Tab>
</Tabs>

#### `type = pem-string`

<Tabs>
<Tab title="HOCON">

```hocon
canton.remote-participants.<remote-participant>.admin-api.tls.trust-collection-file {
  type = pem-string
  pem-bytes = "..."   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Default | Description |
|---|---|---|---|
| `pem-bytes` | string | **required** |  |

</Tab>
</Tabs>
