```bash
# Stop the current deployment
./stop.sh

# Update the bundle and image tag
export IMAGE_TAG=<NEW_VERSION>

# Restart
./start.sh -s "<SPONSOR_SV_URL>" -p "<PARTY_HINT>" -m "<MIGRATION_ID>" -w
```
