---
title: "Configuration types"
description: "Sections of the Canton configuration that take a type, and the keys each type accepts."
---

{/* GENERATED_FROM source="digital-asset/canton" ref="1be6d4933767495c3ba950e8513086ca10188757" version="3.7.0-SNAPSHOT" path="docs-open/target/config-reference.json" */}

Some sections are a choice between shapes. You pick one by setting a `type` key, and that choice decides which other keys the section accepts; a key belonging to another type fails startup. Every page documents the types it uses; this page collects all of them. Independent choices nest, so a type can contain further `type` keys; those link onward.

## AggregationType

Set `type` to one of `buckets`, `exponential`.

Occurs at:

- `canton.monitoring.metrics.histograms[].aggregation`

### `type = buckets`

<Tabs>
<Tab title="HOCON">

```hocon
canton.monitoring.metrics.histograms = [
  {
    aggregation {
      type = buckets
      boundaries = [ ]   # required
    }
  }
]
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `boundaries` | array of Double | yes |  |  |

</Tab>
</Tabs>

### `type = exponential`

<Tabs>
<Tab title="HOCON">

```hocon
canton.monitoring.metrics.histograms = [
  {
    aggregation {
      type = exponential
      max-buckets = 0   # required
      max-scale = 0   # required
    }
  }
]
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `max-buckets` | int | yes |  |  |
| `max-scale` | int | yes |  |  |

</Tab>
</Tabs>

## AuthServiceConfig

Set `type` to one of `wildcard`, `unsafe-jwt-hmac-256`, `jwt-rs-256-crt`, `jwt-es-256-crt`, `jwt-es-512-crt`, `jwt-jwks`, `party-jwt`.

Occurs at:

- `canton.mediators.<mediator>.admin-api.auth-services[]`
- `canton.participants.<participant>.admin-api.auth-services[]`
- `canton.participants.<participant>.ledger-api.auth-services[]`
- `canton.sequencers.<sequencer>.admin-api.auth-services[]`

### `type = wildcard`

```hocon
canton.mediators.<mediator>.admin-api.auth-services = [
  {
    type = wildcard
    # takes no further keys
  }
]
```

### `type = unsafe-jwt-hmac-256`

[UNSAFE] Enables JWT-based authorization with shared secret HMAC256 signing: USE THIS EXCLUSIVELY FOR TESTING

<Tabs>
<Tab title="HOCON">

```hocon
canton.mediators.<mediator>.admin-api.auth-services = [
  {
    type = unsafe-jwt-hmac-256
    access-level = wildcard
    # max-token-life = <duration>
    privileged = false
    secret = "..."   # required
    # target-audience = <string>
    # target-scope = <string>
    users = [
      {
        allowed-services = [ ]   # required
        user-id = "..."   # required
      }
    ]
  }
]
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `access-level` | one of `admin`, `wildcard` |  | `wildcard` |  |
| `max-token-life` | duration (NonNegativeDuration) |  | `NonNegativeDuration(Duration.Inf)` ‡ |  |
| `privileged` | boolean |  | `false` |  |
| `secret` | string (NonEmptyString) | yes |  |  |
| `target-audience` | string |  | _unset_ |  |
| `target-scope` | string |  | _unset_ |  |
| `users` | array of AuthorizedUser |  | `[]` |  |
| `users[].allowed-services` | array of string | yes |  |  |
| `users[].user-id` | string | yes |  |  |

</Tab>
</Tabs>

### `type = jwt-rs-256-crt`

Enables JWT-based authorization, where the JWT is signed by RSA256 with a public key loaded from the given X509 certificate file (.crt)

<Tabs>
<Tab title="HOCON">

```hocon
canton.mediators.<mediator>.admin-api.auth-services = [
  {
    type = jwt-rs-256-crt
    access-level = wildcard
    certificate = "..."   # required
    # max-token-life = <duration>
    privileged = false
    # target-audience = <string>
    # target-scope = <string>
    users = [
      {
        allowed-services = [ ]   # required
        user-id = "..."   # required
      }
    ]
  }
]
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `access-level` | one of `admin`, `wildcard` |  | `wildcard` |  |
| `certificate` | string | yes |  |  |
| `max-token-life` | duration (NonNegativeDuration) |  | `NonNegativeDuration(Duration.Inf)` ‡ |  |
| `privileged` | boolean |  | `false` |  |
| `target-audience` | string |  | _unset_ |  |
| `target-scope` | string |  | _unset_ |  |
| `users` | array of AuthorizedUser |  | `[]` |  |
| `users[].allowed-services` | array of string | yes |  |  |
| `users[].user-id` | string | yes |  |  |

</Tab>
</Tabs>

### `type = jwt-es-256-crt`

"Enables JWT-based authorization, where the JWT is signed by ECDSA256 with a public key loaded from the given X509 certificate file (.crt)"

<Tabs>
<Tab title="HOCON">

```hocon
canton.mediators.<mediator>.admin-api.auth-services = [
  {
    type = jwt-es-256-crt
    access-level = wildcard
    certificate = "..."   # required
    # max-token-life = <duration>
    privileged = false
    # target-audience = <string>
    # target-scope = <string>
    users = [
      {
        allowed-services = [ ]   # required
        user-id = "..."   # required
      }
    ]
  }
]
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `access-level` | one of `admin`, `wildcard` |  | `wildcard` |  |
| `certificate` | string | yes |  |  |
| `max-token-life` | duration (NonNegativeDuration) |  | `NonNegativeDuration(Duration.Inf)` ‡ |  |
| `privileged` | boolean |  | `false` |  |
| `target-audience` | string |  | _unset_ |  |
| `target-scope` | string |  | _unset_ |  |
| `users` | array of AuthorizedUser |  | `[]` |  |
| `users[].allowed-services` | array of string | yes |  |  |
| `users[].user-id` | string | yes |  |  |

</Tab>
</Tabs>

### `type = jwt-es-512-crt`

Enables JWT-based authorization, where the JWT is signed by ECDSA512 with a public key loaded from the given X509 certificate file (.crt)

<Tabs>
<Tab title="HOCON">

```hocon
canton.mediators.<mediator>.admin-api.auth-services = [
  {
    type = jwt-es-512-crt
    access-level = wildcard
    certificate = "..."   # required
    # max-token-life = <duration>
    privileged = false
    # target-audience = <string>
    # target-scope = <string>
    users = [
      {
        allowed-services = [ ]   # required
        user-id = "..."   # required
      }
    ]
  }
]
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `access-level` | one of `admin`, `wildcard` |  | `wildcard` |  |
| `certificate` | string | yes |  |  |
| `max-token-life` | duration (NonNegativeDuration) |  | `NonNegativeDuration(Duration.Inf)` ‡ |  |
| `privileged` | boolean |  | `false` |  |
| `target-audience` | string |  | _unset_ |  |
| `target-scope` | string |  | _unset_ |  |
| `users` | array of AuthorizedUser |  | `[]` |  |
| `users[].allowed-services` | array of string | yes |  |  |
| `users[].user-id` | string | yes |  |  |

</Tab>
</Tabs>

### `type = jwt-jwks`

Enables JWT-based authorization, where the JWT is signed by RSA256 with a public key loaded from the given JWKS URL

<Tabs>
<Tab title="HOCON">

```hocon
canton.mediators.<mediator>.admin-api.auth-services = [
  {
    type = jwt-jwks
    access-level = wildcard
    # max-token-life = <duration>
    privileged = false
    # target-audience = <string>
    # target-scope = <string>
    url = "..."   # required
    users = [
      {
        allowed-services = [ ]   # required
        user-id = "..."   # required
      }
    ]
  }
]
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `access-level` | one of `admin`, `wildcard` |  | `wildcard` |  |
| `max-token-life` | duration (NonNegativeDuration) |  | `NonNegativeDuration(Duration.Inf)` ‡ |  |
| `privileged` | boolean |  | `false` |  |
| `target-audience` | string |  | _unset_ |  |
| `target-scope` | string |  | _unset_ |  |
| `url` | string (NonEmptyString) | yes |  |  |
| `users` | array of AuthorizedUser |  | `[]` |  |
| `users[].allowed-services` | array of string | yes |  |  |
| `users[].user-id` | string | yes |  |  |

</Tab>
</Tabs>

### `type = party-jwt`

Enables Party JWT-based authorization, where the JWT is signed by the signing key from the corresponding party.

```hocon
canton.mediators.<mediator>.admin-api.auth-services = [
  {
    type = party-jwt
    # takes no further keys
  }
]
```

## BatchAggregatorConfig

Parameters for that batcher that batches queries (e.g., to a DB).

Set `type` to one of `batching`, `no-batching`.

Occurs at:

- `canton.mediators.<mediator>.mediator.deduplication-store.persist-batching`
- `canton.mediators.<mediator>.parameters.batching.aggregator`
- `canton.mediators.<mediator>.parameters.batching.contract-store-aggregator`
- `canton.mediators.<mediator>.parameters.batching.mediator-fetch-finalized-responses-aggregator`
- `canton.mediators.<mediator>.parameters.batching.mediator-store-finalized-responses-aggregator`
- `canton.mediators.<mediator>.parameters.batching.topology-cache-aggregator`
- `canton.participants.<participant>.parameters.acs-commitments.load-batching`
- `canton.participants.<participant>.parameters.batching.aggregator`
- … and 11 more

### `type = batching`

<Tabs>
<Tab title="HOCON">

```hocon
canton.sequencers.<sequencer>.traffic-config.batch-aggregator-config {
  type = batching
  maximum-batch-size = 500
  maximum-in-flight = 2
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `maximum-batch-size` | int (PositiveNumeric) |  | `500` | Maximum number of queries in a batch. |
| `maximum-in-flight` | int (PositiveNumeric) |  | `2` | Maximum number of in-flight get queries. |

</Tab>
</Tabs>

### `type = no-batching`

<Tabs>
<Tab title="HOCON">

```hocon
canton.sequencers.<sequencer>.traffic-config.batch-aggregator-config {
  type = no-batching
  max-parallel-batches = 2
  maximum-batch-size = 500
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `max-parallel-batches` | int (PositiveNumeric) |  | `2` | Maximum number of batches to execute in parallel when using runMany. |
| `maximum-batch-size` | int (PositiveNumeric) |  | `500` | Maximum number of queries in a batch. |

</Tab>
</Tabs>

## ClockConfig

Set `type` to one of `sim-clock`, `wall-clock`, `remote-clock`.

Occurs at:

- `canton.parameters.clock`

### `type = sim-clock`

```hocon
canton.parameters.clock {
  type = sim-clock
  # takes no further keys
}
```

### `type = wall-clock`

Configure Canton to use the wall clock (default)

<Tabs>
<Tab title="HOCON">

```hocon
canton.parameters.clock {
  type = wall-clock
  skews {
    <skew> = [ ]
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `skews.<skew>` | duration |  | `[]` | map of clock skews, indexed by node name (used for testing, default empty) Any node whose name is contained in the map will use a WallClock, but the time of the clock will by shifted by the associated duration, which can be positive or negative. The clock shift will be constant during the life of the node. |

</Tab>
</Tabs>

### `type = remote-clock`

Configure Canton to use a remote clock In crash recovery testing scenarios, we want several processes to use the same time. In most cases, we can rely on NTP and the host clock. However, in cases where we test static time, we need the spawn processes to access the main processes clock. For such cases we can use a remote clock. However, no user should ever require this.

<Tabs>
<Tab title="HOCON">

```hocon
canton.parameters.clock {
  type = remote-clock
  remote-api {
    address = "127.0.0.1"
    channel {
      # flow-control-window = <int>
      # initial-flow-control-window = <int>
      keep-alive-client {
        idle-timeout = "30 minutes"
        keep-alive-without-calls = false
        time = "40 seconds"
        timeout = "15 seconds"
      }
      max-inbound-message-size = 0   # required
      trace-context-propagation = enabled|disabled   # required
    }
    port = 0   # required
    tls {
      client-cert {
        cert-chain-file {
          type = pem-file|pem-string   # required
        }
        private-key-file = "..."   # required
      }
      enabled = true
      trust-collection-file {
        type = pem-file|pem-string   # required
      }
    }
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `remote-api.address` | string |  | `127.0.0.1` |  |
| `remote-api.channel.flow-control-window` | int (PositiveInt) |  | _unset_ | Switches to manual gRPC flow control and sets its window; if `None`, then it is not configured and the implementation default is used. At most one of `flowControlWindow` and `initialFlowControlWindow` can be set. |
| `remote-api.channel.initial-flow-control-window` | int (PositiveInt) |  | _unset_ | Switches to automatic gRPC flow control and sets its initial window; if `None`, then it is not configured and the implementation default is used. At most one of `flowControlWindow` and `initialFlowControlWindow` can be set. |
| `remote-api.channel.keep-alive-client.idle-timeout` | duration (NonNegativeFiniteDuration) |  | `30 minutes` | If there are no RPCs during this duration, the channel will close all connections and switch to Idle state (default is 30 min) |
| `remote-api.channel.keep-alive-client.keep-alive-without-calls` | boolean |  | `false` | Enables sending of keep alive even when there are no RPCs on the channel (default to false). This increases network resource consumption for idle connections and requires the server to enable permitKeepAliveWithoutCalls. Prefer using idleTimeout. |
| `remote-api.channel.keep-alive-client.time` | duration (NonNegativeFiniteDuration) |  | `40 seconds` | Sets the time without read activity before sending a keepalive ping. Do not set to small numbers (default is 40s) |
| `remote-api.channel.keep-alive-client.timeout` | duration (NonNegativeFiniteDuration) |  | `15 seconds` | Sets the time waiting for read activity after sending a keepalive ping (default is 15s). The default timeout was previously 20s and was lowered to 15s, because besides configuring the gRPC KeepAliveManager, it also sets up the socket's TCP_USER_TIMEOUT (see html). 15s gives a larger margin to detect a faulty connection earlier and retry a submission on another sequencer via amplification, thereby avoiding a request failure. |
| `remote-api.channel.max-inbound-message-size` | int (NonNegativeInt) | yes |  |  |
| `remote-api.channel.trace-context-propagation` | one of `enabled`, `disabled` | yes |  |  |
| `remote-api.port` | int (Port) | yes |  |  |
| `remote-api.tls.client-cert.cert-chain-file.type` | one of `pem-file`, `pem-string` | yes |  | A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
| `remote-api.tls.client-cert.private-key-file` | string (PemFile) | yes |  |  |
| `remote-api.tls.enabled` | boolean |  | `true` | allows enabling TLS without `trustCollectionFile` or `clientCert` |
| `remote-api.tls.trust-collection-file.type` | one of `pem-file`, `pem-string` | yes |  | A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |

</Tab>
</Tabs>

Further types inside this one: [PemFileOrString](#pemfileorstring) at `remote-api.tls.client-cert.cert-chain-file.type`, [PemFileOrString](#pemfileorstring) at `remote-api.tls.trust-collection-file.type`.

## EncryptedPrivateStoreConfig

Encapsulates possible configurations for different encrypted private stores

Set `type` to one of `kms`.

Occurs at:

- `canton.mediators.<mediator>.crypto.private-key-store.encryption`
- `canton.participants.<participant>.crypto.private-key-store.encryption`
- `canton.sequencers.<sequencer>.crypto.private-key-store.encryption`

### `type = kms`

Configuration for a KMS encrypted private store

<Tabs>
<Tab title="HOCON">

```hocon
canton.mediators.<mediator>.crypto.private-key-store.encryption {
  type = kms
  reverted = false
  # wrapper-key-id = <string>
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `reverted` | boolean |  | `false` | when true decrypts the stored private keys and stores them in clear, disabling the encrypted crypto private key store in the process |
| `wrapper-key-id` | string (KmsKeyId) |  | _unset_ | defines key identifier for the wrapper key (e.g. ARN for AWS SDK). When it's None Canton will either create a new key or use a previous existent key |

</Tab>
</Tabs>

## Exporter

Configuration for how to export spans

Set `type` to one of `disabled`, `zipkin`, `otlp`.

Occurs at:

- `canton.monitoring.tracing.tracer.exporter`

### `type = disabled`

```hocon
canton.monitoring.tracing.tracer.exporter {
  type = disabled
  # takes no further keys
}
```

### `type = zipkin`

Zipkin exporter is deprecated from opentelemetry, with removal planned for mid-2026. See https://opentelemetry.io/blog/2025/deprecating-zipkin-exporters/ for more details.

<Tabs>
<Tab title="HOCON">

```hocon
canton.monitoring.tracing.tracer.exporter {
  type = zipkin
  address = "localhost"
  port = 9411
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `address` | string |  | `localhost` |  |
| `port` | int |  | `9411` |  |

</Tab>
</Tabs>

### `type = otlp`

<Tabs>
<Tab title="HOCON">

```hocon
canton.monitoring.tracing.tracer.exporter {
  type = otlp
  additional-headers {
    <additional-header> = [ ]
  }
  address = "localhost"
  # connect-timeout = <duration>
  port = 4317
  # timeout = <duration>
  # trust-collection-path = <string>
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `additional-headers.<additional-header>` | string |  | `[]` |  |
| `address` | string |  | `localhost` |  |
| `connect-timeout` | duration |  | _unset_ |  |
| `port` | int |  | `4317` |  |
| `timeout` | duration |  | _unset_ |  |
| `trust-collection-path` | string |  | _unset_ |  |

</Tab>
</Tabs>

## ExtensionServiceAuthConfig

Authentication configuration for outbound calls to an extension service.

Set `type` to one of `none`, `bearer-token-file`.

Occurs at:

- `canton.participants.<participant>.parameters.engine.extensions.<extension>.auth`

### `type = none`

```hocon
canton.participants.<participant>.parameters.engine.extensions.<extension>.auth {
  type = none
  # takes no further keys
}
```

### `type = bearer-token-file`

Send the configured file contents as an HTTP bearer token.

<Tabs>
<Tab title="HOCON">

```hocon
canton.participants.<participant>.parameters.engine.extensions.<extension>.auth {
  type = bearer-token-file
  token-file = "/path/to/file"   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `token-file` | path (ExistingFile) | yes |  |  |

</Tab>
</Tabs>

## FiniteDurationDistribution

Set `type` to one of `constant-distribution`, `weighted-distribution`, `linear-distribution`, `power-distribution`.

Occurs at:

- `canton.sequencers.<sequencer>.sequencer.config.standalone.test-slowdown.post-ordering-delay`
- `canton.sequencers.<sequencer>.sequencer.config.standalone.test-slowdown.send-delay.default-delay-distribution`
- `canton.sequencers.<sequencer>.sequencer.config.standalone.test-slowdown.send-delay.delays-by-recipients[].delay-distribution`
- `canton.sequencers.<sequencer>.sequencer.config.standalone.test-slowdown.topology-delay.get-ordering-topology-delay`
- `canton.sequencers.<sequencer>.sequencer.config.standalone.test-slowdown.topology-delay.request-inspection-delay`

### `type = constant-distribution`

<Tabs>
<Tab title="HOCON">

```hocon
canton.sequencers.<sequencer>.sequencer.config.standalone.test-slowdown.post-ordering-delay {
  type = constant-distribution
  duration = "30 seconds"   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `duration` | duration | yes |  |  |

</Tab>
</Tabs>

### `type = weighted-distribution`

<Tabs>
<Tab title="HOCON">

```hocon
canton.sequencers.<sequencer>.sequencer.config.standalone.test-slowdown.post-ordering-delay {
  type = weighted-distribution
  weighted-durations = [
    {
      duration = "30 seconds"   # required
      weight = 0   # required
    }
  ]
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `weighted-durations` | array of WeightedDuration | yes |  |  |
| `weighted-durations[].duration` | duration | yes |  |  |
| `weighted-durations[].weight` | int (PositiveInt) | yes |  |  |

</Tab>
</Tabs>

### `type = linear-distribution`

<Tabs>
<Tab title="HOCON">

```hocon
canton.sequencers.<sequencer>.sequencer.config.standalone.test-slowdown.post-ordering-delay {
  type = linear-distribution
  high = "30 seconds"   # required
  low = "30 seconds"   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `high` | duration | yes |  |  |
| `low` | duration | yes |  |  |

</Tab>
</Tabs>

### `type = power-distribution`

<Tabs>
<Tab title="HOCON">

```hocon
canton.sequencers.<sequencer>.sequencer.config.standalone.test-slowdown.post-ordering-delay {
  type = power-distribution
  low = "30 seconds"   # required
  mean = "30 seconds"   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `low` | duration | yes |  |  |
| `mean` | duration | yes |  |  |

</Tab>
</Tabs>

## HowLongToBlacklist

Set `type` to one of `linear`, `linear-with-parameters`, `exponential`, `no-blacklisting`.

Occurs at:

- `canton.sequencers.<sequencer>.sequencer.config.leader-selection-policy-config-for-pv-34.how-long-to-blacklist`
- `canton.sequencers.<sequencer>.sequencer.config.standalone.blacklist-leader-selection-policy-config.how-long-to-blacklist`

### `type = linear`

<Tabs>
<Tab title="HOCON">

```hocon
canton.sequencers.<sequencer>.sequencer.config.leader-selection-policy-config-for-pv-34.how-long-to-blacklist {
  type = linear
  # maximum-epoch-blacklisted = <int>
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `maximum-epoch-blacklisted` | int |  | _unset_ |  |

</Tab>
</Tabs>

### `type = linear-with-parameters`

<Tabs>
<Tab title="HOCON">

```hocon
canton.sequencers.<sequencer>.sequencer.config.leader-selection-policy-config-for-pv-34.how-long-to-blacklist {
  type = linear-with-parameters
  initial-value = 0   # required
  # maximum-epoch-blacklisted = <int>
  slope = 0   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `initial-value` | int | yes |  |  |
| `maximum-epoch-blacklisted` | int |  | _unset_ |  |
| `slope` | int | yes |  |  |

</Tab>
</Tabs>

### `type = exponential`

<Tabs>
<Tab title="HOCON">

```hocon
canton.sequencers.<sequencer>.sequencer.config.leader-selection-policy-config-for-pv-34.how-long-to-blacklist {
  type = exponential
  initial-value = 0   # required
  # maximum-epoch-blacklisted = <int>
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `initial-value` | int | yes |  |  |
| `maximum-epoch-blacklisted` | int |  | _unset_ |  |

</Tab>
</Tabs>

### `type = no-blacklisting`

```hocon
canton.sequencers.<sequencer>.sequencer.config.leader-selection-policy-config-for-pv-34.how-long-to-blacklist {
  type = no-blacklisting
  # takes no further keys
}
```

## IdentityConfig

Control how the identity of the node is determined. At startup, we need to determine the node's identity. We support various modes of operations for different deployments. We distinguish between setting the node-id and subsequently generating the topology transactions and the remaining keys. As such it is possible to manually set the node-id but let the system automatically generate the remaining topology transactions.

Set `type` to one of `external`, `auto`, `manual`.

Occurs at:

- `canton.mediators.<mediator>.init.identity`
- `canton.participants.<participant>.init.identity`
- `canton.sequencers.<sequencer>.init.identity`

### `type = external`

Read the node identifier from the config file

<Tabs>
<Tab title="HOCON">

```hocon
canton.mediators.<mediator>.init.identity {
  type = external
  certificates = [ ]
  identifier = "..."   # required
  # namespace = <string>
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `certificates` | array of File |  | `[]` | optional certificates to use (in case we have external root keys) |
| `identifier` | string | yes |  | the string to use as identifier of the node |
| `namespace` | string |  | _unset_ | optional fingerprint to use as the namespace (default is to extract it from the first certificate) |

</Tab>
</Tabs>

### `type = auto`

Automatically generate a namespace key and initialize the node id

<Tabs>
<Tab title="HOCON">

```hocon
canton.mediators.<mediator>.init.identity {
  type = auto
  identifier {
    type = random|explicit|config   # required
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `identifier.type` | one of `random`, `explicit`, `config` | yes | `config` † |  |

</Tab>
</Tabs>

Further types inside this one: [NodeIdentifierConfig](#nodeidentifierconfig) at `identifier.type`.

### `type = manual`

```hocon
canton.mediators.<mediator>.init.identity {
  type = manual
  # takes no further keys
}
```

## KmsConfig

Set `type` to one of `driver`, `aws`, `gcp`.

Occurs at:

- `canton.mediators.<mediator>.crypto.kms`
- `canton.participants.<participant>.crypto.kms`
- `canton.sequencers.<sequencer>.crypto.kms`

### `type = driver`

A KMS configuration for an external KMS driver.

<Tabs>
<Tab title="HOCON">

```hocon
canton.mediators.<mediator>.crypto.kms {
  type = driver
  config = { }   # required
  health-check-period = "10 seconds"
  name = "..."   # required
  retries {
    create-key-check {
      initial-delay = "30 seconds"   # required
      max-delay = "30 seconds"   # required
      max-retries = 0   # required
    }
    failures {
      initial-delay = "30 seconds"   # required
      max-delay = "30 seconds"   # required
      max-retries = 0   # required
    }
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `config` | config-value | yes |  | The driver specific raw config section |
| `health-check-period` | duration (PositiveFiniteDuration) |  | `10 seconds` | How long to wait between health checks of the KMS driver |
| `name` | string | yes |  | The name of the external KMS driver |
| `retries.create-key-check.initial-delay` | duration (NonNegativeFiniteDuration) | yes |  | initial delay before the first retry |
| `retries.create-key-check.max-delay` | duration (NonNegativeDuration) | yes |  | max delay between retries |
| `retries.create-key-check.max-retries` | int | yes |  | max number of retries |
| `retries.failures.initial-delay` | duration (NonNegativeFiniteDuration) | yes |  | initial delay before the first retry |
| `retries.failures.max-delay` | duration (NonNegativeDuration) | yes |  | max delay between retries |
| `retries.failures.max-retries` | int | yes |  | max number of retries |

</Tab>
</Tabs>

### `type = aws`

Stores the configuration for AWS KMS. This configuration is mandatory if we want to protect Canton's private keys using an AWS KMS.

<Tabs>
<Tab title="HOCON">

```hocon
canton.mediators.<mediator>.crypto.kms {
  type = aws
  audit-logging = false
  custom-tags {
    <custom-tag> = [ ]
  }
  disable-ssl-verification = false
  # endpoint-override = <string>
  multi-region-key = false
  region = "..."   # required
  retries {
    create-key-check {
      initial-delay = "30 seconds"   # required
      max-delay = "30 seconds"   # required
      max-retries = 0   # required
    }
    failures {
      initial-delay = "30 seconds"   # required
      max-delay = "30 seconds"   # required
      max-retries = 0   # required
    }
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `audit-logging` | boolean |  | `false` | when enabled, all calls to KMS will be logged. Defaults to false. |
| `custom-tags.<custom-tag>` | string |  | `[]` | custom tags to attach to AWS KMS keys created by Canton. |
| `disable-ssl-verification` | boolean |  | `false` | When set to true, SSL verification is disabled. Mostly for testing purposes. Can only be used if non-standard-config is enabled. |
| `endpoint-override` | string |  | _unset_ | the [optional] endpoint for a proxy to be used by the KMS client. |
| `multi-region-key` | boolean |  | `false` | flag to enable multiRegion keys (Canton will generate single region keys by default) |
| `region` | string | yes |  | defines the AWS region to be use (e.g. us-east-1) |
| `retries.create-key-check.initial-delay` | duration (NonNegativeFiniteDuration) | yes |  | initial delay before the first retry |
| `retries.create-key-check.max-delay` | duration (NonNegativeDuration) | yes |  | max delay between retries |
| `retries.create-key-check.max-retries` | int | yes |  | max number of retries |
| `retries.failures.initial-delay` | duration (NonNegativeFiniteDuration) | yes |  | initial delay before the first retry |
| `retries.failures.max-delay` | duration (NonNegativeDuration) | yes |  | max delay between retries |
| `retries.failures.max-retries` | int | yes |  | max number of retries |

</Tab>
</Tabs>

### `type = gcp`

Stores the configuration for GCP KMS. This configuration is mandatory if we want to protect Canton's private keys using a GCP KMS.

<Tabs>
<Tab title="HOCON">

```hocon
canton.mediators.<mediator>.crypto.kms {
  type = gcp
  audit-logging = false
  # endpoint-override = <string>
  key-ring-id = "..."   # required
  location-id = "..."   # required
  project-id = "..."   # required
  retries {
    create-key-check {
      initial-delay = "30 seconds"   # required
      max-delay = "30 seconds"   # required
      max-retries = 0   # required
    }
    failures {
      initial-delay = "30 seconds"   # required
      max-delay = "30 seconds"   # required
      max-retries = 0   # required
    }
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `audit-logging` | boolean |  | `false` | when enabled, all calls to KMS will be logged. Defaults to false. |
| `endpoint-override` | string |  | _unset_ | the [optional] endpoint for a proxy to be used by the KMS client. |
| `key-ring-id` | string | yes |  | defines a key-ring to where keys will be added. This can be created as a multi-region key-ring, which enables multi-region keys |
| `location-id` | string | yes |  | defines the GCP region to use (e.g. us-east1) |
| `project-id` | string | yes |  | defines a GCP project to use (e.g. gcp-kms-testing) |
| `retries.create-key-check.initial-delay` | duration (NonNegativeFiniteDuration) | yes |  | initial delay before the first retry |
| `retries.create-key-check.max-delay` | duration (NonNegativeDuration) | yes |  | max delay between retries |
| `retries.create-key-check.max-retries` | int | yes |  | max number of retries |
| `retries.failures.initial-delay` | duration (NonNegativeFiniteDuration) | yes |  | initial delay before the first retry |
| `retries.failures.max-delay` | duration (NonNegativeDuration) | yes |  | max delay between retries |
| `retries.failures.max-retries` | int | yes |  | max number of retries |

</Tab>
</Tabs>

## MetricsReporterConfig

Set `type` to one of `prometheus`, `otlp`, `csv`, `logging`.

Occurs at:

- `canton.monitoring.metrics.reporters[]`

### `type = prometheus`

<Tabs>
<Tab title="HOCON">

```hocon
canton.monitoring.metrics.reporters = [
  {
    type = prometheus
    address = "localhost"
    filters = [
      {
        contains = ""
        ends-with = ""
        starts-with = ""
      }
    ]
    port = 9464
  }
]
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `address` | string |  | `localhost` |  |
| `filters` | array of MetricsFilterConfig |  | `[]` |  |
| `filters[].contains` | string |  | `""` |  |
| `filters[].ends-with` | string |  | `""` |  |
| `filters[].starts-with` | string |  | `""` |  |
| `port` | int (Port) |  | `9464` |  |

</Tab>
</Tabs>

### `type = otlp`

<Tabs>
<Tab title="HOCON">

```hocon
canton.monitoring.metrics.reporters = [
  {
    type = otlp
    auth {
      type = oauth-client-credentials   # required
    }
    endpoint = "http://localhost:4317"
    filters = [
      {
        contains = ""
        ends-with = ""
        starts-with = ""
      }
    ]
    interval = "10 seconds"
    protocol = grpc
  }
]
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `auth.type` | one of `oauth-client-credentials` | yes |  |  |
| `endpoint` | string |  | `http://localhost:4317` |  |
| `filters` | array of MetricsFilterConfig |  | `[]` |  |
| `filters[].contains` | string |  | `""` |  |
| `filters[].ends-with` | string |  | `""` |  |
| `filters[].starts-with` | string |  | `""` |  |
| `interval` | duration (NonNegativeFiniteDuration) |  | `10 seconds` |  |
| `protocol` | one of `grpc`, `http-protobuf` |  | `grpc` |  |

</Tab>
</Tabs>

Further types inside this one: [OtlpAuth](#otlpauth) at `auth.type`.

### `type = csv`

CSV metrics reporter configuration This reporter will write the given metrics into respective csv files. Please note that you should use filters as otherwise, you'll get many files

<Tabs>
<Tab title="HOCON">

```hocon
canton.monitoring.metrics.reporters = [
  {
    type = csv
    context-keys = [ ]
    directory = "/path/to/file"   # required
    filters = [
      {
        contains = ""
        ends-with = ""
        starts-with = ""
      }
    ]
    interval = "5 seconds"
  }
]
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `context-keys` | array of string |  | `node, synchronizer` | which context keys to include in the name. defaults to node names |
| `directory` | path | yes |  | where to write the csv files to |
| `filters` | array of MetricsFilterConfig |  | `[]` | which metrics to include |
| `filters[].contains` | string |  | `""` |  |
| `filters[].ends-with` | string |  | `""` |  |
| `filters[].starts-with` | string |  | `""` |  |
| `interval` | duration (NonNegativeFiniteDuration) |  | `5 seconds` | how often to write the csv files |

</Tab>
</Tabs>

### `type = logging`

Log metrics reporter configuration This reporter will log the metrics in the given interval

<Tabs>
<Tab title="HOCON">

```hocon
canton.monitoring.metrics.reporters = [
  {
    type = logging
    filters = [
      {
        contains = ""
        ends-with = ""
        starts-with = ""
      }
    ]
    interval = "30 seconds"
    log-as-info = true
  }
]
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `filters` | array of MetricsFilterConfig |  | `[]` | which metrics to include |
| `filters[].contains` | string |  | `""` |  |
| `filters[].ends-with` | string |  | `""` |  |
| `filters[].starts-with` | string |  | `""` |  |
| `interval` | duration (NonNegativeFiniteDuration) |  | `30 seconds` | how often to log the metrics |
| `log-as-info` | boolean |  | `true` |  |

</Tab>
</Tabs>

## NodeIdentifierConfig

Set `type` to one of `random`, `explicit`, `config`.

Occurs at:

- `canton.mediators.<mediator>.init.identity.identifier`
- `canton.participants.<participant>.init.identity.identifier`
- `canton.sequencers.<sequencer>.init.identity.identifier`

### `type = random`

```hocon
canton.mediators.<mediator>.init.identity.identifier {
  type = random
  # takes no further keys
}
```

### `type = explicit`

Sets an explicit name for the node identifier

<Tabs>
<Tab title="HOCON">

```hocon
canton.mediators.<mediator>.init.identity.identifier {
  type = explicit
  name = "..."   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `name` | string | yes |  | name to use as identifier |

</Tab>
</Tabs>

### `type = config`

```hocon
canton.mediators.<mediator>.init.identity.identifier {
  type = config
  # takes no further keys
}
```

## OtlpAuth

Set `type` to one of `oauth-client-credentials`.

Occurs at:

- `canton.monitoring.metrics.reporters[].auth`

### `type = oauth-client-credentials`

<Tabs>
<Tab title="HOCON">

```hocon
canton.monitoring.metrics.reporters = [
  {
    auth {
      type = oauth-client-credentials
      client-id = "..."   # required
      client-secret = "..."   # required
      # scope = <string>
      token-url = "..."   # required
    }
  }
]
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `client-id` | string | yes |  |  |
| `client-secret` | string | yes |  |  |
| `scope` | string |  | _unset_ |  |
| `token-url` | string | yes |  |  |

</Tab>
</Tabs>

## PemFileOrString

A class representing either an existing PEM file path or an inlined PEM string for configuration file fields

Set `type` to one of `pem-file`, `pem-string`.

Occurs at:

- `canton.mediators.<mediator>.admin-api.tls.cert-chain-file`
- `canton.mediators.<mediator>.admin-api.tls.client-auth.admin-client.cert-chain-file`
- `canton.mediators.<mediator>.admin-api.tls.trust-collection-file`
- `canton.parameters.clock.remote-api.tls.client-cert.cert-chain-file`
- `canton.parameters.clock.remote-api.tls.trust-collection-file`
- `canton.participants.<participant>.admin-api.tls.cert-chain-file`
- `canton.participants.<participant>.admin-api.tls.client-auth.admin-client.cert-chain-file`
- `canton.participants.<participant>.admin-api.tls.trust-collection-file`
- … and 26 more

### `type = pem-file`

A class representing an existing PEM file path for configuration file fields

<Tabs>
<Tab title="HOCON">

```hocon
canton.mediators.<mediator>.admin-api.tls.cert-chain-file {
  type = pem-file
  pem-file = "/path/to/file"   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `pem-file` | path (ExistingFile) | yes |  |  |

</Tab>
</Tabs>

### `type = pem-string`

A class representing an inlined PEM string for configuration file fields

<Tabs>
<Tab title="HOCON">

```hocon
canton.mediators.<mediator>.admin-api.tls.cert-chain-file {
  type = pem-string
  pem-bytes = "..."   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `pem-bytes` | string | yes |  |  |

</Tab>
</Tabs>

## Sampler

Set `type` to one of `always-on`, `always-off`, `trace-id-ratio`.

Occurs at:

- `canton.monitoring.tracing.tracer.sampler`

### `type = always-on`

<Tabs>
<Tab title="HOCON">

```hocon
canton.monitoring.tracing.tracer.sampler {
  type = always-on
  parent-based = true
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `parent-based` | boolean |  | `true` |  |

</Tab>
</Tabs>

### `type = always-off`

<Tabs>
<Tab title="HOCON">

```hocon
canton.monitoring.tracing.tracer.sampler {
  type = always-off
  parent-based = true
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `parent-based` | boolean |  | `true` |  |

</Tab>
</Tabs>

### `type = trace-id-ratio`

<Tabs>
<Tab title="HOCON">

```hocon
canton.monitoring.tracing.tracer.sampler {
  type = trace-id-ratio
  parent-based = true
  ratio = 0   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `parent-based` | boolean |  | `true` |  |
| `ratio` | number | yes |  |  |

</Tab>
</Tabs>

## SequencerConfig

Set `type` to one of `database`, `external`, `bft-sequencer`.

Occurs at:

- `canton.sequencers.<sequencer>.sequencer`

### `type = database`

<Tabs>
<Tab title="HOCON">

```hocon
canton.sequencers.<sequencer>.sequencer {
  type = database
  high-availability {
    connection-pool {
      active-timeout = "15 seconds"
      connection {
        client-connection-check-interval = "5 seconds"
        connection-timeout = "10 seconds"
        health-check-period = "5 seconds"
        health-check-timeout = "15 seconds"
        initial-acquisition-interval = "200 milliseconds"
        initial-acquisition-max-retries = 5
        keep-alive-count = 5
        keep-alive-idle = "10 seconds"
        keep-alive-interval = "1 second"
        lock {
          health-check-period = "5 seconds"
          health-check-timeout = "15 seconds"
        }
        max-inconclusive-read-only-checks = 3
        passive-check-period = "15 seconds"
      }
      health-check-period = "5 seconds"
      initial-locked-connection-timeout = "15 seconds"
      initial-must-remain-active-connection-timeout = "1 minute"
    }
    # enabled = <boolean>
    exclusive-storage {
      connection-pool {
        active-timeout = "15 seconds"
        connection {
          client-connection-check-interval = "5 seconds"
          connection-timeout = "10 seconds"
          health-check-period = "5 seconds"
          health-check-timeout = "15 seconds"
          initial-acquisition-interval = "200 milliseconds"
          initial-acquisition-max-retries = 5
          keep-alive-count = 5
          keep-alive-idle = "10 seconds"
          keep-alive-interval = "1 second"
          lock {
            health-check-period = "5 seconds"
            health-check-timeout = "15 seconds"
          }
          max-inconclusive-read-only-checks = 3
          passive-check-period = "15 seconds"
        }
        health-check-period = "5 seconds"
        initial-locked-connection-timeout = "15 seconds"
        initial-must-remain-active-connection-timeout = "1 minute"
      }
      max-connections = 2
    }
    keep-alive-interval = "50 milliseconds"
    offline-duration = "8 seconds"
    online-check-interval = "5 seconds"
    total-node-count = 10
  }
  pruning {
    max-pruning-batch-size = 50000
    # pruning-metric-update-interval = <duration>
    traffic-purchased-retention = "1 hour"
  }
  reader {
    checkpoint-interval = "5 seconds"
    event-generation-parallelism = 4
    payload-batch-size = 10
    payload-batch-window = "5 milliseconds"
    payload-fetch-parallelism = 2
    # polling-interval = <duration>
    read-batch-size = 100
  }
  # testing-interceptor = <string>
  writer {
    type = low-latency|high-throughput   # required
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `high-availability.connection-pool.active-timeout` | duration (PositiveFiniteDuration) |  | `15 seconds` | Time to wait until the first connection in the pool becomes active during failover. |
| `high-availability.connection-pool.connection.client-connection-check-interval` | duration (NonNegativeFiniteDuration) |  | `5 seconds` | Interval for client connection checks. Corresponds to the Postgres `client_connection_check_interval` configuration parameter (Postgres &gt;= 14 only). Millisecond granularity is the lowest supported precision. `None` leaves the server default unchanged whereas a Some(0) explicitly disables the check. On macOS with Postgres 14, this must be disabled, since in Postgres 14, this setting requires the non-standard `POLLRDHUP` extension to the `poll` system call, which is only available on Linux. See html for details. |
| `high-availability.connection-pool.connection.connection-timeout` | duration (PositiveFiniteDuration) |  | `10 seconds` | Timeout for requesting a new connection from the underlying database driver in seconds granularity. |
| `high-availability.connection-pool.connection.health-check-period` | duration (PositiveFiniteDuration) |  | `5 seconds` | Health check period, i.e., how long to wait between one health check completed and the next to start. |
| `high-availability.connection-pool.connection.health-check-timeout` | duration (PositiveFiniteDuration) |  | `15 seconds` | Timeout for running a health check in seconds granularity. |
| `high-availability.connection-pool.connection.initial-acquisition-interval` | duration (PositiveFiniteDuration) |  | `200 milliseconds` | Retry intervals during the initial attempts to acquire the lock. |
| `high-availability.connection-pool.connection.initial-acquisition-max-retries` | int |  | `5` | Maximum number of retries when trying to acquire the lock for the first time before trying to acquire the lock in a background task. |
| `high-availability.connection-pool.connection.keep-alive-count` | int |  | `5` | TCP keep-alive count, i.e., how many unanswered keep-alive messages required to consider the connection lost. |
| `high-availability.connection-pool.connection.keep-alive-idle` | duration (PositiveFiniteDuration) |  | `10 seconds` | TCP keep-alive idle time, i.e., how long to wait until sending a keep-alive message when idle. |
| `high-availability.connection-pool.connection.keep-alive-interval` | duration (PositiveFiniteDuration) |  | `1 second` | TCP keep-alive interval, i.e., how long to wait until resending an unanswered keep-alive message. |
| `high-availability.connection-pool.connection.lock.health-check-period` | duration (PositiveFiniteDuration) |  | `5 seconds` | Health check period, i.e., how long to wait between one health check completed and the next to start. |
| `high-availability.connection-pool.connection.lock.health-check-timeout` | duration (PositiveFiniteDuration) |  | `15 seconds` | Timeout for running a health check in seconds granularity. |
| `high-availability.connection-pool.connection.max-inconclusive-read-only-checks` | int |  | `3` | Number of consecutive inconclusive (timed-out or failed) read-only checks tolerated before the connection is considered lost. A single inconclusive check can be due to transient contention, but a persistently inconclusive check is problematic. |
| `high-availability.connection-pool.connection.passive-check-period` | duration (PositiveFiniteDuration) |  | `15 seconds` | How long to wait between trying to become active. |
| `high-availability.connection-pool.health-check-period` | duration (PositiveFiniteDuration) |  | `5 seconds` | Health check period, i.e., how long to wait between health checks of the connection pool. |
| `high-availability.connection-pool.initial-locked-connection-timeout` | duration (PositiveFiniteDuration) |  | `15 seconds` | Time to wait until the initial connection is either active or passive (external sequencer does not use this) |
| `high-availability.connection-pool.initial-must-remain-active-connection-timeout` | duration (PositiveFiniteDuration) |  | `1 minute` | If using an external sequencer we want to make sure it is always active, unless it is closing. This timeout is the initial time to wait before first becoming active, otherwise it is shutdown. |
| `high-availability.enabled` | boolean |  | _unset_ | Set to Some(true) to enable HA for the sequencer. If None it will be enabled if the storage supports replication. |
| `high-availability.exclusive-storage.connection-pool.active-timeout` | duration (PositiveFiniteDuration) |  | `15 seconds` | Time to wait until the first connection in the pool becomes active during failover. |
| `high-availability.exclusive-storage.connection-pool.connection.client-connection-check-interval` | duration (NonNegativeFiniteDuration) |  | `5 seconds` | Interval for client connection checks. Corresponds to the Postgres `client_connection_check_interval` configuration parameter (Postgres &gt;= 14 only). Millisecond granularity is the lowest supported precision. `None` leaves the server default unchanged whereas a Some(0) explicitly disables the check. On macOS with Postgres 14, this must be disabled, since in Postgres 14, this setting requires the non-standard `POLLRDHUP` extension to the `poll` system call, which is only available on Linux. See html for details. |
| `high-availability.exclusive-storage.connection-pool.connection.connection-timeout` | duration (PositiveFiniteDuration) |  | `10 seconds` | Timeout for requesting a new connection from the underlying database driver in seconds granularity. |
| `high-availability.exclusive-storage.connection-pool.connection.health-check-period` | duration (PositiveFiniteDuration) |  | `5 seconds` | Health check period, i.e., how long to wait between one health check completed and the next to start. |
| `high-availability.exclusive-storage.connection-pool.connection.health-check-timeout` | duration (PositiveFiniteDuration) |  | `15 seconds` | Timeout for running a health check in seconds granularity. |
| `high-availability.exclusive-storage.connection-pool.connection.initial-acquisition-interval` | duration (PositiveFiniteDuration) |  | `200 milliseconds` | Retry intervals during the initial attempts to acquire the lock. |
| `high-availability.exclusive-storage.connection-pool.connection.initial-acquisition-max-retries` | int |  | `5` | Maximum number of retries when trying to acquire the lock for the first time before trying to acquire the lock in a background task. |
| `high-availability.exclusive-storage.connection-pool.connection.keep-alive-count` | int |  | `5` | TCP keep-alive count, i.e., how many unanswered keep-alive messages required to consider the connection lost. |
| `high-availability.exclusive-storage.connection-pool.connection.keep-alive-idle` | duration (PositiveFiniteDuration) |  | `10 seconds` | TCP keep-alive idle time, i.e., how long to wait until sending a keep-alive message when idle. |
| `high-availability.exclusive-storage.connection-pool.connection.keep-alive-interval` | duration (PositiveFiniteDuration) |  | `1 second` | TCP keep-alive interval, i.e., how long to wait until resending an unanswered keep-alive message. |
| `high-availability.exclusive-storage.connection-pool.connection.lock.health-check-period` | duration (PositiveFiniteDuration) |  | `5 seconds` | Health check period, i.e., how long to wait between one health check completed and the next to start. |
| `high-availability.exclusive-storage.connection-pool.connection.lock.health-check-timeout` | duration (PositiveFiniteDuration) |  | `15 seconds` | Timeout for running a health check in seconds granularity. |
| `high-availability.exclusive-storage.connection-pool.connection.max-inconclusive-read-only-checks` | int |  | `3` | Number of consecutive inconclusive (timed-out or failed) read-only checks tolerated before the connection is considered lost. A single inconclusive check can be due to transient contention, but a persistently inconclusive check is problematic. |
| `high-availability.exclusive-storage.connection-pool.connection.passive-check-period` | duration (PositiveFiniteDuration) |  | `15 seconds` | How long to wait between trying to become active. |
| `high-availability.exclusive-storage.connection-pool.health-check-period` | duration (PositiveFiniteDuration) |  | `5 seconds` | Health check period, i.e., how long to wait between health checks of the connection pool. |
| `high-availability.exclusive-storage.connection-pool.initial-locked-connection-timeout` | duration (PositiveFiniteDuration) |  | `15 seconds` | Time to wait until the initial connection is either active or passive (external sequencer does not use this) |
| `high-availability.exclusive-storage.connection-pool.initial-must-remain-active-connection-timeout` | duration (PositiveFiniteDuration) |  | `1 minute` | If using an external sequencer we want to make sure it is always active, unless it is closing. This timeout is the initial time to wait before first becoming active, otherwise it is shutdown. |
| `high-availability.exclusive-storage.max-connections` | int (PositiveInt) |  | `2` | maximum read plus write connections. |
| `high-availability.keep-alive-interval` | duration (NonNegativeFiniteDuration) |  | `50 milliseconds` | how frequently will we ensure the sequencer watermark is updated to ensure it still appears alive |
| `high-availability.offline-duration` | duration (NonNegativeFiniteDuration) |  | `8 seconds` | how long should a sequencer watermark be lagging for it to be flagged as offline |
| `high-availability.online-check-interval` | duration (NonNegativeFiniteDuration) |  | `5 seconds` | how frequently should this sequencer check that nodes are still online |
| `high-availability.total-node-count` | int (PositiveInt) |  | `10` | how many sequencer writers will there ever be in this synchronizer. recommend setting to a value larger than the current topology to allow for expansion. |
| `pruning.max-pruning-batch-size` | int (PositiveInt) |  | `50000` | Maximum number of events to prune from a sequencer at a time, used to break up batches internally |
| `pruning.pruning-metric-update-interval` | duration (PositiveDurationSeconds) |  | `PositiveDurationSeconds.ofHours(1L).some` ‡ | How frequently to update the `max-event-age` pruning progress metric in the background. A setting of None disables background metric updating. |
| `pruning.traffic-purchased-retention` | duration (NonNegativeFiniteDuration) |  | `1 hour` | Retention duration on how long to retain traffic purchased entry updates for each member |
| `reader.checkpoint-interval` | duration (NonNegativeFiniteDuration) |  | `5 seconds` | how frequently to checkpoint state |
| `reader.event-generation-parallelism` | int |  | `4` | how many events will be generated from the fetched payloads in parallel |
| `reader.payload-batch-size` | int |  | `10` | max number of payloads to fetch from the datastore in one page |
| `reader.payload-batch-window` | duration (NonNegativeFiniteDuration) |  | `5 milliseconds` | max time window to wait for more payloads before fetching the current batch from the datastore |
| `reader.payload-fetch-parallelism` | int |  | `2` | how many batches of payloads will be fetched in parallel |
| `reader.polling-interval` | duration (NonNegativeFiniteDuration) |  | _unset_ | how frequently to poll for new events from the database. only used in the enterprise edition if high availability has been configured, otherwise will rely on local writes performed by this sequencer to indicate that new events are available. |
| `reader.read-batch-size` | int |  | `100` | max number of events to fetch from the datastore in one page |
| `testing-interceptor` | string (TestingInterceptor) |  | _unset_ |  |
| `writer.type` | one of `low-latency`, `high-throughput` | yes |  | Configuration for the database based sequencer writer |

</Tab>
</Tabs>

Further types inside this one: [SequencerWriterConfig](#sequencerwriterconfig) at `writer.type`.

### `type = external`

Read the node identifier from the config file

<Tabs>
<Tab title="HOCON">

```hocon
canton.sequencers.<sequencer>.sequencer {
  type = external
  block {
    circuit-breaker {
      enabled = true
      messages {
        acknowledgement {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
        commitment {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
        confirmation-request {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
        confirmation-response {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
        lsu-sequencing-test {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
        time-proof {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
        top-up {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
        topology {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
        unexpected {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
        verdict {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
      }
    }
    reader {
      checkpoint-interval = "5 seconds"
      event-generation-parallelism = 4
      payload-batch-size = 10
      payload-batch-window = "5 milliseconds"
      payload-fetch-parallelism = 2
      # polling-interval = <duration>
      read-batch-size = 100
    }
    stream-instrumentation {
      buffer-size = 256
      is-enabled = false
    }
    # testing-interceptor = <string>
    throughput-cap {
      clock-tick-interval = "100 milliseconds"
      delayed-activation = true
      enabled = false
      messages {
        confirmation-request {
          global-kbps-cap = 2000
          global-tps-cap = 10
          per-client-kbps-cap = 1000
          per-client-tps-cap = 4
        }
        confirmation-response {
          global-kbps-cap = 2000
          global-tps-cap = 10
          per-client-kbps-cap = 1000
          per-client-tps-cap = 4
        }
        topology {
          global-kbps-cap = 2000
          global-tps-cap = 10
          per-client-kbps-cap = 1000
          per-client-tps-cap = 4
        }
      }
      observation-period-seconds = 60
      strict = true
      thresholds = [ ]
      update-every-ms = 100
    }
    writer {
      type = low-latency|high-throughput   # required
    }
  }
  config = "..."   # required
  sequencer-type = "..."   # required
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `block.circuit-breaker.enabled` | boolean |  | `true` |  |
| `block.circuit-breaker.messages.acknowledgement.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.acknowledgement.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.acknowledgement.max-failures` | int |  | `10` |  |
| `block.circuit-breaker.messages.acknowledgement.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.acknowledgement.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.circuit-breaker.messages.commitment.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.commitment.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.commitment.max-failures` | int |  | `10` (in practice `30`) ◊ |  |
| `block.circuit-breaker.messages.commitment.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.commitment.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.circuit-breaker.messages.confirmation-request.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.confirmation-request.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.confirmation-request.max-failures` | int |  | `10` |  |
| `block.circuit-breaker.messages.confirmation-request.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.confirmation-request.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.circuit-breaker.messages.confirmation-response.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.confirmation-response.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.confirmation-response.max-failures` | int |  | `10` (in practice `60`) ◊ |  |
| `block.circuit-breaker.messages.confirmation-response.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.confirmation-response.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.circuit-breaker.messages.lsu-sequencing-test.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.lsu-sequencing-test.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.lsu-sequencing-test.max-failures` | int |  | `10` |  |
| `block.circuit-breaker.messages.lsu-sequencing-test.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.lsu-sequencing-test.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.circuit-breaker.messages.time-proof.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.time-proof.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.time-proof.max-failures` | int |  | `10` (in practice `30`) ◊ |  |
| `block.circuit-breaker.messages.time-proof.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.time-proof.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.circuit-breaker.messages.top-up.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.top-up.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.top-up.max-failures` | int |  | `10` (in practice `30`) ◊ |  |
| `block.circuit-breaker.messages.top-up.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.top-up.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.circuit-breaker.messages.topology.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.topology.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.topology.max-failures` | int |  | `10` |  |
| `block.circuit-breaker.messages.topology.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.topology.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.circuit-breaker.messages.unexpected.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.unexpected.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.unexpected.max-failures` | int |  | `10` |  |
| `block.circuit-breaker.messages.unexpected.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.unexpected.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.circuit-breaker.messages.verdict.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.verdict.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.verdict.max-failures` | int |  | `10` (in practice `60`) ◊ |  |
| `block.circuit-breaker.messages.verdict.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.verdict.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.reader.checkpoint-interval` | duration (NonNegativeFiniteDuration) |  | `5 seconds` | how frequently to checkpoint state |
| `block.reader.event-generation-parallelism` | int |  | `4` | how many events will be generated from the fetched payloads in parallel |
| `block.reader.payload-batch-size` | int |  | `10` | max number of payloads to fetch from the datastore in one page |
| `block.reader.payload-batch-window` | duration (NonNegativeFiniteDuration) |  | `5 milliseconds` | max time window to wait for more payloads before fetching the current batch from the datastore |
| `block.reader.payload-fetch-parallelism` | int |  | `2` | how many batches of payloads will be fetched in parallel |
| `block.reader.polling-interval` | duration (NonNegativeFiniteDuration) |  | _unset_ | how frequently to poll for new events from the database. only used in the enterprise edition if high availability has been configured, otherwise will rely on local writes performed by this sequencer to indicate that new events are available. |
| `block.reader.read-batch-size` | int |  | `100` | max number of events to fetch from the datastore in one page |
| `block.stream-instrumentation.buffer-size` | int (PositiveInt) |  | `256` (in practice `128`) ◊ |  |
| `block.stream-instrumentation.is-enabled` | boolean |  | `false` |  |
| `block.testing-interceptor` | string (TestingInterceptor) |  | _unset_ |  |
| `block.throughput-cap.clock-tick-interval` | duration (NonNegativeFiniteDuration) |  | `100 milliseconds` |  |
| `block.throughput-cap.delayed-activation` | boolean |  | `true` | If true (default) then the limits will be enforced after an initial grace period equal to the observationPeriodSeconds. |
| `block.throughput-cap.enabled` | boolean |  | `false` |  |
| `block.throughput-cap.messages.confirmation-request.global-kbps-cap` | number (PositiveDouble) |  | `2000` |  |
| `block.throughput-cap.messages.confirmation-request.global-tps-cap` | number (PositiveDouble) |  | `10` |  |
| `block.throughput-cap.messages.confirmation-request.per-client-kbps-cap` | number (PositiveDouble) |  | `1000` |  |
| `block.throughput-cap.messages.confirmation-request.per-client-tps-cap` | number (PositiveDouble) |  | `4` |  |
| `block.throughput-cap.messages.confirmation-response.global-kbps-cap` | number (PositiveDouble) |  | `2000` |  |
| `block.throughput-cap.messages.confirmation-response.global-tps-cap` | number (PositiveDouble) |  | `10` |  |
| `block.throughput-cap.messages.confirmation-response.per-client-kbps-cap` | number (PositiveDouble) |  | `1000` |  |
| `block.throughput-cap.messages.confirmation-response.per-client-tps-cap` | number (PositiveDouble) |  | `4` |  |
| `block.throughput-cap.messages.topology.global-kbps-cap` | number (PositiveDouble) |  | `2000` |  |
| `block.throughput-cap.messages.topology.global-tps-cap` | number (PositiveDouble) |  | `10` (in practice `2`) ◊ |  |
| `block.throughput-cap.messages.topology.per-client-kbps-cap` | number (PositiveDouble) |  | `1000` |  |
| `block.throughput-cap.messages.topology.per-client-tps-cap` | number (PositiveDouble) |  | `4` (in practice `1`) ◊ |  |
| `block.throughput-cap.observation-period-seconds` | int |  | `60` |  |
| `block.throughput-cap.strict` | boolean |  | `true` | how should the per member rate cap be computed |
| `block.throughput-cap.thresholds` | array of PositiveDouble |  | `0.9` |  |
| `block.throughput-cap.update-every-ms` | int (NonNegativeInt) |  | `100` | how often should the new caps be computed (only used in non-strict mode) |
| `block.writer.type` | one of `low-latency`, `high-throughput` | yes | `high-throughput` † | Configuration for the database based sequencer writer |
| `config` | ConfigCursor (unclassified) | yes |  |  |
| `sequencer-type` | string | yes |  |  |

</Tab>
</Tabs>

Further types inside this one: [SequencerWriterConfig](#sequencerwriterconfig) at `block.writer.type`.

### `type = bft-sequencer`

<Tabs>
<Tab title="HOCON">

```hocon
canton.sequencers.<sequencer>.sequencer {
  type = bft-sequencer
  block {
    circuit-breaker {
      enabled = true
      messages {
        acknowledgement {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
        commitment {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
        confirmation-request {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
        confirmation-response {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
        lsu-sequencing-test {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
        time-proof {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
        top-up {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
        topology {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
        unexpected {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
        verdict {
          allowed-block-delay = "90 seconds"
          exponential-backoff-factor = 1
          max-failures = 10
          max-reset-timeout = "36500 days"
          reset-timeout = "30 seconds"
        }
      }
    }
    reader {
      checkpoint-interval = "5 seconds"
      event-generation-parallelism = 4
      payload-batch-size = 10
      payload-batch-window = "5 milliseconds"
      payload-fetch-parallelism = 2
      # polling-interval = <duration>
      read-batch-size = 100
    }
    stream-instrumentation {
      buffer-size = 256
      is-enabled = false
    }
    # testing-interceptor = <string>
    throughput-cap {
      clock-tick-interval = "100 milliseconds"
      delayed-activation = true
      enabled = false
      messages {
        confirmation-request {
          global-kbps-cap = 2000
          global-tps-cap = 10
          per-client-kbps-cap = 1000
          per-client-tps-cap = 4
        }
        confirmation-response {
          global-kbps-cap = 2000
          global-tps-cap = 10
          per-client-kbps-cap = 1000
          per-client-tps-cap = 4
        }
        topology {
          global-kbps-cap = 2000
          global-tps-cap = 10
          per-client-kbps-cap = 1000
          per-client-tps-cap = 4
        }
      }
      observation-period-seconds = 60
      strict = true
      thresholds = [ ]
      update-every-ms = 100
    }
    writer {
      type = low-latency|high-throughput   # required
    }
  }
  config {
    availability-dissemination-patience = "5 seconds"
    availability-max-non-ordered-batches-per-node = 1000
    availability-min-proposal-creation-delay = "250 milliseconds"
    availability-number-of-attempts-of-downloading-output-fetch-before-warning = 5
    batch-aggregator {
      type = batching|no-batching   # required
    }
    blocking-db-read-timeout = "1 minute"
    consensus-block-completion-timeout = "10 seconds"
    consensus-empty-block-creation-timeout = "500 milliseconds"
    consensus-enable-flushing-segment = true
    consensus-enable-log-end-of-epoch-progress = false
    consensus-flushing-min-blocks = 2
    consensus-new-epoch-topology-warn-timeout = "2 seconds"
    # consensus-queue-max-size = <int>
    consensus-queue-per-node-quota = 1024
    # dedicated-execution-context-divisor = <int>
    delayed-init-queue-max-size = 1024
    enable-performance-metrics = true
    # epoch-state-transfer-future-epoch-queue-max-size = <int>
    epoch-state-transfer-future-epoch-queue-per-node-quota = 1024
    epoch-state-transfer-how-many-future-epochs-to-download-in-parallel = 5
    epoch-state-transfer-retry-timeout = "4 seconds"
    epoch-state-transfer-timeout-for-future-epoch = "500 milliseconds"
    # init-query-timeout = <duration>
    # init-timeout = <duration>
    initial-network {
      connection-management-config {
        # connection-retry-delay-multiplier = <int>
        flow-control-buffer = 128
        flow-control-buffer-drop-newest = false
        # flow-control-ready-allowance = <int>
        initial-connection-max-delay = "500 milliseconds"
        initial-connection-retry-delay = "500 milliseconds"
        max-connection-attempts-before-warning = 30
        max-connection-retry-delay = "2 minutes"
      }
      endpoint-authentication {
        auth-token {
          backoff {
            base = 2
            jitter {
              type = equal|full   # required
            }
            max-retry-interval = "2 minutes"
          }
          min-retry-interval = "500 milliseconds"
          refresh-auth-token-before-expiry = "20 seconds"
          retries = 20
        }
        enabled = true
      }
      overwrite-stored-endpoints = false
      peer-endpoints = [
        {
          address = "..."   # required
          channel {
            # flow-control-window = <int>
            # initial-flow-control-window = <int>
            keep-alive-client {
              idle-timeout = "30 minutes"
              keep-alive-without-calls = false
              time = "40 seconds"
              timeout = "15 seconds"
            }
            max-inbound-message-size = 0   # required
            trace-context-propagation = enabled|disabled   # required
          }
          port = 0   # required
          tls-config {
            client-cert {
              cert-chain-file {
                type = pem-file|pem-string   # required
              }
              private-key-file = "..."   # required
            }
            enabled = true
            trust-collection-file {
              type = pem-file|pem-string   # required
            }
          }
        }
      ]
      server-endpoint {
        address = "..."   # required
        external-address = "..."   # required
        external-port = 0   # required
        external-tls-config {
          client-cert {
            cert-chain-file {
              type = pem-file|pem-string   # required
            }
            private-key-file = "..."   # required
          }
          enabled = true
          trust-collection-file {
            type = pem-file|pem-string   # required
          }
        }
        # flow-control-window = <int>
        # initial-flow-control-window = <int>
        keep-alive-server {
          permit-keep-alive-time = "20 seconds"
          permit-keep-alive-without-calls = false
          time = "40 seconds"
          timeout = "20 seconds"
        }
        limits {
          active {
            <key> = [ ]
          }
          throttle-logging-rate-per-second = 10
          warn-on-undefined-limits = false
        }
        max-concurrent-calls-per-connection = 100000
        # max-inbound-message-size = <int>
        # port = <int>
        tls {
          cert-chain-file {
            type = pem-file|pem-string   # required
          }
          # ciphers = [ ... ]
          client-auth {
            type = require|optional|none   # required
          }
          enable-cert-revocation-checking = false
          minimum-server-protocol-version = "TLSv1.2"
          private-key-file = "..."   # required
          trust-collection-file {
            type = pem-file|pem-string   # required
          }
        }
      }
    }
    leader-selection-policy-config-for-pv-34 {
      how-long-to-blacklist {
        type = linear|linear-with-parameters|exponential|no-blacklisting   # required
      }
      how-many-can-we-blacklist = num-faults-tolerated|no-blacklisting   # required
    }
    manual-vacuum-enabled = false
    max-batch-creation-interval = "100 milliseconds"
    # max-mempool-queue-size = <int>
    min-requests-in-batch = 3
    network-send-attempts = 5
    # network-send-retry-jitter-cap = <duration>
    # network-send-retry-minimum-delay = <duration>
    # output-enqueue-max-retries = <int>
    output-enqueue-max-retry-delay = "5 seconds"
    output-fetch-how-many-recipients = 2
    output-fetch-minimum-delay = "200 milliseconds"
    output-fetch-timeout = "200 milliseconds"
    output-fetch-timeout-cap = "200 milliseconds"
    output-size-of-chunk-of-epochs-to-load-at-start = 10
    # segment-length-for-pv-34 = <int>
    send-blacklist-ttl = "3 minutes"
    sequencer-core-subscription-config {
      pause-orderer-threshold-buffer-size = 5000
      pekko-queue-source-buffer-size = 5000
      resume-orderer-threshold-buffer-size = 1000
    }
    standalone {
      blacklist-leader-selection-policy-config {
        how-long-to-blacklist {
          type = linear|linear-with-parameters|exponential|no-blacklisting   # required
        }
        how-many-can-we-blacklist = num-faults-tolerated|no-blacklisting   # required
      }
      max-batches-per-block-proposal = 0   # required
      max-requests-in-batch = 0   # required
      pbft-view-change-timeout = "30 seconds"   # required
      peers = [
        {
          sequencer-id = "..."   # required
          signing-public-key-proto-file = "/path/to/file"   # required
        }
      ]
      segment-length = 0   # required
      signing-private-key-proto-file = "/path/to/file"   # required
      signing-public-key-proto-file = "/path/to/file"   # required
      test-slowdown {
        post-ordering-delay {
          type = constant-distribution|weighted-distribution|linear-distribution|power-distribution   # required
        }
        send-delay {
          default-delay-distribution {
            type = constant-distribution|weighted-distribution|linear-distribution|power-distribution   # required
          }
          delays-by-recipients = [
            {
              delay-distribution {
                type = constant-distribution|weighted-distribution|linear-distribution|power-distribution   # required
              }
              instance-names = [ ]   # required
              # probability-of-grpc-send-accepted-by-flow-control = <number>
              # probability-of-grpc-send-success = <number>
            }
          ]
        }
        topology-delay {
          # broadcast-request-probability = <number>
          get-ordering-topology-delay {
            type = constant-distribution|weighted-distribution|linear-distribution|power-distribution   # required
          }
          # pending-topology-changes-probability = <number>
          # possible-ordering-topology-change-in-request-probability = <number>
          request-inspection-delay {
            type = constant-distribution|weighted-distribution|linear-distribution|power-distribution   # required
          }
        }
      }
      this-sequencer-id = "..."   # required
    }
    storage {
      type = memory|h2|postgres   # required
    }
    # view-change-timeout-override = <duration>
  }
  # config.initial-network.server-endpoint.tls.client-auth.admin-client.cert-chain-file.type takes a type of its own, documented separately
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `block.circuit-breaker.enabled` | boolean |  | `true` |  |
| `block.circuit-breaker.messages.acknowledgement.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.acknowledgement.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.acknowledgement.max-failures` | int |  | `10` |  |
| `block.circuit-breaker.messages.acknowledgement.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.acknowledgement.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.circuit-breaker.messages.commitment.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.commitment.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.commitment.max-failures` | int |  | `10` (in practice `30`) ◊ |  |
| `block.circuit-breaker.messages.commitment.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.commitment.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.circuit-breaker.messages.confirmation-request.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.confirmation-request.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.confirmation-request.max-failures` | int |  | `10` |  |
| `block.circuit-breaker.messages.confirmation-request.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.confirmation-request.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.circuit-breaker.messages.confirmation-response.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.confirmation-response.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.confirmation-response.max-failures` | int |  | `10` (in practice `60`) ◊ |  |
| `block.circuit-breaker.messages.confirmation-response.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.confirmation-response.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.circuit-breaker.messages.lsu-sequencing-test.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.lsu-sequencing-test.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.lsu-sequencing-test.max-failures` | int |  | `10` |  |
| `block.circuit-breaker.messages.lsu-sequencing-test.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.lsu-sequencing-test.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.circuit-breaker.messages.time-proof.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.time-proof.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.time-proof.max-failures` | int |  | `10` (in practice `30`) ◊ |  |
| `block.circuit-breaker.messages.time-proof.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.time-proof.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.circuit-breaker.messages.top-up.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.top-up.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.top-up.max-failures` | int |  | `10` (in practice `30`) ◊ |  |
| `block.circuit-breaker.messages.top-up.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.top-up.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.circuit-breaker.messages.topology.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.topology.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.topology.max-failures` | int |  | `10` |  |
| `block.circuit-breaker.messages.topology.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.topology.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.circuit-breaker.messages.unexpected.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.unexpected.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.unexpected.max-failures` | int |  | `10` |  |
| `block.circuit-breaker.messages.unexpected.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.unexpected.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.circuit-breaker.messages.verdict.allowed-block-delay` | duration (NonNegativeFiniteDuration) |  | `90 seconds` |  |
| `block.circuit-breaker.messages.verdict.exponential-backoff-factor` | number |  | `1` |  |
| `block.circuit-breaker.messages.verdict.max-failures` | int |  | `10` (in practice `60`) ◊ |  |
| `block.circuit-breaker.messages.verdict.max-reset-timeout` | duration (NonNegativeFiniteDuration) |  | `36500 days` |  |
| `block.circuit-breaker.messages.verdict.reset-timeout` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `block.reader.checkpoint-interval` | duration (NonNegativeFiniteDuration) |  | `5 seconds` | how frequently to checkpoint state |
| `block.reader.event-generation-parallelism` | int |  | `4` | how many events will be generated from the fetched payloads in parallel |
| `block.reader.payload-batch-size` | int |  | `10` | max number of payloads to fetch from the datastore in one page |
| `block.reader.payload-batch-window` | duration (NonNegativeFiniteDuration) |  | `5 milliseconds` | max time window to wait for more payloads before fetching the current batch from the datastore |
| `block.reader.payload-fetch-parallelism` | int |  | `2` | how many batches of payloads will be fetched in parallel |
| `block.reader.polling-interval` | duration (NonNegativeFiniteDuration) |  | _unset_ | how frequently to poll for new events from the database. only used in the enterprise edition if high availability has been configured, otherwise will rely on local writes performed by this sequencer to indicate that new events are available. |
| `block.reader.read-batch-size` | int |  | `100` | max number of events to fetch from the datastore in one page |
| `block.stream-instrumentation.buffer-size` | int (PositiveInt) |  | `256` (in practice `128`) ◊ |  |
| `block.stream-instrumentation.is-enabled` | boolean |  | `false` |  |
| `block.testing-interceptor` | string (TestingInterceptor) |  | _unset_ |  |
| `block.throughput-cap.clock-tick-interval` | duration (NonNegativeFiniteDuration) |  | `100 milliseconds` |  |
| `block.throughput-cap.delayed-activation` | boolean |  | `true` | If true (default) then the limits will be enforced after an initial grace period equal to the observationPeriodSeconds. |
| `block.throughput-cap.enabled` | boolean |  | `false` |  |
| `block.throughput-cap.messages.confirmation-request.global-kbps-cap` | number (PositiveDouble) |  | `2000` |  |
| `block.throughput-cap.messages.confirmation-request.global-tps-cap` | number (PositiveDouble) |  | `10` |  |
| `block.throughput-cap.messages.confirmation-request.per-client-kbps-cap` | number (PositiveDouble) |  | `1000` |  |
| `block.throughput-cap.messages.confirmation-request.per-client-tps-cap` | number (PositiveDouble) |  | `4` |  |
| `block.throughput-cap.messages.confirmation-response.global-kbps-cap` | number (PositiveDouble) |  | `2000` |  |
| `block.throughput-cap.messages.confirmation-response.global-tps-cap` | number (PositiveDouble) |  | `10` |  |
| `block.throughput-cap.messages.confirmation-response.per-client-kbps-cap` | number (PositiveDouble) |  | `1000` |  |
| `block.throughput-cap.messages.confirmation-response.per-client-tps-cap` | number (PositiveDouble) |  | `4` |  |
| `block.throughput-cap.messages.topology.global-kbps-cap` | number (PositiveDouble) |  | `2000` |  |
| `block.throughput-cap.messages.topology.global-tps-cap` | number (PositiveDouble) |  | `10` (in practice `2`) ◊ |  |
| `block.throughput-cap.messages.topology.per-client-kbps-cap` | number (PositiveDouble) |  | `1000` |  |
| `block.throughput-cap.messages.topology.per-client-tps-cap` | number (PositiveDouble) |  | `4` (in practice `1`) ◊ |  |
| `block.throughput-cap.observation-period-seconds` | int |  | `60` |  |
| `block.throughput-cap.strict` | boolean |  | `true` | how should the per member rate cap be computed |
| `block.throughput-cap.thresholds` | array of PositiveDouble |  | `0.9` |  |
| `block.throughput-cap.update-every-ms` | int (NonNegativeInt) |  | `100` | how often should the new caps be computed (only used in non-strict mode) |
| `block.writer.type` | one of `low-latency`, `high-throughput` | yes | `high-throughput` † | Configuration for the database based sequencer writer |
| `config.availability-dissemination-patience` | duration |  | `5 seconds` | The amount of time that the availability module waits for a dissemination acknowledgement from a peer before re-sending the batch. If `None`, re-sending is disabled. |
| `config.availability-max-non-ordered-batches-per-node` | int |  | `1000` | The maximum number of unordered batches that the availability module will accept from each peer. The purpose of this limit is to prevent malicious peers from consuming memory and storage by flooding new batches to correct nodes. |
| `config.availability-min-proposal-creation-delay` | duration |  | `250 milliseconds` | The minimum delay between consecutive proposal creations in the availability module. This prevents the node from creating proposals too frequently. |
| `config.availability-number-of-attempts-of-downloading-output-fetch-before-warning` | int |  | `5` | The number of consecutive failed attempts to retrieve data for an ordered batch before the ordering node emits a warning to the logs |
| `config.batch-aggregator.type` | one of `batching`, `no-batching` | yes |  | Parameters for that batcher that batches queries (e.g., to a DB). |
| `config.blocking-db-read-timeout` | duration |  | `1 minute` | The maximum time allowed for block reads to restore state from the database after restarts. Currently, if the timeout is reached during any of the blocking reads on startup, the node shuts down and must restart to try again. |
| `config.consensus-block-completion-timeout` | duration |  | `10 seconds` | The maximum time that nodes will wait for each leader to order the next expected block in the corresponding index. After each block in a segment is ordered (and at the start of each epoch), this timeout is reset for that corresponding segment. If the timeout is reached for a given segment, the node will vote for a View Change. |
| `config.consensus-empty-block-creation-timeout` | duration |  | `500 milliseconds` | The time after which a leader will create an empty block if there is no regular block available to propose, in order to ensure that: (i) View Changes are avoided, and (ii) BFT time advances at a regular frequency. Note that due to (i), it is recommended that this consensusEmptyBlockCreationTimeout be substantially less than the consensusBlockCompletionTimeout to account for latency, retransmissions, etc. |
| `config.consensus-enable-flushing-segment` | boolean |  | `true` | If `true`, when this node detects that a strong quorum of segments are completed while the one it leads is in progress, it will flush the segment and complete all slots in parallel to avoid making other nodes wait for the epoch to complete. This is a performance optimization that can be disabled if issues arise with the flushing logic. |
| `config.consensus-enable-log-end-of-epoch-progress` | boolean |  | `false` | If `true`, logs are emitted to track progress at the end of each epoch with the status of segments from all leaders. |
| `config.consensus-flushing-min-blocks` | int |  | `2` | The minimum number of blocks that must be missing to complete in a segment before the node will be able to decide to flush the segment and complete all slots in parallel if it detects that a strong quorum of segments are completed while the one it leads is in progress. If fewer blocks than this are missing, the node will complete them normally one at a time. |
| `config.consensus-new-epoch-topology-warn-timeout` | duration |  | `2 seconds` | The time that nodes will wait at the start of a new epoch for the topology to be activated. If the timeout is reached, the node will create a warning log to indicate the reason for delay in progress was the topology activation, but the node will continue to wait for the topology to be activated. |
| `config.consensus-queue-max-size` | int |  | `DefaultConsensusQueueMaxSize` ‡ | The maximum size per consensus-related queue. |
| `config.consensus-queue-per-node-quota` | int |  | `1024` | The maximum number of messages per node stored in consensus-related queues (quotas are maintained separately per queue). |
| `config.dedicated-execution-context-divisor` | int |  | `DefaultDedicatedExecutionContextDivisor` ‡ | Optionally, a dedicated execution context for the BFT orderer. When set, the BFT orderer creates an independent execution context, separate from the colocated sequencer, using `Threading.detectNumberOfThreads` / `dedicatedExecutionContextDivisor`. This is potentially useful in deployments with heavy execution context contention between the sequencer and BFT ordering layer. If set to the default None, the BFT orderer shares the same execution context as the sequencer. |
| `config.delayed-init-queue-max-size` | int |  | `1024` | The maximum size of the delayed init queue. This queue is used by modules to save incoming events in memory while the module is still initializing. Once startup is complete, the module processes all events from the delayed init queue first before continuing to read newly received events. |
| `config.enable-performance-metrics` | boolean |  | `true` | If `true`, the BFT layer emits metrics to track real-time and historical performance. |
| `config.epoch-state-transfer-future-epoch-queue-max-size` | int |  | `DefaultConsensusQueueMaxSize` ‡ | The maximum size of future epoch queue. |
| `config.epoch-state-transfer-future-epoch-queue-per-node-quota` | int |  | `1024` | The maximum number of messages per node stored in future epoch queue |
| `config.epoch-state-transfer-how-many-future-epochs-to-download-in-parallel` | int (NonNegativeLong) |  | `5` | The amount of epochs to speculatively download in parallel during State Transfer. Can be 0 to turn off speculative downloading |
| `config.epoch-state-transfer-retry-timeout` | duration |  | `4 seconds` | The state transfer retry timeout covering periods from requesting blocks from a single epoch up to receiving all the corresponding batches. |
| `config.epoch-state-transfer-timeout-for-future-epoch` | duration |  | `500 milliseconds` | A timeout for how long we will wait if we already potentially have all blocks due to speculatively downloaded an epoch before we make a new network request. This timeout should be quite short since it is only accounting for local processing. |
| `config.init-query-timeout` | duration (NonNegativeFiniteDuration) |  | `DefaultInitQueryTimeout` ‡ | The maximum time allowed for individual queries during BFT block orderer initialization (e.g., topology or state queries). Must be less than or equal to `initTimeout`. |
| `config.init-timeout` | duration (NonNegativeFiniteDuration) |  | `DefaultInitTimeout` ‡ | The maximum total time allowed for the BFT block orderer to complete initialization, including all startup queries and state restoration. If exceeded, the node fails to start. |
| `config.initial-network.connection-management-config.connection-retry-delay-multiplier` | int (NonNegativeInt) |  | `NonNegativeInt.two` ‡ | The backoff factor applied to the delay between subsequent failed retry attempts |
| `config.initial-network.connection-management-config.flow-control-buffer` | int (PositiveInt) |  | `128` | If set, enables P2P flow control on the sender side with the specified buffer size. If not set, the gRPC implementation will manage send buffers in case of slow receivers (and may OOM). |
| `config.initial-network.connection-management-config.flow-control-buffer-drop-newest` | boolean |  | `false` | If true, newest excess sends, rather than oldest, will be dropped from the flow control buffer. |
| `config.initial-network.connection-management-config.flow-control-ready-allowance` | int (PositiveInt) |  | `PositiveInt.one` ‡ | If `flowControlBuffer` is set, a "ready to send" signal will be valid for the specified number of messages without further checks. |
| `config.initial-network.connection-management-config.initial-connection-max-delay` | duration (NonNegativeFiniteDuration) |  | `500 milliseconds` | The base max delay between retry attempts, used to bootstrap the jitter mechanism |
| `config.initial-network.connection-management-config.initial-connection-retry-delay` | duration (NonNegativeFiniteDuration) |  | `500 milliseconds` | The base delay between retry attempts when connecting to a peer |
| `config.initial-network.connection-management-config.max-connection-attempts-before-warning` | int (NonNegativeInt) |  | `30` | The number of failed connection attempts after which a WARN log is emitted |
| `config.initial-network.connection-management-config.max-connection-retry-delay` | duration (NonNegativeFiniteDuration) |  | `2 minutes` | The maximum delay between retry attempts to connect to a peer |
| `config.initial-network.endpoint-authentication.auth-token.backoff.base` | int (NonNegativeInt) |  | `2` |  |
| `config.initial-network.endpoint-authentication.auth-token.backoff.jitter.type` | one of `equal`, `full` | yes |  |  |
| `config.initial-network.endpoint-authentication.auth-token.backoff.max-retry-interval` | duration (NonNegativeFiniteDuration) |  | `2 minutes` |  |
| `config.initial-network.endpoint-authentication.auth-token.min-retry-interval` | duration (NonNegativeFiniteDuration) |  | `500 milliseconds` |  |
| `config.initial-network.endpoint-authentication.auth-token.refresh-auth-token-before-expiry` | duration (NonNegativeFiniteDuration) |  | `20 seconds` | how much time before the auth token expires should we fetch a new one? |
| `config.initial-network.endpoint-authentication.auth-token.retries` | int (NonNegativeInt) |  | `20` |  |
| `config.initial-network.endpoint-authentication.enabled` | boolean |  | `true` | Whether authentication should be setup between peers. For security reasons, this should NOT be disabled in any real production deployments, unless a decision is explicitly made to exclusively rely on mTLS for peer-to-peer authentication. |
| `config.initial-network.overwrite-stored-endpoints` | boolean |  | `false` | whether the peers in storage should be replaced by this config's peers endpoint on startup |
| `config.initial-network.peer-endpoints` | array of P2PEndpointConfig |  | `[]` | set of peers that the local node expects to connect to on startup |
| `config.initial-network.peer-endpoints[].address` | string | yes |  |  |
| `config.initial-network.peer-endpoints[].channel.flow-control-window` | int (PositiveInt) |  | _unset_ | Switches to manual gRPC flow control and sets its window; if `None`, then it is not configured and the implementation default is used. At most one of `flowControlWindow` and `initialFlowControlWindow` can be set. |
| `config.initial-network.peer-endpoints[].channel.initial-flow-control-window` | int (PositiveInt) |  | _unset_ | Switches to automatic gRPC flow control and sets its initial window; if `None`, then it is not configured and the implementation default is used. At most one of `flowControlWindow` and `initialFlowControlWindow` can be set. |
| `config.initial-network.peer-endpoints[].channel.keep-alive-client.idle-timeout` | duration (NonNegativeFiniteDuration) |  | `30 minutes` | If there are no RPCs during this duration, the channel will close all connections and switch to Idle state (default is 30 min) |
| `config.initial-network.peer-endpoints[].channel.keep-alive-client.keep-alive-without-calls` | boolean |  | `false` | Enables sending of keep alive even when there are no RPCs on the channel (default to false). This increases network resource consumption for idle connections and requires the server to enable permitKeepAliveWithoutCalls. Prefer using idleTimeout. |
| `config.initial-network.peer-endpoints[].channel.keep-alive-client.time` | duration (NonNegativeFiniteDuration) |  | `40 seconds` | Sets the time without read activity before sending a keepalive ping. Do not set to small numbers (default is 40s) |
| `config.initial-network.peer-endpoints[].channel.keep-alive-client.timeout` | duration (NonNegativeFiniteDuration) |  | `15 seconds` | Sets the time waiting for read activity after sending a keepalive ping (default is 15s). The default timeout was previously 20s and was lowered to 15s, because besides configuring the gRPC KeepAliveManager, it also sets up the socket's TCP_USER_TIMEOUT (see html). 15s gives a larger margin to detect a faulty connection earlier and retry a submission on another sequencer via amplification, thereby avoiding a request failure. |
| `config.initial-network.peer-endpoints[].channel.max-inbound-message-size` | int (NonNegativeInt) | yes |  |  |
| `config.initial-network.peer-endpoints[].channel.trace-context-propagation` | one of `enabled`, `disabled` | yes |  |  |
| `config.initial-network.peer-endpoints[].port` | int (Port) | yes |  |  |
| `config.initial-network.peer-endpoints[].tls-config.client-cert.cert-chain-file.type` | one of `pem-file`, `pem-string` | yes |  | A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
| `config.initial-network.peer-endpoints[].tls-config.client-cert.private-key-file` | string (PemFile) | yes |  |  |
| `config.initial-network.peer-endpoints[].tls-config.enabled` | boolean |  | `true` | allows enabling TLS without `trustCollectionFile` or `clientCert` |
| `config.initial-network.peer-endpoints[].tls-config.trust-collection-file.type` | one of `pem-file`, `pem-string` | yes |  | A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
| `config.initial-network.server-endpoint.address` | string | yes |  | The internal binding address for the server. |
| `config.initial-network.server-endpoint.external-address` | string | yes |  | The external address that remote peers should connect to in order to reach the local server. In practice, this may not be the actual internal address that the local server listens on, and instead may represent, for example, an intermediate TLS-terminating proxy. |
| `config.initial-network.server-endpoint.external-port` | int (Port) | yes |  | The external port that remote peers should connect to in order to reach the local server. In practice, this may not be the actual internal internalPort that the local server listens on, and instead may represent, for example, an intermediate TLS-terminating proxy. |
| `config.initial-network.server-endpoint.external-tls-config.client-cert.cert-chain-file.type` | one of `pem-file`, `pem-string` | yes |  | A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
| `config.initial-network.server-endpoint.external-tls-config.client-cert.private-key-file` | string (PemFile) | yes |  |  |
| `config.initial-network.server-endpoint.external-tls-config.enabled` | boolean |  | `true` | allows enabling TLS without `trustCollectionFile` or `clientCert` |
| `config.initial-network.server-endpoint.external-tls-config.trust-collection-file.type` | one of `pem-file`, `pem-string` | yes |  | A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
| `config.initial-network.server-endpoint.flow-control-window` | int (PositiveInt) |  | `ServerConfig.defaultFlowControlWindow` ‡ |  |
| `config.initial-network.server-endpoint.initial-flow-control-window` | int (PositiveInt) |  | `ServerConfig.defaultInitialFlowControlWindow` ‡ |  |
| `config.initial-network.server-endpoint.keep-alive-server.permit-keep-alive-time` | duration (NonNegativeFiniteDuration) |  | `20 seconds` |  |
| `config.initial-network.server-endpoint.keep-alive-server.permit-keep-alive-without-calls` | boolean |  | `false` |  |
| `config.initial-network.server-endpoint.keep-alive-server.time` | duration (NonNegativeFiniteDuration) |  | `40 seconds` |  |
| `config.initial-network.server-endpoint.keep-alive-server.timeout` | duration (NonNegativeFiniteDuration) |  | `20 seconds` |  |
| `config.initial-network.server-endpoint.limits.active.<key>` | int (NonNegativeInt) |  | `[]` | map of service name to maximum number of parallel active requests or streams |
| `config.initial-network.server-endpoint.limits.throttle-logging-rate-per-second` | int (NonNegativeInt) |  | `10` | maximum rate for logging rejections for requests exceeding the limit (prevents DOS on logs) |
| `config.initial-network.server-endpoint.limits.warn-on-undefined-limits` | boolean |  | `false` | emit warning if a limit is not configured for a stream |
| `config.initial-network.server-endpoint.max-concurrent-calls-per-connection` | int (NonNegativeInt) |  | `100000` |  |
| `config.initial-network.server-endpoint.max-inbound-message-size` | int (NonNegativeInt) |  | `ServerConfig.defaultMaxInboundMessageSize` ‡ | maximum allowed size of incoming messages |
| `config.initial-network.server-endpoint.port` | int (Port) |  | _unset_ | The internal binding port for the server. |
| `config.initial-network.server-endpoint.tls.cert-chain-file.type` | one of `pem-file`, `pem-string` | yes |  | A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
| `config.initial-network.server-endpoint.tls.ciphers` | array of string |  | `TlsConfig.defaultCiphers` ‡ | supported ciphers. Set to None (or null in config file) to use default settings defaultCiphers |
| `config.initial-network.server-endpoint.tls.client-auth.type` | one of `require`, `optional`, `none` | yes |  | Configuration on whether server requires auth, requests auth, or no auth |
| `config.initial-network.server-endpoint.tls.enable-cert-revocation-checking` | boolean |  | `false` | whether to enable certificate revocation checking per https://tersesystems.com/blog/2014/03/22/fixing-certificate-revocation/ |
| `config.initial-network.server-endpoint.tls.minimum-server-protocol-version` | string |  | `TLSv1.2` | minimum supported TLS protocol. Set None (or null in config file) to use default settings defaultMinimumServerProtocol |
| `config.initial-network.server-endpoint.tls.private-key-file` | string (PemFile) | yes |  | a file containing the server's private key. The key must not use a password. |
| `config.initial-network.server-endpoint.tls.trust-collection-file.type` | one of `pem-file`, `pem-string` | yes |  | A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
| `config.leader-selection-policy-config-for-pv-34.how-long-to-blacklist.type` | one of `linear`, `linear-with-parameters`, `exponential`, `no-blacklisting` | yes |  |  |
| `config.leader-selection-policy-config-for-pv-34.how-many-can-we-blacklist` | one of `num-faults-tolerated`, `no-blacklisting` | yes |  | The number of peers that can be simultaneously blacklisted, and defaults to the number of tolerated faults: f = (N-1)/3. |
| `config.manual-vacuum-enabled` | boolean |  | `false` | If `true`, the BFT block orderer will attempt to manually vacuum each partition after it becomes cold. |
| `config.max-batch-creation-interval` | duration |  | `100 milliseconds` | The maximum amount of time the mempool will wait for incoming requests before creating and sending a batch to the availability module. |
| `config.max-mempool-queue-size` | int |  | `DefaultMaxMempoolQueueSize` ‡ | The maximum number of pending requests that will be held in the in-memory mempool. Once this queue size is reached, subsequent requests are rejected with a mempool overloaded error. |
| `config.min-requests-in-batch` | int |  | `3` | When the mempool does not create and send a batch due to other factors (see maxBatchCreationInterval), this is the number of requests that the mempool will wait for before shipping a batch to the availability module. |
| `config.network-send-attempts` | int (PositiveInt) |  | `5` | The maximum number of gRPC message send attempts before dropping the message. |
| `config.network-send-retry-jitter-cap` | duration (PositiveFiniteDuration) |  | `DefaultNetworkSendRetryMaximumDelay` ‡ | The maximum jittered delay that a node will use to wait between consecutive gRPC message send attempts. The minimum delay is added to the jittered delay. |
| `config.network-send-retry-minimum-delay` | duration (PositiveFiniteDuration) |  | `DefaultNetworkSendRetryMinimumDelay` ‡ | The minimum delay between consecutive gRPC message send attempts. This prevents a node from overwhelming peers with rapid successive gRPC message sends, for example if the jitter returns very low values. |
| `config.output-enqueue-max-retries` | int |  | `DefaultOutputEnqueueMaxRetries` ‡ | The maximum number of retry attempts when enqueuing ordered output blocks for delivery to the sequencer core. |
| `config.output-enqueue-max-retry-delay` | duration |  | `5 seconds` | The maximum delay between retry attempts when enqueuing ordered output blocks for delivery to the sequencer core. |
| `config.output-fetch-how-many-recipients` | int (PositiveInt) |  | `2` | The number of recipients that a batch will be attempted to be fetched from in parallel. |
| `config.output-fetch-minimum-delay` | duration |  | `200 milliseconds` | The minimum delay between consecutive output fetch attempts. This prevents a node from overwhelming peers with rapid successive fetch requests, for example if the jitter returns very low values. |
| `config.output-fetch-timeout` | duration |  | `200 milliseconds` | The baseline timeout that a node's availability module will wait while fetching batch data from a peer before giving up and trying a different peer. Timeouts for subsequent fetch attempts are increased via exponential backoff and random jitter between 0 and the computed value. The unit is important, as the exponential is computed on the non-converted value. |
| `config.output-fetch-timeout-cap` | duration |  | `200 milliseconds` | The maximum jittered duration that a node's availability module will wait while fetching batch data from a peer. The minimum delay is added to the jittered delay. |
| `config.output-size-of-chunk-of-epochs-to-load-at-start` | int |  | `10` | The number of epochs to load per chunk when restoring output state from the database on startup. Larger values may speed up startup but consume more memory. |
| `config.segment-length-for-pv-34` | int |  | _unset_ | Optionally, the number of blocks per segment (only taken into account in Protocol Version 34). If set to None, the default epoch length is used. |
| `config.send-blacklist-ttl` | duration |  | `3 minutes` | The time-to-live for blacklisting randomly chosen peers that have failed to respond to a request as part of a workflow (e.g., batches fetching or a state transfer session). After this time has elapsed, the peer is removed from the blacklist and can be re-contacted. |
| `config.sequencer-core-subscription-config.pause-orderer-threshold-buffer-size` | int |  | `5000` | The threshold buffer size after which the sequencer core signals the orderer to pause. |
| `config.sequencer-core-subscription-config.pekko-queue-source-buffer-size` | int |  | `5000` | The buffer size of the Pekko source queue that provides blocks from the orderer to the sequencer core. |
| `config.sequencer-core-subscription-config.resume-orderer-threshold-buffer-size` | int |  | `1000` | The threshold buffer size at or below which the sequencer core signals the orderer to resume after a pause. |
| `config.standalone.blacklist-leader-selection-policy-config.how-long-to-blacklist.type` | one of `linear`, `linear-with-parameters`, `exponential`, `no-blacklisting` | yes |  |  |
| `config.standalone.blacklist-leader-selection-policy-config.how-many-can-we-blacklist` | one of `num-faults-tolerated`, `no-blacklisting` | yes |  | The number of peers that can be simultaneously blacklisted, and defaults to the number of tolerated faults: f = (N-1)/3. |
| `config.standalone.max-batches-per-block-proposal` | int | yes |  | The maximum number of batches that can be included in a single block proposal. |
| `config.standalone.max-requests-in-batch` | int | yes |  | The maximum number of requests that can be included in a single batch. |
| `config.standalone.pbft-view-change-timeout` | duration | yes |  | The base duration to use for view change timeouts for ISS segments. |
| `config.standalone.peers` | array of BftBlockOrderingStandalonePeerConfig | yes |  | The list of peers in the standalone network, including their sequencer IDs and public keys. |
| `config.standalone.peers[].sequencer-id` | string | yes |  | The sequencer ID of the peer. |
| `config.standalone.peers[].signing-public-key-proto-file` | path | yes |  | The file containing the public key used to verify incoming messages from the peer. |
| `config.standalone.segment-length` | int | yes |  | The number of blocks per segment. |
| `config.standalone.signing-private-key-proto-file` | path | yes |  | The file containing the private key used to sign outgoing messages. |
| `config.standalone.signing-public-key-proto-file` | path | yes |  | The file containing the public key used to verify incoming messages. |
| `config.standalone.test-slowdown.post-ordering-delay.type` | one of `constant-distribution`, `weighted-distribution`, `linear-distribution`, `power-distribution` | yes |  |  |
| `config.standalone.test-slowdown.send-delay.default-delay-distribution.type` | one of `constant-distribution`, `weighted-distribution`, `linear-distribution`, `power-distribution` | yes |  |  |
| `config.standalone.test-slowdown.send-delay.delays-by-recipients` | array of DelayByRecipients |  | `[]` | Optional list of specific delay distributions and failure probabilities applied to specific recipients. |
| `config.standalone.test-slowdown.send-delay.delays-by-recipients[].delay-distribution.type` | one of `constant-distribution`, `weighted-distribution`, `linear-distribution`, `power-distribution` | yes |  |  |
| `config.standalone.test-slowdown.send-delay.delays-by-recipients[].instance-names` | array of string | yes |  |  |
| `config.standalone.test-slowdown.send-delay.delays-by-recipients[].probability-of-grpc-send-accepted-by-flow-control` | number |  | _unset_ |  |
| `config.standalone.test-slowdown.send-delay.delays-by-recipients[].probability-of-grpc-send-success` | number |  | _unset_ |  |
| `config.standalone.test-slowdown.topology-delay.broadcast-request-probability` | number |  | _unset_ | Optional probability of a submission request being a broadcast, which triggers a deeper and more costly inspection about whether it may affect the ordering topology. |
| `config.standalone.test-slowdown.topology-delay.get-ordering-topology-delay.type` | one of `constant-distribution`, `weighted-distribution`, `linear-distribution`, `power-distribution` | yes |  |  |
| `config.standalone.test-slowdown.topology-delay.pending-topology-changes-probability` | number |  | _unset_ | Optional probability of the ordering topology returned by `getOrderingTopology` signalling that there are pending topology changes, which triggers getting an up-to-date ordering topology again before starting the subsequent epoch, regardless of whether broadcast submission requests are going to be ordered before the end of the epoch. |
| `config.standalone.test-slowdown.topology-delay.possible-ordering-topology-change-in-request-probability` | number |  | _unset_ | Optional probability of detecting that a broadcast submission request may also alter the ordering topology for the subsequent epoch, which triggers getting an up-to-date ordering topology via `getOrderingTopology` before starting a new epoch. |
| `config.standalone.test-slowdown.topology-delay.request-inspection-delay.type` | one of `constant-distribution`, `weighted-distribution`, `linear-distribution`, `power-distribution` | yes |  |  |
| `config.standalone.this-sequencer-id` | string | yes |  | The sequencer ID of the local node. |
| `config.storage.type` | one of `memory`, `h2`, `postgres` | yes | `memory` † | Determines how a node stores persistent data. |
| `config.view-change-timeout-override` | duration |  | _unset_ | Optionally, the base duration to use for view change timeouts for ISS segments. If specified, this value will be used instead of the pbftViewChangeTimeout set in the network-wide SequencingParameters topology transaction. Note that this value is not persisted (in-memory only), and must be set back to None to return using the SequencingParameters value. |

</Tab>
</Tabs>

Further types inside this one: [SequencerWriterConfig](#sequencerwriterconfig) at `block.writer.type`, [BatchAggregatorConfig](#batchaggregatorconfig) at `config.batch-aggregator.type`, [PemFileOrString](#pemfileorstring) at `config.initial-network.peer-endpoints[].tls-config.client-cert.cert-chain-file.type`, [PemFileOrString](#pemfileorstring) at `config.initial-network.peer-endpoints[].tls-config.trust-collection-file.type`, [PemFileOrString](#pemfileorstring) at `config.initial-network.server-endpoint.external-tls-config.client-cert.cert-chain-file.type`, [PemFileOrString](#pemfileorstring) at `config.initial-network.server-endpoint.external-tls-config.trust-collection-file.type`, [PemFileOrString](#pemfileorstring) at `config.initial-network.server-endpoint.tls.cert-chain-file.type`, [ServerAuthRequirementConfig](#serverauthrequirementconfig) at `config.initial-network.server-endpoint.tls.client-auth.type`, [PemFileOrString](#pemfileorstring) at `config.initial-network.server-endpoint.tls.client-auth.admin-client.cert-chain-file.type`, [PemFileOrString](#pemfileorstring) at `config.initial-network.server-endpoint.tls.trust-collection-file.type`, [HowLongToBlacklist](#howlongtoblacklist) at `config.leader-selection-policy-config-for-pv-34.how-long-to-blacklist.type`, [HowLongToBlacklist](#howlongtoblacklist) at `config.standalone.blacklist-leader-selection-policy-config.how-long-to-blacklist.type`, [FiniteDurationDistribution](#finitedurationdistribution) at `config.standalone.test-slowdown.post-ordering-delay.type`, [FiniteDurationDistribution](#finitedurationdistribution) at `config.standalone.test-slowdown.send-delay.default-delay-distribution.type`, [FiniteDurationDistribution](#finitedurationdistribution) at `config.standalone.test-slowdown.send-delay.delays-by-recipients[].delay-distribution.type`, [FiniteDurationDistribution](#finitedurationdistribution) at `config.standalone.test-slowdown.topology-delay.get-ordering-topology-delay.type`, [FiniteDurationDistribution](#finitedurationdistribution) at `config.standalone.test-slowdown.topology-delay.request-inspection-delay.type`, [StorageConfig](#storageconfig) at `config.storage.type`.

## SequencerWriterConfig

Configuration for the database based sequencer writer

Set `type` to one of `low-latency`, `high-throughput`.

Occurs at:

- `canton.sequencers.<sequencer>.sequencer.block.writer`
- `canton.sequencers.<sequencer>.sequencer.writer`

### `type = low-latency`

Use to have events immediately flushed to the database. Useful for decreasing latency however at a high throughput a large number of writes will be detrimental for performance.

<Tabs>
<Tab title="HOCON">

```hocon
canton.sequencers.<sequencer>.sequencer.writer {
  type = low-latency
  # buffered-events-max-memory = <int>
  buffered-events-preload-batch-size = 50
  # checkpoint-backfill-parallelism = <int>
  checkpoint-interval = "30 seconds"
  # commit-mode-validation = synchronous|local
  event-write-batch-max-size = 1
  event-write-max-concurrency = 1
  payload-queue-size = 1000
  payload-to-event-margin = "60 seconds"
  payload-write-batch-max-size = 1
  payload-write-max-concurrency = 2
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `buffered-events-max-memory` | int (BytesUnit) |  | `DefaultBufferedEventsMaxMemory` ‡ |  |
| `buffered-events-preload-batch-size` | int (PositiveInt) |  | `50` |  |
| `checkpoint-backfill-parallelism` | int (PositiveInt) |  | `DefaultCheckpointBackfillParallelism` ‡ |  |
| `checkpoint-interval` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `commit-mode-validation` | one of `synchronous`, `local` |  | `CommitMode.Default.some` ‡ |  |
| `event-write-batch-max-size` | int |  | `1` |  |
| `event-write-max-concurrency` | int |  | `1` |  |
| `payload-queue-size` | int |  | `1000` |  |
| `payload-to-event-margin` | duration (NonNegativeFiniteDuration) |  | `60 seconds` |  |
| `payload-write-batch-max-size` | int |  | `1` |  |
| `payload-write-max-concurrency` | int |  | `2` |  |

</Tab>
</Tabs>

### `type = high-throughput`

Creates batches of incoming events to minimize the number of writes to the database. Useful for a high throughput usecase when batches will be quickly filled and written. Will be detrimental for latency if used and a lower throughput of events causes writes to always be delayed to the batch max duration.

<Tabs>
<Tab title="HOCON">

```hocon
canton.sequencers.<sequencer>.sequencer.writer {
  type = high-throughput
  # buffered-events-max-memory = <int>
  buffered-events-preload-batch-size = 50
  # checkpoint-backfill-parallelism = <int>
  checkpoint-interval = "30 seconds"
  # commit-mode-validation = synchronous|local
  event-write-batch-max-size = 100
  event-write-max-concurrency = 1
  payload-queue-size = 1000
  payload-to-event-margin = "60 seconds"
  payload-write-batch-max-size = 50
  payload-write-max-concurrency = 4
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `buffered-events-max-memory` | int (BytesUnit) |  | `DefaultBufferedEventsMaxMemory` ‡ |  |
| `buffered-events-preload-batch-size` | int (PositiveInt) |  | `50` |  |
| `checkpoint-backfill-parallelism` | int (PositiveInt) |  | `DefaultCheckpointBackfillParallelism` ‡ |  |
| `checkpoint-interval` | duration (NonNegativeFiniteDuration) |  | `30 seconds` |  |
| `commit-mode-validation` | one of `synchronous`, `local` |  | `CommitMode.Default.some` ‡ |  |
| `event-write-batch-max-size` | int |  | `100` |  |
| `event-write-max-concurrency` | int |  | `1` |  |
| `payload-queue-size` | int |  | `1000` |  |
| `payload-to-event-margin` | duration (NonNegativeFiniteDuration) |  | `60 seconds` |  |
| `payload-write-batch-max-size` | int |  | `50` |  |
| `payload-write-max-concurrency` | int |  | `4` |  |

</Tab>
</Tabs>

## ServerAuthRequirementConfig

Configuration on whether server requires auth, requests auth, or no auth

Set `type` to one of `require`, `optional`, `none`.

Occurs at:

- `canton.mediators.<mediator>.admin-api.tls.client-auth`
- `canton.participants.<participant>.admin-api.tls.client-auth`
- `canton.participants.<participant>.ledger-api.tls.client-auth`
- `canton.sequencers.<sequencer>.admin-api.tls.client-auth`
- `canton.sequencers.<sequencer>.sequencer.config.initial-network.server-endpoint.tls.client-auth`

### `type = require`

A variant of ServerAuthRequirementConfig by which the server requires auth from clients

<Tabs>
<Tab title="HOCON">

```hocon
canton.mediators.<mediator>.admin-api.tls.client-auth {
  type = require
  admin-client {
    cert-chain-file {
      type = pem-file|pem-string   # required
    }
    private-key-file = "..."   # required
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `admin-client.cert-chain-file.type` | one of `pem-file`, `pem-string` | yes |  | A class representing either an existing PEM file path or an inlined PEM string for configuration file fields |
| `admin-client.private-key-file` | string (PemFile) | yes |  |  |

</Tab>
</Tabs>

Further types inside this one: [PemFileOrString](#pemfileorstring) at `admin-client.cert-chain-file.type`.

### `type = optional`

```hocon
canton.mediators.<mediator>.admin-api.tls.client-auth {
  type = optional
  # takes no further keys
}
```

### `type = none`

```hocon
canton.mediators.<mediator>.admin-api.tls.client-auth {
  type = none
  # takes no further keys
}
```

## StorageConfig

Determines how a node stores persistent data.

Set `type` to one of `memory`, `h2`, `postgres`.

Occurs at:

- `canton.mediators.<mediator>.storage`
- `canton.participants.<participant>.storage`
- `canton.sequencers.<sequencer>.sequencer.config.storage`
- `canton.sequencers.<sequencer>.storage`

### `type = memory`

Dictates that persistent data is stored in memory. So in fact, the data is not persistent. It is deleted whenever the node is stopped.

<Tabs>
<Tab title="HOCON">

```hocon
canton.mediators.<mediator>.storage {
  type = memory
  parameters {
    connection-allocation {
      # num-ledger-api = <int>
      # num-reads = <int>
      # num-writes = <int>
    }
    connection-timeout = "5 seconds"
    fail-fast-on-startup = true
    failed-to-fatal-delay = "5 minutes"
    # max-connections = <int>
    migrate-and-start = false
    migrations-paths = [ ]
    partitions {
      initial-bft-orderer-tables-partition-size = 1500
    }
    repeatable-migrations-paths = [ ]
    unsafe-baseline-on-migrate = false
    unsafe-clean-on-validation-error = false
    # warn-on-slow-query = <duration>
    warn-on-slow-query-interval = "5 seconds"
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `parameters.connection-allocation.num-ledger-api` | int (PositiveInt) |  | _unset_ |  |
| `parameters.connection-allocation.num-reads` | int (PositiveInt) |  | _unset_ |  |
| `parameters.connection-allocation.num-writes` | int (PositiveInt) |  | _unset_ |  |
| `parameters.connection-timeout` | duration (NonNegativeFiniteDuration) |  | `5 seconds` (in practice `30 seconds`) ◊ | How long to wait for acquiring a database connection |
| `parameters.fail-fast-on-startup` | boolean |  | `true` | If true, the node will fail-fast when the database cannot be connected to If false, the node will wait indefinitely for the database to come up |
| `parameters.failed-to-fatal-delay` | duration (NonNegativeFiniteDuration) |  | `5 minutes` | Delay after which, if the storage is continuously in a Failed state, it will escalate to Fatal. The default value is 5 minutes. Components that use the storage as a health dependency can then determine how to react. Currently, the sequencer declares it as a fatal dependency for its liveness health, which means it will transition to NOT_SERVING if this delay is exceeded, allowing a monitoring infrastructure to restart it. **NOTE**: Currently this only applies to DbStorageSingle, which is only used by the sequencer. TODO(i24240): Apply the same behavior to `DbStorageMulti` |
| `parameters.max-connections` | int (PositiveInt) |  | _unset_ | Allows for setting the maximum number of db connections used by Canton and the ledger API server. If None, the value will be auto-detected from the number of processors. Has no effect, if the number of connections is already set via slick options (i.e., `config.numThreads`). |
| `parameters.migrate-and-start` | boolean |  | `false` | if true, db migrations will be applied to the database (default is to abort start if db migrates are pending to force an explicit upgrade) |
| `parameters.migrations-paths` | array of string |  | `[]` | Where should database migrations be read from. Enables specialized DDL for different database servers (e.g. Postgres). |
| `parameters.partitions.initial-bft-orderer-tables-partition-size` | int |  | `1500` | Initial partition size for bft-orderer tables. Note that this config is only read once, during the initial database setup and later changes to this value won't have any effect. This is also only used in Postgres setups. |
| `parameters.repeatable-migrations-paths` | array of string |  | `[]` | Additional locations to scan for migrations. The goal is to be able to provide additional database table settings under this path, i.e. vacuum/analyze settings for Postgres. The paths should only contain repeatable migrations (files like `R__table_settings.sql`). |
| `parameters.unsafe-baseline-on-migrate` | boolean |  | `false` | TO BE USED ONLY FOR TESTING! &lt;p&gt;Whether to automatically call baseline when migrate is executed against a non-empty schema with no schema history table. This schema will then be baselined with the \{@code baselineVersion\} before executing the migrations. Only migrations above \{@code baselineVersion\} will then be applied.&lt;/p&gt; &lt;p&gt;This is useful for databases projects where the initial vendor schema is not empty&lt;/p&gt; If baseline should be called on migrate for non-empty schemas, \{ @code false\} if not. (default: \{ @code false\}) |
| `parameters.unsafe-clean-on-validation-error` | boolean |  | `false` | TO BE USED ONLY FOR TESTING! Clean the database if validation during DB migration fails. |
| `parameters.warn-on-slow-query` | duration (PositiveFiniteDuration) |  | _unset_ | Optional time when we start logging a query as slow. |
| `parameters.warn-on-slow-query-interval` | duration (PositiveFiniteDuration) |  | `5 seconds` | How often to repeat the logging statement for slow queries. |

</Tab>
</Tabs>

### `type = h2`

<Tabs>
<Tab title="HOCON">

```hocon
canton.mediators.<mediator>.storage {
  type = h2
  parameters {
    connection-allocation {
      # num-ledger-api = <int>
      # num-reads = <int>
      # num-writes = <int>
    }
    connection-timeout = "5 seconds"
    fail-fast-on-startup = true
    failed-to-fatal-delay = "5 minutes"
    # max-connections = <int>
    migrate-and-start = false
    migrations-paths = [ ]
    partitions {
      initial-bft-orderer-tables-partition-size = 1500
    }
    repeatable-migrations-paths = [ ]
    unsafe-baseline-on-migrate = false
    unsafe-clean-on-validation-error = false
    # warn-on-slow-query = <duration>
    warn-on-slow-query-interval = "5 seconds"
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `parameters.connection-allocation.num-ledger-api` | int (PositiveInt) |  | _unset_ |  |
| `parameters.connection-allocation.num-reads` | int (PositiveInt) |  | _unset_ |  |
| `parameters.connection-allocation.num-writes` | int (PositiveInt) |  | _unset_ |  |
| `parameters.connection-timeout` | duration (NonNegativeFiniteDuration) |  | `5 seconds` (in practice `30 seconds`) ◊ | How long to wait for acquiring a database connection |
| `parameters.fail-fast-on-startup` | boolean |  | `true` | If true, the node will fail-fast when the database cannot be connected to If false, the node will wait indefinitely for the database to come up |
| `parameters.failed-to-fatal-delay` | duration (NonNegativeFiniteDuration) |  | `5 minutes` | Delay after which, if the storage is continuously in a Failed state, it will escalate to Fatal. The default value is 5 minutes. Components that use the storage as a health dependency can then determine how to react. Currently, the sequencer declares it as a fatal dependency for its liveness health, which means it will transition to NOT_SERVING if this delay is exceeded, allowing a monitoring infrastructure to restart it. **NOTE**: Currently this only applies to DbStorageSingle, which is only used by the sequencer. TODO(i24240): Apply the same behavior to `DbStorageMulti` |
| `parameters.max-connections` | int (PositiveInt) |  | _unset_ | Allows for setting the maximum number of db connections used by Canton and the ledger API server. If None, the value will be auto-detected from the number of processors. Has no effect, if the number of connections is already set via slick options (i.e., `config.numThreads`). |
| `parameters.migrate-and-start` | boolean |  | `false` | if true, db migrations will be applied to the database (default is to abort start if db migrates are pending to force an explicit upgrade) |
| `parameters.migrations-paths` | array of string |  | `[]` | Where should database migrations be read from. Enables specialized DDL for different database servers (e.g. Postgres). |
| `parameters.partitions.initial-bft-orderer-tables-partition-size` | int |  | `1500` | Initial partition size for bft-orderer tables. Note that this config is only read once, during the initial database setup and later changes to this value won't have any effect. This is also only used in Postgres setups. |
| `parameters.repeatable-migrations-paths` | array of string |  | `[]` | Additional locations to scan for migrations. The goal is to be able to provide additional database table settings under this path, i.e. vacuum/analyze settings for Postgres. The paths should only contain repeatable migrations (files like `R__table_settings.sql`). |
| `parameters.unsafe-baseline-on-migrate` | boolean |  | `false` | TO BE USED ONLY FOR TESTING! &lt;p&gt;Whether to automatically call baseline when migrate is executed against a non-empty schema with no schema history table. This schema will then be baselined with the \{@code baselineVersion\} before executing the migrations. Only migrations above \{@code baselineVersion\} will then be applied.&lt;/p&gt; &lt;p&gt;This is useful for databases projects where the initial vendor schema is not empty&lt;/p&gt; If baseline should be called on migrate for non-empty schemas, \{ @code false\} if not. (default: \{ @code false\}) |
| `parameters.unsafe-clean-on-validation-error` | boolean |  | `false` | TO BE USED ONLY FOR TESTING! Clean the database if validation during DB migration fails. |
| `parameters.warn-on-slow-query` | duration (PositiveFiniteDuration) |  | _unset_ | Optional time when we start logging a query as slow. |
| `parameters.warn-on-slow-query-interval` | duration (PositiveFiniteDuration) |  | `5 seconds` | How often to repeat the logging statement for slow queries. |

</Tab>
</Tabs>

### `type = postgres`

<Tabs>
<Tab title="HOCON">

```hocon
canton.mediators.<mediator>.storage {
  type = postgres
  parameters {
    connection-allocation {
      # num-ledger-api = <int>
      # num-reads = <int>
      # num-writes = <int>
    }
    connection-timeout = "5 seconds"
    fail-fast-on-startup = true
    failed-to-fatal-delay = "5 minutes"
    # max-connections = <int>
    migrate-and-start = false
    migrations-paths = [ ]
    partitions {
      initial-bft-orderer-tables-partition-size = 1500
    }
    repeatable-migrations-paths = [ ]
    unsafe-baseline-on-migrate = false
    unsafe-clean-on-validation-error = false
    # warn-on-slow-query = <duration>
    warn-on-slow-query-interval = "5 seconds"
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `parameters.connection-allocation.num-ledger-api` | int (PositiveInt) |  | _unset_ |  |
| `parameters.connection-allocation.num-reads` | int (PositiveInt) |  | _unset_ |  |
| `parameters.connection-allocation.num-writes` | int (PositiveInt) |  | _unset_ |  |
| `parameters.connection-timeout` | duration (NonNegativeFiniteDuration) |  | `5 seconds` (in practice `30 seconds`) ◊ | How long to wait for acquiring a database connection |
| `parameters.fail-fast-on-startup` | boolean |  | `true` | If true, the node will fail-fast when the database cannot be connected to If false, the node will wait indefinitely for the database to come up |
| `parameters.failed-to-fatal-delay` | duration (NonNegativeFiniteDuration) |  | `5 minutes` | Delay after which, if the storage is continuously in a Failed state, it will escalate to Fatal. The default value is 5 minutes. Components that use the storage as a health dependency can then determine how to react. Currently, the sequencer declares it as a fatal dependency for its liveness health, which means it will transition to NOT_SERVING if this delay is exceeded, allowing a monitoring infrastructure to restart it. **NOTE**: Currently this only applies to DbStorageSingle, which is only used by the sequencer. TODO(i24240): Apply the same behavior to `DbStorageMulti` |
| `parameters.max-connections` | int (PositiveInt) |  | _unset_ | Allows for setting the maximum number of db connections used by Canton and the ledger API server. If None, the value will be auto-detected from the number of processors. Has no effect, if the number of connections is already set via slick options (i.e., `config.numThreads`). |
| `parameters.migrate-and-start` | boolean |  | `false` | if true, db migrations will be applied to the database (default is to abort start if db migrates are pending to force an explicit upgrade) |
| `parameters.migrations-paths` | array of string |  | `[]` | Where should database migrations be read from. Enables specialized DDL for different database servers (e.g. Postgres). |
| `parameters.partitions.initial-bft-orderer-tables-partition-size` | int |  | `1500` | Initial partition size for bft-orderer tables. Note that this config is only read once, during the initial database setup and later changes to this value won't have any effect. This is also only used in Postgres setups. |
| `parameters.repeatable-migrations-paths` | array of string |  | `[]` | Additional locations to scan for migrations. The goal is to be able to provide additional database table settings under this path, i.e. vacuum/analyze settings for Postgres. The paths should only contain repeatable migrations (files like `R__table_settings.sql`). |
| `parameters.unsafe-baseline-on-migrate` | boolean |  | `false` | TO BE USED ONLY FOR TESTING! &lt;p&gt;Whether to automatically call baseline when migrate is executed against a non-empty schema with no schema history table. This schema will then be baselined with the \{@code baselineVersion\} before executing the migrations. Only migrations above \{@code baselineVersion\} will then be applied.&lt;/p&gt; &lt;p&gt;This is useful for databases projects where the initial vendor schema is not empty&lt;/p&gt; If baseline should be called on migrate for non-empty schemas, \{ @code false\} if not. (default: \{ @code false\}) |
| `parameters.unsafe-clean-on-validation-error` | boolean |  | `false` | TO BE USED ONLY FOR TESTING! Clean the database if validation during DB migration fails. |
| `parameters.warn-on-slow-query` | duration (PositiveFiniteDuration) |  | _unset_ | Optional time when we start logging a query as slow. |
| `parameters.warn-on-slow-query-interval` | duration (PositiveFiniteDuration) |  | `5 seconds` | How often to repeat the logging statement for slow queries. |

</Tab>
</Tabs>

## TrafficEnforcementServerConfig

Set `type` to one of `internal`.

Occurs at:

- `canton.participants.<participant>.traffic-accounting.traffic-enforcement-server`

### `type = internal`

Configuration for the internal, in-process traffic enforcement server.

<Tabs>
<Tab title="HOCON">

```hocon
canton.participants.<participant>.traffic-accounting.traffic-enforcement-server {
  type = internal
  account-lookup-timeout = "20 seconds"
  database-query-timeout = "1 second"
  in-process-tea-server-name = "TeaGrpcInProcServer"
  projection {
    handler-retry-delay = "1 second"
    # initial-completion-offset-begin-exclusive = <int>
    # max-handler-retries = <int>
    max-projection-restart-backoff = "30 seconds"
    min-projection-restart-backoff = "1 second"
    # projection-max-restarts = <int>
    projection-restart-random-factor = 0.2
  }
}
```


</Tab>
<Tab title="Table">

| Key | Type | Required | Default | Description |
|---|---|---|---|---|
| `account-lookup-timeout` | duration (PositiveFiniteDuration) |  | `20 seconds` | Total per-call budget for the `GetAccount` RPC, covering the client's retries around `databaseQueryTimeout`. Must be strictly greater than `databaseQueryTimeout`, so a timed-out query still leaves room for a retry. |
| `database-query-timeout` | duration (PositiveFiniteDuration) |  | `1 second` | Per-attempt deadline for the single database read backing `GetAccount`. Enforced on PostgreSQL via `SET LOCAL statement_timeout`, which is expressed in whole milliseconds, so this must be at least one millisecond. Has no effect on H2 (no millisecond-level query timeout setting). |
| `in-process-tea-server-name` | string |  | `TeaGrpcInProcServer` | The name of the in-process gRPC serving the traffic service API of the traffic enforcement server |
| `projection.handler-retry-delay` | duration (PositiveFiniteDuration) |  | `1 second` | delay between restarts of retrying a failed event processing |
| `projection.initial-completion-offset-begin-exclusive` | int |  | _unset_ | the first ever time the TEA is started it will have no prior offset to start its completion stream from. By default it will start at ledgerEnd. Use this config to override the offset at which it will start. |
| `projection.max-handler-retries` | int (PositiveInt) |  | `PositiveInt.MaxValue` ‡ | max number of retries on projection handler failure |
| `projection.max-projection-restart-backoff` | duration (PositiveFiniteDuration) |  | `30 seconds` | maximum amount of time before between retries of the projection on failure |
| `projection.min-projection-restart-backoff` | duration (PositiveFiniteDuration) |  | `1 second` | minimum amount of time before retrying the projection on failure |
| `projection.projection-max-restarts` | int |  | `Int.MaxValue` ‡ |  |
| `projection.projection-restart-random-factor` | number |  | `0.2` |  |

</Tab>
</Tabs>
