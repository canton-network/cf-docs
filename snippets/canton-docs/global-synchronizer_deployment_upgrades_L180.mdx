```bash
# Stop the validator
./stop.sh

# Update to the new version
export IMAGE_TAG=<NEW_VERSION>

# Start with migration flags
./start.sh -s "<SPONSOR_SV_URL>" -p "<PARTY_HINT>" \
  -m "<NEW_MIGRATION_ID>" -M -w
```
